# 0292 — libghostty-spm cannot express per-surface command or wait_after_command, forcing an app per Tab for -c panes

| | |
|---|---|
| **Status** | resolved |
| **Module** | GhosttyKit |
| **Platform** | macOS |
| **First seen** | 2026-07-31 |
| **Closed** | 2026-08-01 |
| **Branch** | issue/0292 |

## Description

The C API already supports everything needed to run every Tab on one shared `ghostty_app_t`: `ghostty_surface_config_s` carries per-surface `command`, `env_vars` / `env_var_count`, `wait_after_command`, `working_directory`, and `font_size` (`ghostty.h:479-496` @ 1.3.2; verified against `BattyKit/.build/artifacts/libghostty-spm/libghostty/GhosttyKit.xcframework/macos-arm64_x86_64/Headers/libghostty/ghostty.h` — the earlier `452-469` citation was against a stale 1.2.2 header). But the libghostty-spm wrapper at 1.3.2 wires up only `env_vars`, `font_size`, and `working_directory` (`TerminalController+Surface.swift:22-40` @ 1.3.2) — **not `command` and not `wait_after_command`**.

The consequence for Batty: after #0287 moves ordinary Tabs onto a shared controller, `-c` panes (#0282) must stay on dedicated per-pane controllers, because their `command` override and `wait-after-command` can only be expressed at controller (app-config) level (`TabRuntime.swift:143-209`). Adding the two missing fields to `TerminalSurfaceOptions` and `createSurface` is small and additive — the C struct already carries both — and would let `-c` panes join the shared controller too.

This is an **upstream contribution**: file the ask at github.com/Lakr233/libghostty-spm. This Batty issue tracks the upstream ask and the follow-up dependency bump that adopts it.

## Resolution notes

> 🟡 Resolved 2026-08-01 — a reviewed, submission-ready upstream write-up is in the repo. **Nothing has been filed.**

**Read this before closing.** The deliverable here is a draft, not a filing. No issue, PR, fork, push, or comment was made against any upstream repository — that decision was explicitly reserved for the user. This issue should not move to `closed` until either the ask is filed upstream (link it here) or a decision is recorded not to file it.

## Root cause

`ghostty_surface_config_s` carries per-surface `command` and `wait_after_command` (verified at `ghostty.h:479-496`, fields at `:490` and `:494`), but libghostty-spm's `TerminalSurfaceOptions` exposes only `backend`, `fontSize`, `workingDirectory`, `envVars`, and `context`, and `createSurface` never populates the two command fields. An embedder that needs a per-Tab command override must therefore give each such Tab its own `ghostty_app_t`, since `command` is only expressible through app-level config.

## Fix

`issues/0292/upstream-issue.md` — a self-contained write-up for github.com/Lakr233/libghostty-spm: the C-API evidence, the exact gap in `createSurface`, why an embedder cannot work around it in its own tree, and a spelled-out patch shape (two new properties, `withCString`-scoped `command` population, `isEquivalent` updated, `waitAfterCommand` as `Bool?` written only when set so it preserves whatever `ghostty_surface_config_new()` established).

## Review

Reviewed by claude-opus-5 over two rounds, then one edit pass applying the reviewer's own recommendations.

Round 1 blocked on five items, two of which were substantive. Every `ghostty.h` citation had been taken from a **stale 1.2.2 header** left in DerivedData from before #0286, while the draft stated in bold that it reported against 1.3.2 — the struct is at 479-496, not 452-469. And the draft claimed no workaround existed, overlooking that `GhosttyKit` re-exports the raw C API (expensive, since it means reimplementing the wrapper's view/coordinator/callback layer, but real, and the maintainer would have named it immediately).

Round 2 approved after the reviewer personally re-verified every header line against the correct 1,227-line file and confirmed provenance by a fingerprint: 1.3.2 carries 98 `GHOSTTY_API` prefixes, 1.2.2 carries zero, so the quoted declarations could only have come from 1.3.2.

## Verification

- Every file:line citation re-verified against the pinned checkout at `b146b73` (tag 1.3.2) — by the implementer, then independently by the reviewer.
- The proposed patch shape was judged compile-plausible against 1.3.2: `Sendable` conformance holds, `const char*` imports as `UnsafePointer<CChar>` and `String.withCString` yields exactly that, the `withCString` scope correctly encloses `ghostty_surface_new`, and appending defaulted parameters preserves source compatibility.
- `scripts/build.sh unit` — 783 tests in 92 suites (markdown-only diff; regression check).
- Self-containment verified: `grep -i batty` returns nothing; no internal issue numbers or private doc names.

## Relation

- Child of umbrella #0285.
- Follows #0286 (the 1.3.2 bump establishes the current-API baseline described above).
- Unblocks the last piece of #0287, but is explicitly **not a blocker** for it — #0287 ships with `-c` panes on their own controllers.

## Expected behavior

`TerminalSurfaceOptions` exposes `command` and `waitAfterCommand`, `createSurface` populates the corresponding `ghostty_surface_config_s` fields, and Batty (after a bump to the release containing the change) moves `-c` panes onto the shared controller.

## Actual behavior

Per-surface `command` / `wait_after_command` are unreachable through the wrapper; any pane needing them needs its own `ghostty_app_t`, with its own glyph-atlas set and threads.

## Attachments

[Upstream issue draft, ready for submission](0292/upstream-issue.md)

## Notes

- Local clone for preparing the PR: `~/Developer/brennanMKE/libghostty-spm`.
- When drafting the upstream PR/issue, the precise seam is documented in `issues/0285/investigation-source.md` §5 ("Why Batty can't just fix this alone") and the Orchestrator addendum's per-Tab config table.
- `-c` panes are rare and few, so the practical memory impact of this issue alone is small; its value is completing the shared-controller design so no code path needs a per-Tab app.

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-08-01 | claude-sonnet-5 | 9,642 | 4,043 | 5,063,799 | 133,110 | $1.41 |
| 2026-08-01 | claude-opus-5 | 48 | 293 | 1,881,418 | 121,027 | $1.70 |
| 2026-08-01 | claude-sonnet-5 | 110 | 5,655 | 9,198,937 | 199,122 | $2.39 |
| 2026-08-01 | claude-sonnet-5 | 52 | 1,797 | 5,385,132 | 230,398 | $1.67 |

**Total: $7.17** (one session drafted both #0292 and #0293 and one reviewed both; costs are recorded on each issue, not doubled — do not sum across the two)
