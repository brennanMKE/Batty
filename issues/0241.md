# 0241 — Theme does not adapt to Dark/Light mode; store a separate theme per appearance

| | |
|---|---|
| **Status** | resolved |
| **Module** | Theme / Settings |
| **Platform** | macOS |
| **First seen** | 2026-06-15 |
| **Closed** | 2026-06-15 |
| **Branch** | issue/0241 |

## Description

The active Theme does not adapt when the user switches the system (or app) appearance between Dark and Light. A theme chosen while in Dark mode stays applied when macOS flips to Light, so a dark-on-black palette is left running against a light desktop (and vice versa), which reads poorly. We should keep a separate theme preference for each appearance — a Dark theme and a Light theme — pick reasonable defaults for both, and when the user changes the theme, store it against the appearance that is currently active. On an appearance change the matching theme is re-applied automatically.

Ghostty already supports this via its text config (`theme = light:<name>,dark:<name>`); Batty should offer the equivalent through its existing Theme UI rather than a config file.

## Steps to reproduce

1. Launch Batty in Dark mode and pick a dark theme from the Theme selector.
2. Switch macOS to Light appearance (System Settings → Appearance, or the auto Dark/Light schedule at sunset/sunrise).
3. Observe the terminal palette.

## Expected behavior

- Two independent theme preferences are persisted: one used while the appearance is Dark, one while it is Light.
- Reasonable built-in defaults are seeded for both modes so a fresh install looks correct in either appearance without the user configuring anything.
- Changing the theme writes to the slot for the **currently active** appearance only — choosing a theme in Light mode does not disturb the Dark choice.
- When the appearance changes (manual toggle or the auto schedule), the theme for the new appearance is applied live to all surfaces that follow the global theme, and the View → Theme chrome updates to match.

## Actual behavior

A single global theme is stored and applied regardless of appearance. The selected theme persists across a Dark↔Light switch, leaving a mismatched palette until the user manually reopens the Theme selector and picks something else.

## Resolution notes

> 🟢 Resolved 2026-06-15 — The global theme is now stored per appearance (a Dark slot and a Light slot), with built-in defaults for both; choosing a theme writes the slot for the current appearance, and a KVO observer on `NSApp.effectiveAppearance` live-re-applies the matching theme on a Dark↔Light switch. Existing users' single chosen theme is migrated into both slots; per-session theme overrides are unchanged.

The light↔dark **visual** switch is the user's sign-off, hence `resolved` (not `closed`). The full visual confirmation can be done once the Mac mini's UI-test environment recovers; the logic is unit-tested.

## Root cause

Not a bug in the usual sense — a missing feature. The app stored exactly one global theme under `co.sstools.Batty.themeName` and had no appearance awareness anywhere (no `effectiveAppearance`/`colorScheme` observation), so a theme chosen in one mode stayed applied when macOS flipped to the other. Ghostty offers the same capability via `theme = light:…,dark:…` in its text config; Batty now does it through the existing Theme UI.

## Fix

`ThemePreference` replaced the single key with two appearance slots (`…themeName.dark` / `…themeName.light`); `defaultsKey(isDark:)` and `activeTheme(isDark:)` resolve the right slot, and `applyActiveSessionTheme` consults the current-appearance slot in its global tier (the per-session `localThemeName` override still wins). A one-time `migrateAndSeedIfNeeded` (guarded by a `…themeName.migrated` flag) copies any existing legacy value into **both** slots (so upgraders keep their choice in either mode) and seeds built-in defaults on a fresh install. A new `AppearanceObserver` holds a `NSKeyValueObservation` on `NSApp.effectiveAppearance` and calls `applyGlobalThemeForCurrentAppearance()` (→ existing `applyThemeToAllSurfaces`) when the appearance flips; it is retained by `BattyAppDelegate` for the app's lifetime. The Theme selector, View → Theme menu (`BattyCommands`), Settings, and command palette write/read the slot for the current appearance (via `NSApp.effectiveAppearance` / `@Environment(\.colorScheme)`), and Settings labels which appearance's slot is being edited.

## Review

Reviewed by claude-opus-4-8, one round, approved first pass. The reviewer independently re-ran the unit gate and verified the highest-risk surface: the appearance-observation re-apply is event-origin (AppKit KVO on the main thread, outside any SwiftUI update transaction), has no constraint-dirtying/responder calls, no feedback loop (applying a theme doesn't change `effectiveAppearance` and never writes back the observed `@AppStorage` slot), and uses no `Task`-hop. It also confirmed migration runs exactly once and preserves an existing theme, default polarity is correct, and `localThemeName` overrides are intact.

## Verification

`scripts/build.sh unit` — **`** TEST SUCCEEDED **`, 336 tests in 40 suites** (319 baseline on this branch's `main` + 17 new `ThemePreferenceTests`: two-slot key selection, legacy migration into both slots, run-once guard, fresh-install default seeding, default-theme catalog presence + `isDark` polarity, per-appearance resolution). Run by implementer and reviewer. **UI tests were not run** — the Mac mini's UI-test automation is degraded (an unrelated environmental "Running Background" activation failure, see #0240); the Dark↔Light *visual* switch is inherently a user visual sign-off regardless, so the feature is `resolved` pending that confirmation.

## Files changed

- `BattyKit/Sources/BattyKit/Theme/ThemePreference.swift` — two-slot keys, `activeTheme(isDark:)`, `migrateAndSeedIfNeeded`, default seeding, `applyActiveSessionTheme`/`applyGlobalThemeForCurrentAppearance`, `NSAppearance.isDark`.
- `BattyKit/Sources/BattyKit/Theme/AppearanceObserver.swift` (new) — KVO on `NSApp.effectiveAppearance` → re-apply.
- `Batty/BattyApp.swift` — holds the `AppearanceObserver` for the app's lifetime.
- `BattyKit/Sources/BattyKit/Commands/BattyCommands.swift`, `Views/ThemeSelectorView.swift`, `Views/SettingsView.swift`, `Views/CommandPaletteView.swift` — read/write the current-appearance slot; Settings labels the active appearance.
- `BattyKit/Sources/BattyKit/Runtime/TabRuntime.swift` — `activeTheme()` delegates to the appearance-aware `ThemePreference.activeTheme()`.
- `BattyKit/Sources/BattyKit/Testing/UITestDriver.swift` — `.applyTheme` writes the appearance-specific key.
- `BattyKit/Tests/BattyKitTests/ThemePreferenceTests.swift` (new) — 17 tests.

## Gotchas

- **Defaults:** dark = `TokyoNight` (`#1a1b26`), light = `TokyoNight Day` (`#e1e2e7`) — chosen as a matched dark/light family already in the catalog; tests assert both exist and have the right `isDark` polarity, so a future catalog change that drops/renames them will fail loudly.
- **Migration copies the legacy value into BOTH slots**, not just the current appearance's, so an upgrader sees their old theme in either mode until they deliberately change one. The legacy `co.sstools.Batty.themeName` key is left in place for downgrade safety and never read again by production code.
- **Dead code (left intentionally, per review):** the public no-arg `ThemePreference.migrateAndSeedIfNeeded()` overload is never called — migration runs lazily inside `activeTheme()`. Harmless; kept as a defensible public API surface.
- **Independent of the #0234 multi-window umbrella** — but note `themeChrome` remains a single shared chrome across windows (called out in #0237's Gotchas); per-window theming is out of scope here and not in any design doc.

Current design (for whoever picks this up):

- `ThemePreference` (`BattyKit/Sources/BattyKit/Theme/ThemePreference.swift`) stores one global theme name under a single `UserDefaults` key, `co.sstools.Batty.themeName` (`ThemePreference.defaultsKey`). There is no appearance dimension.
- `ThemeSelectorView.selectTheme` (`BattyKit/Sources/BattyKit/Views/ThemeSelectorView.swift:139`) writes that one key and calls `store.applyThemeToAllSurfaces`.
- `AppStateStore.applyActiveSessionTheme` resolves the fallback chain `session.localThemeName → global key → none`. The per-session `localThemeName` override mechanism should be preserved; this change is about the *global* slot becoming two slots (Dark / Light).
- A grep for `effectiveAppearance` / `NSAppearance` / `colorScheme` across `Batty` and `BattyKit/Sources` returns nothing — there is currently no appearance observation anywhere, so the work includes observing appearance changes (e.g. `NSApp.effectiveAppearance` / KVO on `effectiveAppearance`, or a SwiftUI `@Environment(\.colorScheme)` bridge) and re-running theme application when it flips.

Design sketch:

- Replace the single global key with two keys (e.g. `…themeName.dark` and `…themeName.light`), or one composite preference. Keep `defaultsKey` resolution backward-compatible — migrate an existing single value into the slot matching the appearance at first launch after upgrade so current users don't lose their choice.
- Add appearance observation that, on change, resolves the global theme for the new appearance and calls the existing `applyThemeToAllSurfaces` path.
- The Theme selector and the View → Theme menu should make it clear which appearance's slot is being edited (it follows the current appearance), per `docs/swiftui-observation-rules.md` for any observed-state writes triggered by appearance flips.

This is a feature/enhancement rather than a regression; scope is terminal theming only (per project scope).

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-06-15 | claude-sonnet-4-6 | 82 | 3,879 | 6,260,047 | 192,582 | $2.66 |
| 2026-06-15 | claude-opus-4-8 | 3,657 | 2,790 | 974,226 | 60,133 | $0.95 |

**Total: $3.61**
