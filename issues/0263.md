# 0263 — Sidebar pane list under a session should always be sorted by row then column

| | |
|---|---|
| **Status** | resolved |
| **Module** | Sidebar / Split |
| **Platform** | macOS |
| **First seen** | 2026-07-09 |
| **Closed** | 2026-07-09 |
| **Branch** | issue/0263 |

## Description

The per-pane rows under each Session heading in the left-column sidebar (added in #0259) must always be sorted **by row, then column** — all panes in row 1 left-to-right, then all panes in row 2, and so on — and must keep that order as the split layout changes. The user's request, verbatim:

> "The list of panes under the session heading in the left column should always be sorted by row then column. New panes could be added at any time but should maintain this sort order."

To be precise about the sort keys: each pane carries a `column/row` position derived from the split tree (#0259, label form `col/row`). "Sorted by row then column" means primary key = `row` ascending, secondary key = `column` ascending — row-major order, even though the label prints column first.

## Resolution notes

> 🟢 Resolved 2026-07-09 — the sidebar pane rows are now sorted row-major (row ascending, then column) via a derived `SplitTree` accessor, so the list reads top-to-bottom, left-to-right and self-maintains as panes are split/closed/rearranged.

Visual confirmation of the sidebar order is the user's sign-off — see Gotchas.

## Root cause

The sidebar `ForEach` iterated `session.tree.allPanes`, which is depth-first tree-traversal order, not row-major. For a mixed layout like `horizontal(vertical(A,C), B)` it listed A(1/1), C(1/2), B(2/1) — row 2's pane (C) appeared before row 1 finished (B). There was no ordering applied; the list just followed tree shape.

## Fix

Added a pure computed accessor `SplitTree.panesSortedByRowThenColumn` that reads `panePositions` fresh and sorts `allPanes` by `(row, column)` ascending, and switched the sidebar `ForEach` to use it. The order is derived from the same `panePositions` that `PaneRow` uses for its label, so a row's label and its sort slot can never disagree, and it self-maintains through splits/closes/rearrangements with no stored ordering state. `allPanes` itself is untouched (its ~30 other consumers do counting/lookup where order doesn't matter), so this is purely additive. Hidden panes (#0256) remain in the list at their row-major slot.

## Review

Reviewed by claude-opus-4-8, approved first pass. The reviewer traced the issue repro against the actual `assignPositions`/`gridSpan` derivation (`horizontal(vertical(A,C),B)` → `[A,B,C]`), confirmed the sort and `PaneRow` read the same `panePositions` source, that every `allPanes` leaf has a position (nothing dropped; set-equality genuinely proven), that unique positions make the sort deterministic, and that the accessor is a pure `body` read with no observation violation.

## Verification

`scripts/build.sh` — BUILD SUCCEEDED. `scripts/build.sh unit` — 478 tests in 57 suites passed (re-run independently by the reviewer), including 5 new tests in `SplitTreePanePositionTests`: the exact issue repro (`allPanes` gives `[A,C,B]`, sorted gives `[A,B,C]`), the #0259 column-span-reservation layout (`[A,B,D,C]`), a no-op guard on an already-row-major single row, hidden-pane slot stability (#0256), and set-equality (every pane exactly once) over a 6-level alternating-split tree — all confirmed by name. Visual sidebar order was NOT confirmed headlessly (app not launched, per the "never restart the app" rule) — that's the user's sign-off.

## Files changed

- `BattyKit/Sources/BattyKit/Model/SplitTree.swift` — new pure `panesSortedByRowThenColumn` computed accessor (sorts `allPanes` by `(row, column)` from `panePositions`).
- `BattyKit/Sources/BattyKit/Views/SessionSidebarView.swift` — sidebar pane `ForEach` switched from `allPanes` to `panesSortedByRowThenColumn`.
- `BattyKit/Tests/BattyKitTests/SplitTreePanePositionTests.swift` — 5 new row-major ordering tests.

## Gotchas

- **Visual sign-off pending (user).** Marked `resolved`, not `closed`. Confirm the sidebar pane list under a session reads row-major (top-to-bottom, left-to-right) and stays that way after adding/closing/splitting panes.
- **The sort operates on `allPanes` (includes hidden panes)**, matching the current sidebar behavior of showing an eye-toggle row for hidden panes — hidden panes keep their row-major slot rather than being dropped.
- **Order is derived, never stored** — it recomputes from `panePositions` each render (same O(N) cost `PaneRow` already pays for labels), so it stays correct through any tree change with nothing to keep in sync.

## Current behavior

The sidebar emits pane rows in `session.tree.allPanes` order (`SessionSidebarView.swift`, the `ForEach(session.tree.allPanes)` around line 39), which is `allLeafPanes` — a depth-first, left/top-first traversal of the split tree. That traversal is not row-major in general. Concrete example: start with pane A, split horizontally (B appears beside it at 2/1), then split A vertically (C appears below it at 1/2). The tree is `horizontal(vertical(A, C), B)`, so the sidebar lists **A (1/1), C (1/2), B (2/1)** — row 2's pane appears before row 1 is finished.

## Expected behavior

- Pane rows under a Session heading are ordered by `row` ascending, then `column` ascending within each row. The example above lists A (1/1), B (2/1), C (1/2).
- The order holds continuously: panes added by splits (at any time, in any pane), removed panes, and rearranged layouts all re-sort into row-then-column order rather than drifting into tree-traversal order.

## Notes

- **View side** — `BattyKit/Sources/BattyKit/Views/SessionSidebarView.swift`: the `ForEach(session.tree.allPanes)` (~line 39) feeding `PaneRow`. `PaneRow` already reads `session.tree.panePositions[pane.id]` for its label (~line 263), so the sort inputs are already at hand.
- **Sort inputs** — `SplitTree.panePositions` / `PanePosition.row` and `.column` in `BattyKit/Sources/BattyKit/Model/SplitTree.swift` (`panePositions()` ~line 460, `PanePosition` ~line 506). The fix is to sort the pane array by `(row, column)` from these positions rather than taking `allPanes` order.
- The sort should be **derived** — recomputed from `panePositions` whenever the tree changes — not a stored order that needs maintenance. Since `panePositions` is itself derived from tree shape, sorting on it self-maintains through splits, closes, and rearrangements.
- Cross-references: **#0259** introduced these pane rows and the position derivation. **#0260** also edits the same `SessionSidebarView` pane-row path (row *labels* / auto-naming); the sort is orthogonal to the naming, but whoever implements either should be aware of the other.

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-07-09 | claude-sonnet-5 | 44 | 10,487 | 1,072,796 | 71,585 | $0.50 |
| 2026-07-09 | claude-opus-4-8 | 5,487 | 1,916 | 63,682 | 28,339 | $0.28 |

**Total: $0.78**
