# 0240 — Multi-window regression pass: full UI suite plus per-window terminal-pane checklist

| | |
|---|---|
| **Status** | in-progress |
| **Module** | Tests / Window / TerminalSurface |
| **Platform** | macOS |
| **First seen** | 2026-06-11 |

## Description

Phase 7 (final) of the #0234 multi-window umbrella: the dedicated regression pass after #0235–#0239 land, per `docs/multi-window-design.md` §9. The umbrella exists because multi-window crosses the app's riskiest paths (#0072/#0074/#0075 terminal lifetime, #0227/#0229/#0230 observation, #0143 pane input); this phase is the backstop for anything the per-phase verification missed.

Scope:

- **Full `BattyUITests` suite on the Mac mini** via `scripts/run-ui-tests.sh` (not raw `xcodebuild test`). The suite was written single-window — audit and update tests that assume one window, and add multi-window coverage (second window opens, per-window selection, cross-window bell jump, window close, restoration).
- **Manual `docs/terminal-pane-requirements.md` §6 checklist executed in every window** (at minimum two windows on two displays): pointer input, keyboard input, file drop, text drop, IME, overlays.
- **Log-signal audit per window** per `docs/view-hierarchy.md` §6: `created`/`released terminal view` once per tab, `attached host to window` once per window, no PTY respawns during navigation or window switching.
- Any regression found is filed as its own issue (per CLAUDE.md test strategy) and fixed before this phase resolves.

## Acceptance criteria

- Full UI suite green on the Mac mini, including the new multi-window tests.
- Manual checklist documented as executed per window, with results noted on this issue (anything not verifiable noted in `## Gotchas` — visual sign-off stays with the user; mark `resolved`, not `closed`).
- Log-signal audit clean in a two-window, two-display session.

## Relation

- Parent: [#0234](0234.md). Phase 7; depends on [#0239](0239.md) (and transitively all of #0235–#0238). Last child before the umbrella can go terminal.
- Design: `docs/multi-window-design.md` §9 (regression-risk register).

## Notes

**2026-06-15 — Blocked on a degraded UI-test environment (Mac mini reboot needed). Status stays `open`. Work parked on `issue/0240` (branch carries no code — this is a verification phase).**

Progress before the block:
- **New multi-window UI tests pass in isolation** (verified earlier the same day, after XCTest UI-automation permission was granted): `BattyUITests/MultiWindowTests` 3/3 (`testShiftCmdNOpensSecondWindow`, `testNewWindowHasIndependentSidebar`, `testFileMenuNewWindowOpensSecondWindow`) and `BattyUITests/WindowRestorationTests` 2/2 (`testToggleSidebarAffectsOnlyKeyWindow`, `testNewWindowHasOneFreshSession`). So the core New-Window and per-window-sidebar behaviors are runtime-proven.
- **Unit gate is green** on `main` (all of #0235–#0239): `scripts/build.sh unit` → 319 tests / 39 suites.

The block:
- Running the **full** `BattyUITests` suite (`scripts/run-ui-tests.sh`, ~57 tests) failed **every** test with a single harness-level error: `BattyUITestHarness.swift:22: Failed to activate application 'co.sstools.Batty.beta' (current state: Running Background)`. The app launches but stays backgrounded, so XCUITest can't activate it and every test fails at setup.
- **This is environmental, not a code regression.** Evidence: (1) the same single class that passed in isolation earlier now fails the same way; (2) it reproduces on a trivial non-multi-window class (`SidebarTests`); (3) it reproduces **on the pre-#0239 commit `6d7ccd7`** — i.e. the exact #0238 code that passed ~30 min earlier — so code is not the variable; (4) the unit gate (which launches the same host app but doesn't need it frontmost) stays green.
- **Non-destructive cleanup did NOT clear it:** killed stray `XCTRunner`/`BattyUITests-Runner`/test-built `Batty.app`, killed `testmanagerd` (respawns), removed any `Saved Application State`, confirmed no booted simulators, restarted the Dock. The "Running Background" persists.
- **Likely cause:** ~57 rapid launch/SIGKILL churns from the full-suite run degraded the macOS WindowServer/activation session. This class of failure typically clears only on a **logout or reboot** — not done autonomously (it would end the user's session and screen-sharing).

To finish this phase after a Mac-mini reboot:
1. `scripts/run-ui-tests.sh` (full suite) — expect the pre-existing single-window tests plus the new multi-window tests to pass. Any genuine per-test multi-window regression is filed as its own issue and fixed before this resolves (CLAUDE.md test strategy). Note: some pre-existing tests were written single-window and may need auditing/updating per this issue's scope.
2. Manual `docs/terminal-pane-requirements.md` §6 checklist in two windows on two displays (pointer/keyboard/file-drop/text-drop/IME/overlays) — human visual sign-off; mark `resolved` not `closed`.
3. Log-signal audit per `docs/view-hierarchy.md` §6 in a two-window session (`created`/`released terminal view` once per tab; `attached host to window` once per window — added in #0236; no PTY respawns on navigation/window-switch).

**Environment gotcha for whoever resumes:** if `scripts/run-ui-tests.sh` reports `Failed to activate application … (Running Background)`, the machine needs a reboot — it is not a code problem. Separately, if it reports `test runner hung before establishing connection`, that's a booted iOS simulator (`xcrun simctl shutdown all`); and `Timed out while enabling automation mode` is the XCTest Accessibility/Automation permission (grant it to the runner). All three are environmental and have bitten this umbrella.

**2026-06-15 (later) — Reboot cleared the "Running Background" block, but the full suite now hits a DIFFERENT environmental block: LLDB debugger-attach failure after the macOS update.**

Ran the full suite (`scripts/run-ui-tests.sh`, Xcode 26.5 / 17F42). Result: **66/66 tests failed, every one identically** with `XCTAssertTrue failed - Main window did not appear within 10s` — i.e. all failed at the harness launch-wait, before any test logic ran.

**This is environmental, not a code regression.** Evidence:
- The failure is uniform across all 66 tests, including `MultiWindowTests`/`WindowRestorationTests` that passed in isolation earlier the same day. A real regression would not fail 100% of unrelated tests at launch.
- xcodebuild logged `IDELaunchParametersSnapshot … DebuggerLLDB.DebuggerVersionStore.StoreError error 0` **exactly 66 times — once per test launch**. Each test does `Launch co.sstools.Batty.beta` → immediate `Terminate`: the app launches under the debugger, LLDB fails to attach, the app is killed, and the main window never appears → the 10s wait fails.
- Cause: the just-completed macOS update left Xcode's debugger/runtime components needing re-validation. Until they're (re)installed, LLDB can't attach to a launched app.

**Fix to apply, then re-run (one step, needs `sudo` / GUI — left for the user):**
1. `sudo xcodebuild -runFirstLaunch` (installs/validates the post-update components), **or** open Xcode.app once and let it install "additional required components" when prompted. A reboot afterward is belt-and-suspenders.
2. Re-run `scripts/run-ui-tests.sh`. If launches still die with the LLDB `StoreError`, the debugger is still not attaching — check `xcode-select -p` points at the full Xcode (`/Applications/Xcode.app/Contents/Developer`, not CommandLineTools) and that Xcode finished its component install.
3. Then proceed with the remaining Phase-7 work below (suite triage, manual §6 checklist, log-signal audit).

Full run log saved to `~/batty-uitests-0240-20260615-231851.log` (not committed).

**Code-side work already prepared (read-only audit, no app launch):** the single-window-assumption audit for this phase's "audit and update single-window tests" scope is done. At-risk spots to update once the suite can actually run: `SettingsTests.swift:36,54,74` (hard-coded `windows.element(boundBy: 1)` / `windows.count` — fragile under multi-window), and `BellFeedTests`/`SidebarTests` lookups not scoped to a specific window. Multi-window coverage gaps with no UI test yet: cross-window bell-feed routing, Settings-window + second-main-window interaction, per-window theme isolation. These edits were intentionally NOT made tonight because they can't be verified until the toolchain is unblocked.
