# 0250 — `batty <path>` creates a session in the running app; rename the CLI entry source off main.swift

| | |
|---|---|
| **Status** | resolved |
| **Module** | App / Session |
| **First seen** | 2026-06-23 |
| **Closed** | 2026-06-23 |
| **Branch** | issue/0250 |

## Description

Now that the `batty` CLI exists and installs (#0249), it needs to actually do something. Two changes, in order:

1. **Fix the entry-point source naming.** The CLI source is currently `BattyKit/Sources/batty/main.swift` using top-level code. As the tool grows it should use a proper `@main` entry point (e.g. with `swift-argument-parser`), but **`@main` conflicts with a file named `main.swift`** — SwiftPM treats `main.swift` as the implicit top-level entry, which collides with the `@main` attribute. Rename the source (e.g. `Batty CLI.swift` / `BattyCLI.swift`) and introduce a `@main` entry point.

2. **`batty <path>` tells the running Batty app to create a new Session at that path.** The primary action. Running `batty <path>` — including the common `batty .` for the current directory — should have the Batty Mac app **create a new Session whose first Pane's shell starts in that directory**. `batty .` resolves to the shell's current working directory.

## Resolution notes

> 🟢 Resolved 2026-06-23 — CLI entry renamed to a `@main` ParsableCommand; `batty <path>` (incl. `batty .`) opens a `batty://` URL that the app handles to create a new Session rooted at that directory.

Two things need your sign-off: (1) **the live end-to-end round-trip** (`batty .` → a new Session actually appears in the running app) can't be verified headlessly and needs a relaunched app + the registered URL scheme; (2) **bare `batty` with no args defaults to `batty .`** (creates a session in the CWD) rather than printing usage — a deliberate "friendly" choice the reviewer accepted but flagged for you to confirm. `--help`/`--version` still print and exit.

## Root cause

Feature work, not a bug. Starting state after #0249: the CLI was a top-level-code `main.swift` with only `version`/`help`, no way to act on the running app, and `AppStateStore`/`WindowRuntime` session creation took no starting directory.

## Fix

- **CLI entry (Part 1):** deleted `main.swift` (its top-level code conflicts with `@main`); added `BattyCLI.swift` (`@main struct BattyCLI: ParsableCommand`) + `SessionCommand.swift` (path resolve + `open` dispatch). `swift-argument-parser` added as a **package** dependency of the `batty` executable target only — deliberately NOT an Xcode `XCSwiftPackageProductDependency`, which would reintroduce the #0249 unit-test-gate regression.
- **IPC (Part 2):** a `batty://` URL scheme registered in `Configuration/Info.plist` (`CFBundleURLTypes`). The CLI resolves the path argument to an absolute, validated directory, percent-encodes it into `batty://session?path=…`, and spawns `/usr/bin/open` (which launches/activates Batty if not running). The app receives it in `BattyAppDelegate.application(_:open:)` → `BattyURLHandler.handle(_:store:)` (`@MainActor`) → `AppStateStore.addSession(workingDirectory:)`. URL/path logic lives in `SessionURLBuilder` (in BattyKit) so CLI, app, and tests share one implementation.
- **Session-at-path:** `WindowRuntime.addSession(title:)` → `addSession(title:workingDirectory:)`, threading the path into the existing `TabRuntime(workingDirectory:)` → `terminal.configuration.workingDirectory` mechanism (the same one Cmd-T / CWD-inheritance use), overriding inheritance when set and falling back when nil. The session-name cache keyed on the effective CWD auto-applies a remembered name for the directory.

## Review

Reviewed by Opus (claude-opus-4-8) — **approved first pass**. The reviewer independently confirmed observation-rule compliance (the write runs from an AppKit event-origin `application(_:open:)` callback, no `Task`-hop, no responder/constraint-dirtying AppKit calls in a SwiftUI update), verified the cold-start case can't crash (`AppStateStore.init` always seeds `windows = [window]`, so `addSession` has a host even if the URL opens before any NSWindow), that the path reaches the shell via the existing config mechanism, and that the URL is validated (malformed `batty://` URLs are logged and ignored, not trusted). Flagged the bare-`batty`-defaults-to-`.` decision for user sign-off (recorded above).

## Verification

- `scripts/build.sh unit` — **388 tests in 49 suites passed, `** TEST SUCCEEDED **`** (baseline 372/47; +16 new). Orchestrator- and reviewer-verified.
- New tests (all ran and passed): `SessionURLBuilderTests` (12 — resolve `.`/relative/absolute/`~`, reject nonexistent + file paths, URL build/parse round-trip incl. paths with spaces, wrong-scheme/host rejection) and `AppStateStoreSessionAtPathTests` (4 — explicit path carried, overrides inheritance, nil falls back, cached name applied).
- `scripts/build.sh` — `** BUILD SUCCEEDED **`; `batty` still embedded at `Batty.app/Contents/Resources/bin/batty` (arm64).
- CLI (built binary, exit codes captured directly, not through a pipe): `batty /nonexistent` → exit 1 + `batty: path does not exist or is not a directory: …`; `batty <a-file>` → exit 1; `batty --version` → exit 0 (`0.1.0`).
- `plutil -lint` on `Configuration/Info.plist` and `Batty.xcodeproj/project.pbxproj` → OK; `batty` scheme present.
- NOT verified headlessly (user sign-off): the live `open batty://…` → rendered Session round-trip in the running app, and app front-activation behavior.

## Files changed

- `BattyKit/Sources/batty/main.swift` — deleted (conflicted with `@main`).
- `BattyKit/Sources/batty/BattyCLI.swift` — `@main` ParsableCommand entry point.
- `BattyKit/Sources/batty/SessionCommand.swift` — path resolution + `open` URL dispatch.
- `BattyKit/Package.swift` / `Package.resolved` — `swift-argument-parser` package dependency for the `batty` target.
- `Configuration/Info.plist` — `CFBundleURLTypes` registering the `batty` scheme.
- `BattyKit/Sources/BattyKit/Runtime/SessionURLBuilder.swift` — `nonisolated` resolve/build/parse helpers (shared by CLI, app, tests).
- `BattyKit/Sources/BattyKit/Runtime/BattyURLHandler.swift` — `@MainActor` URL → `addSession` handler.
- `Batty/BattyApp.swift` — `application(_:open:)` routes `batty://` URLs to the handler.
- `BattyKit/Sources/BattyKit/Runtime/WindowRuntime.swift` / `AppStateStore.swift` — `addSession(title:workingDirectory:)`.
- `BattyKit/Tests/BattyKitTests/SessionURLBuilderTests.swift`, `AppStateStoreSessionAtPathTests.swift` — new tests.

## Gotchas

- **Bare `batty` (no args) == `batty .`** — `@Argument var path: String = "."` defaults to the CWD, so a bare invocation creates a session rather than printing usage. Deliberate; confirm it matches your intent.
- **Live URL round-trip is unverified** — the URL scheme only registers after a full app launch of the rebuilt app; verify `batty .` end-to-end after relaunching Batty.
- **Do NOT** wire `swift-argument-parser` (or any package executable product) as an Xcode `XCSwiftPackageProductDependency` — it breaks the `scripts/build.sh unit` gate via BattyKit's `Bundle.module` resolution (the #0249 trap; see `docs/cli-tool-install.md` §4.2).
- The app must not trust the `batty://` URL blindly — `SessionURLBuilder.sessionPath` validates scheme/host/path and ignores malformed URLs; keep that guard if the URL surface grows.

## Expected behavior

- `batty .` → Batty creates a new Session rooted at the current directory.
- `batty ~/Developer/Foo` / `batty ./sub/dir` / relative and absolute paths → new Session rooted at the resolved, absolute path.
- The path is resolved relative to the CLI's own working directory (it runs in the user's shell), then passed to the app as an absolute path.
- Sensible behavior when Batty isn't running, when the path doesn't exist, and when the path isn't a directory (see Open questions).

## Notes — design considerations (not prescriptive)

**IPC: CLI → running app.** There is currently no IPC channel between a separate process and the running Batty app. `HyperlinkOpener` is the only outward path and it's the wrong direction. Candidate mechanisms to evaluate:
- A custom **URL scheme** (`batty://session?path=…`) registered via `CFBundleURLTypes`, opened from the CLI with `NSWorkspace`/`open`, handled in the app via `onOpenURL` / `application(_:open:)`. Simplest; also auto-launches the app if not running and activates it. Path must be percent-encoded.
- A **local socket / `CFMessagePort` / XPC** channel (supacode uses a local socket — see `docs/cli-tool-install.md` §5 "CLI ↔ app communication"). More flexible (could return status, list sessions later) but more machinery, and needs its own launch-if-not-running story.

A URL scheme is likely the lightest first step for a one-way "create a session here" action; pick deliberately and note the choice. Whatever the mechanism, it is **orthogonal to the install plumbing** that #0249 delivered.

**Session creation at a path.** `AppStateStore.addSession(title:)` (`BattyKit/Sources/BattyKit/Runtime/AppStateStore.swift:233`) does not currently accept a starting directory — new Sessions inherit CWD from the previously-focused Pane or fall back to `$HOME` (see `Concepts.md` "CWD inheritance"). This work needs a way to create a Session whose first Pane/Tab/Terminal Session spawns in an explicit directory. Reuse the existing `TerminalConfiguration.workingDirectory` path that Cmd-T / CWD inheritance already use, rather than inventing a parallel mechanism. The session-name cache (exact-path match) should then auto-apply any remembered name for that directory, which is a nice side effect.

**App activation / windowing.** Batty is single-window-by-default (PRD). `batty .` should bring the app to the front and create the Session in the key window (or a window if none is open). Decide whether it launches Batty when not running (a URL scheme gets this for free).

## Open questions

- If Batty is **not running**, should `batty .` launch it and then create the session, or print an error? (Launch-and-create is the friendlier behavior.)
- Non-directory / nonexistent path: error out on the CLI side (before involving the app), or let the app decide? Prefer validating on the CLI side and exiting non-zero with a clear message.
- Should `batty` with no args still print usage (current behavior), with `batty <path>` being the path form? A bare path that isn't a known subcommand should be treated as the session-create path.

## Relation

Follow-up to #0249 (CLI tool + install plumbing). #0249 deliberately shipped a minimal `version`/`help`-only command surface and called out defining real subcommands as separate work — this is that work.

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-06-23 | claude-sonnet-4-6 | 83 | 7,669 | 5,563,103 | 91,309 | $2.13 |
| 2026-06-23 | claude-opus-4-8 | 3,643 | 489 | 509,859 | 49,975 | $0.60 |

**Total: $2.73**
