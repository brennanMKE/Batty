# 0242 — Theme picker opens in every window instead of only the key window

| | |
|---|---|
| **Status** | resolved |
| **Module** | Theme / Window / App |
| **Platform** | macOS |
| **First seen** | 2026-06-16 |
| **Closed** | 2026-06-16 |
| **Commit** | 4ee3715 |

## Description

Multi-window regression: invoking the theme picker (Cmd-T) with two windows open brought up the picker in **both** windows at once, instead of only the current (key) window. Window-scoped UI that used to be a non-issue in single-window now fans out to every open window.

This is almost certainly not unique to the theme picker — the same routing flaw likely affects the **Layout picker** and any other command/overlay that presents per-window UI. Audit all such commands and constrain each to the key window.

## Steps to reproduce

1. Open a second window (Shift-Cmd-N) so two windows are visible.
2. Make one of them the key window.
3. Hit Cmd-T to bring up the theme picker.

## Expected behavior

The theme picker opens only in the current (key) window. Other windows are unaffected.

## Actual behavior

The theme picker opened in **both** open windows simultaneously.

## Notes

- Reported by the user during multi-window regression testing (related to the #0234 multi-window umbrella; theme work landed in #0241 "Add per-appearance Dark/Light theme with live re-apply").
- **The fix must cover more than the theme picker.** Audit every command/overlay that now presents per-window and confirm it targets the key window only. Known candidates to check explicitly:
  - Theme picker (the reported case).
  - Layout picker.
  - Any other Command-Palette / picker / sheet / popover-style presentation driven by a menu command or global keyboard shortcut (e.g. Open Quickly, bell-feed popover, rename, paste-confirm).
- Likely root cause: the presentation state (e.g. an `@Observable` "show picker" flag) lives in app- or scene-shared state rather than per-window state, so every `WindowRuntime`/window observing it presents. Per-window presentation flags were introduced around #0235–#0238 (per-window `WindowRuntime`, per-window sidebar state); verify the picker-presentation flags moved to that same per-window container. See `docs/multi-window-design.md`.
- This belongs to the same regression class #0240 (Phase 7) is meant to catch; once #0240's UI suite can run again, add multi-window coverage asserting a picker opens in exactly one window.

## Root cause

`SessionDetailView` is instantiated **once per content window**, and every instance subscribed to the same process-wide `NotificationCenter.default` toggles: `.battyToggleBellFeed`, `.battyToggleCommandPalette`, `.battyToggleOpenQuickly`, `.battyToggleLayoutPicker`, `.battyToggleThemeSelector`, and `.battyRequestPaste`. The posters (`BattyShortcuts`, `BattyCommands`, `CommandPaletteView`, `PasteConfirmation`) all post with `object: nil` and no window targeting, so a single Cmd-shortcut was delivered to **every** window's view, each of which toggled its own `@State` presentation flag — opening the picker in all windows at once.

The sidebar toggle had already hit and solved this exact problem (`RootWindowView.swift:62`, guarding on `keyWindowRuntime()?.id == windowID`); the five pickers and the paste request were simply never given the same guard when per-window `WindowRuntime` landed in #0235–#0239.

## Fix

Guard each of the six notification receivers in `SessionDetailView` so only the **key content window** acts, via a small `isKeyWindow` helper (`store.keyWindowRuntime()?.id == windowRuntime.id`) — the same proven pattern as the sidebar toggle. The guards live in `onReceive` closures (notification delivery is event-origin), so they are legitimate writers under `docs/swiftui-observation-rules.md`. The bell-feed toolbar button is left unchanged: a button tap is inherently scoped to the window it lives in.

Confirmed this covers the entire family — the only `onReceive` subscribers to these app-wide notifications are `SessionDetailView` (now all six guarded) and `RootWindowView` (sidebar, already guarded); all seven `co.sstools.Batty.*` toggle/request names are accounted for.

## Verification

`scripts/build.sh unit` — build succeeded; **336 unit tests in 40 suites passed**. Behavioral multi-window verification (picker opens in exactly one window) is a UI-test concern and was **not** run — the full `BattyUITests` suite is environmentally blocked on the post-OS-update LLDB attach failure tracked in #0240. See Gotchas.

## Files changed

- `BattyKit/Sources/BattyKit/Views/SessionDetailView.swift` — added an `isKeyWindow` computed helper and a `guard isKeyWindow else { return }` to all six notification receivers (bell feed, command palette, open quickly, layout picker, theme selector, paste request).

## Gotchas

- **Not verified by a UI test.** The multi-window behavior can't be exercised in an automated UI test right now because the suite is blocked by the LLDB toolchain issue in #0240. Visual sign-off is the user's: open two windows, make one key, hit Cmd-T (theme) / the Layout-picker shortcut, and confirm the picker appears only in the key window. Marked `resolved`, not `closed`.
- When #0240's UI suite can run again, add a regression test asserting a picker/sheet opens in exactly one of two open windows. The fix relies on `keyWindowRuntime()` resolving correctly at notification-delivery time; that is the thing a UI test should pin down.
