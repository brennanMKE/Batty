# 0251 — `batty <path>` opens a new empty window instead of a session whose terminal starts at the path

| | |
|---|---|
| **Status** | resolved |
| **Module** | App / Session / TerminalSurface |
| **First seen** | 2026-06-23 |
| **Closed** | 2026-06-24 |
| **Branch** | issue/0251 (round 1) → issue/0251-2 (round 2) |

## Description

Running `batty <path>` (e.g. `batty .` or `batty /Users/brennan/Developer/brennanMKE/NeoPager`) is supposed to make the running Batty app create a new Session whose first Pane's terminal starts in that directory (the #0250 feature). In practice it **opens a new window with a terminal still rooted at the shell default (`$HOME`/passwd), not at the requested path** — the session-at-path is effectively not created where the user can see it, and the cwd is wrong.

## Resolution notes

> 🟢 Resolved 2026-06-24 (user-verified) — `batty <path>` now adds a selected Session at the path to the **active** window, terminal rooted at the path, with no stray window. Took **two rounds**: round 1 fixed a phantom `WindowID`; round 2 stopped SwiftUI's `WindowGroup` from opening a new window for the `batty://` external event.

The fix landed in two rounds because the symptom had two independent causes (see Root cause). The user confirmed the final behavior at runtime via Console logs (`registeredContentWindows=1` throughout, session added to the existing window and selected, `auto-rename → NeoPager` confirming the cwd).

This is a bug in what #0250 delivered. The model-level wiring is unit-tested and passes (`AppStateStore.addSession(workingDirectory:)` carries the path), so the failure is in the integration between the URL handler, the window the session lands in, and the actual ghostty surface spawn — none of which the #0250 unit tests exercised (they can't spawn a real surface or window headlessly).

## Steps to reproduce

1. Build/run the app (the `batty://` scheme must be registered — see #0250 testing notes).
2. From a terminal in some project dir, run `batty .` (or `batty <abs-path>`).
3. Observe: a new window appears; its terminal's cwd is `$HOME`, not the path.

## Actual behavior

- A new (empty/default) window opens rather than a Session being added to the current window's sidebar.
- The terminal's working directory is the passwd default (`/Users/brennan`), not the requested path.

## Expected behavior

- `batty <path>` adds a new Session to the **front/key** window (or, if no window is open, opens one) and **selects** it, so the user sees it.
- That Session's first Pane/Tab terminal spawns its shell **in the requested directory**.

## Attachments

User-provided log excerpt from the running app:

```
Creating session at path: /Users/brennan/Developer/brennanMKE/NeoPager
reading configuration file path=…/ghostty-config-….conf
config: default working directory src=passwd value=/Users/brennan
reading configuration file path=…/ghostty-config-….conf
config: default working directory src=passwd value=/Users/brennan
reading configuration file path=…/ghostty-config-….conf
config: shell src=config value=… (/bin/zsh)
config: default working directory src=passwd value=/Users/brennan
```

The `Creating session at path: …/NeoPager` line (from `BattyURLHandler`) proves the path reaches `addSession`. The three config reads suggest multiple surfaces/windows are being created.

## Notes — investigation so far (orchestrator)

Two distinct suspected root causes; the implementer should confirm both with the running app (this needs runtime observation — the GUI can't be exercised headlessly):

**1. Wrong window target (the dominant "new window" symptom).**
`BattyAppDelegate.application(_:open:)` → `BattyURLHandler.handle(url, store: AppStateStore.shared)` → `store.addSession(workingDirectory:)` → `(keyWindowRuntime() ?? windows[0]).addSession(...)` (`AppStateStore.swift:233-234`).
- `AppStateStore.shared.init` seeds `windows = [window]` (`AppStateStore.swift:178-192`) with **no** sessions.
- The on-screen SwiftUI `WindowGroup(for: WindowID.self)` window (in `Batty/BattyApp.swift`) is associated with its own `WindowRuntime` via `windowRuntime(for:)` — which may **not** be `windows[0]`.
- When the URL is delivered (especially on cold launch, before any window is key), `keyWindowRuntime()` is likely nil, so the session is appended to `windows[0]` — a runtime that isn't the window SwiftUI shows. Net effect: the session is "created" in a phantom runtime, and SwiftUI separately opens a fresh empty window.
- Likely fix direction: the URL handler must target the window the user will actually see — bring an existing window to front and add+select the session there, or open exactly one window and add the session to *its* runtime. Inspect `windowRuntime(for:)`, `RootWindowView`, `OpenWindowHookInstaller`, and how `WindowID` ↔ `WindowRuntime` map. Ensure the new session is selected (`selectedSessionID`).

**2. Working directory not honored at surface spawn (confirm independently).**
The package path *looks* complete: `TabRuntime.init` sets `terminal.configuration.workingDirectory` (`TabRuntime.swift:63-64`); `TerminalHostStore.terminalView(for:windowID:)` sets `view.configuration = tab.terminal.configuration` (line 135) **before** `host.addSubview(view)` (line 139) where the surface spawns; `TerminalController.finalizeSurface` sets `config.working_directory` from `configuration.workingDirectory` before `ghostty_surface_new` (`libghostty-spm …/TerminalController+Surface.swift:85-95`).
- So if the value survives to spawn, the surface *should* start at the path. Cmd-T "CWD inheritance" may actually rely on libghostty's native parent-inheritance rather than this explicit `configuration.workingDirectory` option, meaning **the batty path may be the first real user of the explicit option** — and it may be unwired in some way (e.g. the value is nil at spawn, the coordinator's `configuration` didn't sync before `viewDidMoveToWindow`, or a default config is applied after).
- The `config: default working directory src=passwd` log line refers to the config *file* default and may be a red herring (the surface `working_directory` C-string is a separate field) — confirm with the running app whether the terminal actually lands in the path once the windowing bug (#1) is fixed, since the wrong-window bug alone could explain the wrong cwd (the user may be seeing a *default* session in the spurious new window).

**Verification reality:** the true end-to-end check (run `batty .`, see a session at the path in the front window) requires the running GUI app and is the user's sign-off. Add whatever unit coverage is feasible for the windowing-target logic; do not claim the cwd is fixed without a runtime check.

## Root cause

The symptom ("new empty window" + "wrong cwd") had **two independent causes**, fixed in two rounds:

**Round 1 — phantom `WindowID`.** `AppStateStore.init` seeds `windows = [window]` with a freshly-minted `WindowID` (WID-A). `BattyApp`'s `WindowGroup(for: WindowID.self)` used `defaultValue: { WindowID() }`, minting a *different* ID (WID-B) for the first on-screen window, so `windowRuntime(for: WID-B)` created a *second* runtime (`windows[1]`) and WID-A's `windows[0]` stayed a phantom no NSWindow presented. The app-global `addSession` shim (`… ?? windows[0]`) dropped the session into the phantom.

**Round 2 — SwiftUI `WindowGroup` opens a window for external events.** Even after round 1 routed the session into the real window, an extra empty window still appeared: SwiftUI's `WindowGroup` *by default volunteers to open a new window* for an OS-delivered external event (the `batty://` URL open). So the URL was double-handled — `application(_:open:)` correctly added the session to the active window, AND SwiftUI spawned an empty scene. (User runtime logs confirmed `registeredContentWindows=1` for the session path while a separate scene opened.)

## Fix

**Round 1** (branch `issue/0251`, already on `main`): align SwiftUI window identity with the seeded runtime. `BattyApp` uses `AppStateStore.shared.initialWindowID` (= `windows[0].id`) for the `WindowGroup` `defaultValue` and content nil-coalesce; `AppStateStore` gains `initialWindowID` and `anyContentWindowRuntime()`, and `addSession` targets `anyContentWindowRuntime() ?? windows[0]`, selecting the new session.

**Round 2** (branch `issue/0251-2`): stop SwiftUI from opening a window for the external event. Added `.handlesExternalEvents(matching: Set())` to **both** the content `WindowGroup` and the `Window("Batty Help")` scene. An empty match-set means neither scene volunteers as the target for an OS-delivered `batty://` open, so SwiftUI spawns nothing and the URL is handled solely by `application(_:open:)`. Declining on Help was necessary because, with only the `WindowGroup` declining, SwiftUI fell back to the next external-event-accepting scene (Help) and opened *that* — a regression caught in user testing. Diagnostic `Logger` lines were added on the URL path (kept at `.debug`, except `application(_:open:)` at `.info`) so this area is traceable in future.

The working-directory plumbing needed no change — it was already complete (`TabRuntime.init` → `TerminalHostStore` `view.configuration` → `TerminalController.finalizeSurface` `config.working_directory` → `ghostty_surface_new`); the wrong-cwd was purely the session landing in an unseen window.

## Review

Both rounds reviewed by Opus (claude-opus-4-8) — **approved**. Round 1: confirmed the phantom collapse doesn't duplicate/leak runtimes and Cmd-N/restoration are unaffected. Round 2: confirmed `handlesExternalEvents(matching: Set())` on both scenes is correct, `Settings` can't be a URL fallback target, Cmd-N (internal `openWindow`) is unaffected, and cold launch still creates the initial window (no invisible-session risk). Review nits applied each round (tightened a tautological test; downgraded diagnostic logs to `.debug`).

## Verification

- `scripts/build.sh unit` — **399 tests in 50 suites passed, `** TEST SUCCEEDED **`** (round 1 added 11 in `BattyURLHandlerRoutingTests`; round 2 is scene/URL routing which isn't unit-testable headlessly).
- `scripts/build.sh` — `** BUILD SUCCEEDED **`; `batty` embedded at `Batty.app/Contents/Resources/bin/batty` (arm64).
- **User-verified at runtime (the sign-off):** Console logs show `registeredContentWindows=1` throughout, `addSession targeting` the existing window, `selectedSessionID changed` to the new session, `auto-rename → NeoPager` (correct cwd), and **no second window and no Help window**.

## Files changed

Round 1 (on `main` via `issue/0251`):
- `Batty/BattyApp.swift` — `WindowGroup` default + nil-coalesce use `initialWindowID`.
- `BattyKit/Sources/BattyKit/Runtime/AppStateStore.swift` — `initialWindowID`, `anyContentWindowRuntime()`, `addSession` targeting.
- `BattyKit/Tests/BattyKitTests/BattyURLHandlerRoutingTests.swift` — new suite.

Round 2 (this branch, `issue/0251-2`):
- `Batty/BattyApp.swift` — `.handlesExternalEvents(matching: Set())` on the content `WindowGroup` and the Help `Window`; `.info` log in `application(_:open:)`.
- `BattyKit/Sources/BattyKit/Runtime/AppStateStore.swift` — `registeredContentWindowCount`; `.debug` diagnostics in `registerNSWindow`/`addSession`.
- `BattyKit/Sources/BattyKit/Runtime/BattyURLHandler.swift` — `.debug` before/after diagnostics.

## Gotchas

- **Declining external events must cover every URL-eligible scene, not just the main one.** Making the `WindowGroup` decline pushed SwiftUI to open the *Help* window as the fallback. Any future scene that can accept external events must also `.handlesExternalEvents(matching: Set())` (or the `batty://` open will resurface it). `Settings` is exempt (it never participates in external-event matching); internal `openWindow(value:/id:)` calls (Cmd-N, Help menu) are unaffected.
- **Cold launch yields a default `Session 1` *plus* the path session.** When `batty .` launches Batty from cold, the `WindowGroup` still creates its initial window (Session 1 at `$HOME`) and the URL then adds the path session. This is acceptable (a terminal app opening a default session on launch), but if a cold `batty .` should yield *only* the path session, that's a small follow-up — not filed as a ticket since it's a product preference, not a defect.
- **Latent restoration-phantom (pre-existing):** on state restoration SwiftUI presents the persisted `WindowID`, which won't match the freshly-seeded `windows[0].id`, so a phantom `windows[0]` can reappear. Not introduced or changed here; worth its own ticket if/when multi-window state restoration becomes a real feature.
- `anyContentWindowRuntime()`'s no-key-window fallback picks an unordered `nsWindowMap.values.first` — fine for single-window `batty`, but a future multi-window scenario may want a deterministic front-most pick.

## Relation

Regression / incompleteness in #0250 (which delivered `batty <path>` session creation). Found during user verification of #0250.

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-06-23 | claude-sonnet-4-6 | 441 | 3,418 | 3,519,954 | 102,675 | $1.49 |
| 2026-06-23 | claude-opus-4-8 | 10,168 | 3,161 | 618,740 | 54,614 | $0.78 |
| 2026-06-24 | claude-sonnet-4-6 | 35 | 2,135 | 2,190,379 | 85,591 | $1.01 |
| 2026-06-24 | claude-opus-4-8 | 3,633 | 2,801 | 266,141 | 41,461 | $0.48 |

**Total: $3.76**
