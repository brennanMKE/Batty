# 0283 — Add `batty pane close` over XPC, reporting a real result

| | |
|---|---|
| **Status** | resolved |
| **Module** | App / Split / Pane / Tabs |
| **Platform** | macOS |
| **First seen** | 2026-07-26 |
| **Closed** | 2026-07-27 |
| **Branch** | issue/0283 |

## Description

Third child of the #0257 umbrella. Per the transport amendment recorded there: `pane close` goes over XPC request/reply, not the one-way `batty://` scheme, because it must **report a real result** — closing a nonexistent pane id must exit `4`, not `0`. This is the second mutating XPC verb and should follow the pattern #0282 establishes.

**`pane close` is not `tab close`** *(terminology sweep, 2026-07-27 — this is the real ambiguity in the verb name, so the semantic is pinned here)*: a Pane may hold multiple Tabs, and `Concepts.md` defines pane removal as the consequence of closing the *last* Tab — there is no pane-level close in the code today (see the cascade below). `batty pane close` implements the **pane-level** semantic: it ends **every** Tab's Terminal Session in the target pane — not just the active one — and removes the pane's region from the split tree, with the sibling subtree taking over its space. Closing exactly one tab is #0257's separate `batty tab close [--tab <id>]` verb (no child filed yet), which removes the pane only when the closed tab was its last. An agent that wants the smaller operation must reach for `tab close`, not `pane close`.

## Resolution notes

> 🟢 Resolved 2026-07-27 — `batty pane close` ends **every** tab in the target pane and removes its region, reporting a real result: a stale id exits `4`, a confirmation-guarded pane refuses rather than force-closing, and the app's last pane refuses rather than quitting Batty.

Whether the pane visibly leaves the screen and the sibling takes its space needs the user's runtime sign-off.

## Root cause

Starting state, not a defect. #0282 gave an agent the ability to create panes — and, because a `-c` pane now waits for a keypress after its command exits, no way to clean them up. There was also **no pane-level close in the code at all**: closing was a cascade from `WindowRuntime.closeTab` → `tree.removePane(id:)` → `removeSession`, driven only by closing a pane's last tab.

## Fix

`batty pane close [--pane <id>]` over XPC, targeting through #0281's shared chain, added under #0282's `PaneNounCommand`.

**The pinned semantic:** it ends **every** Tab's Terminal Session in the target pane — not just the active one — and removes the pane's region, with the sibling subtree taking the space. Closing exactly one tab remains #0257's separate `tab close` verb. `WindowRuntime.closePane(id:refuseIfAppsLastPane:)` reuses `closeTab`'s teardown primitives (`cleanUpBellState(forTabIDs:)` over the full set, `TerminalHostStore.releaseTerminalView` per tab, then `removePane`, then `removeSession` when the tree empties) rather than duplicating them.

**Three decisions the issue delegated:**

**Confirmation — refuse, don't bypass or hang.** If any tab has `needsConfirmClose`, the verb returns `.needsConfirmation` → exit 4. Raising the UI dialog would leave the original call answering "failed" while a prompt appeared later for a request already gone — and for a background session, a dialog popping up unasked. Force-closing would silently kill a process the user configured a guard around. *(Currently unreachable: `TerminalViewState+Polyfill.swift` hardwires `needsConfirmClose` to `false`, the same libghostty fork gap `RequestCloseTabTests` documents. The branch is structurally present for when that lands.)*

**Last pane — refuse the app's absolute last pane, and nothing looser.** Closing the last pane of a non-last session cascades normally, and the last pane of a last session in a non-last window closes that window — both matching `closeTab`'s existing behavior. But closing what would be the app's only remaining pane is refused. The justification was verified rather than assumed: the cascade runs `removeSession` → `onAllSessionsClosed` → `terminateIfLastContentWindowGone` → `NSApp.terminate`, and by then every tab is gone, so `QuitConfirmation.shouldQuitOrPrompt` sees `openTabs == 0` and returns quit **with no dialog** (this is #0217, still open). Reusing that cascade unmodified would have handed an agent a one-command path to silently quit Batty — a capability no interactive path grants unprompted.

**Focus after close** reuses `SplitTree.removePane`'s existing reassignment, plus the hidden-pane repair below.

## Review

Reviewed by `claude-opus-5`, three rounds — the last two narrow. Two of the three rounds found real defects, and both were demonstrated with probes rather than argued.

**Round 1 found the hidden-pane invariant break.** `closePane` had no `isHidden` awareness, so: hide a pane with #0256's sidebar eye, then close its visible sibling — `removePane` collapses the split to the hidden leaf and reassigns focus to it, leaving the session with **zero visible panes and focus on a hidden one**. `SplitContainerView` renders a hidden leaf at zero size, so the detail area goes blank, recoverable only through the sidebar. `issues/0256.md` binds both rules and line 49 explicitly names the pane-collapse path as needing to handle hidden siblings. `closeTab` has the same shape, but this issue re-implements the cascade *and* exposes it as a one-command remote operation.

Round 1 also flagged that the last-pane guard used `windows.count == 1` as a **proxy** for "the app has one pane" — defeatable by a lingering session-less `WindowRuntime` — and that the headline multi-tab test asserted only that the pane left the tree, passing identically if teardown had touched zero tabs or only the active one. Same shape as #0282 round 1.

**Round 2 found the fix was half-complete.** Guarding on `visiblePanes.isEmpty` repaired the zero-visible case but left focus able to land on a hidden pane while another stayed visible. Probe: `split(A, split(B, C))`, A hidden, B focused; close B → focus reassigns to `firstLeafPaneID` = A (hidden) while `visiblePanes == [C]` is non-empty, so the repair was skipped. It also caught that the justification comment falsely claimed the un-hidden leaf "inherited the closed pane's screen space" — `firstLeafPaneID` is the *leftmost* leaf, and a second probe showed A un-hiding while C absorbed the space.

Round 2 confirmed findings 2 and 3 closed **by mutation testing**: reverting the guard to the `windows.count` proxy failed the new regression test; mutating teardown to active-tab-only failed the headline test with four issues including two surviving inactive-tab bell entries.

**Round 3 approved**, again by mutation rather than reading: reverting the gate to round 2's version fails the new test at the exact focus assertions, and removing the `showPane` call fails round 1's test at three assertions — so both paths remain reachable and load-bearing. The reviewer also enumerated all four post-`removePane` states and confirmed the dangling-focus case is unreachable, since `removePane` always reassigns.

## Verification

- `cd BattyKit && swift test` after `rm -rf .build` — **724 tests in 84 suites passed** (baseline 699/82; +25 tests, +2 suites: `PaneClosePayloadTests`, `AppStateStoreClosePaneTests`).
- Unsigned workspace gate — same 724/84, `** TEST SUCCEEDED **`.
- `otool -L | grep -c '@rpath'` → **0** on both embedded binaries, freshly built and timestamp-checked.
- `pane close --pane not-a-uuid` → `batty: invalid --pane id:`, exit **1**. `pane close` with no broker → `batty: broker unreachable`, exit **2** in 15 ms. `pane --help` and `pane close --help` render with the pane-vs-tab distinction explicit.
- The multi-tab teardown is asserted through bell state (`BellFeedCleanupTests`' established pattern), because `TerminalHostStore.releaseTerminalView` early-returns headlessly with no registered view.
- **All verification used the unsigned protocol.** No signed build was run; `scripts/build.sh` was never invoked. From round 2 onward, reviewers deliberately declined to run XPC-issuing CLI commands, since with the user's app live a `pane close` could have reached it.

**Needs the user's runtime sign-off:** the pane visibly leaving the screen with the sibling taking its space; the hidden-pane focus move and un-hide being visible; `pane close --pane <stale-uuid>` exiting 4 against a live app; and the last-pane refusal end-to-end.

## Files changed

- `BattyKit/Sources/BattyXPCCore/PaneClosePayload.swift` (new), `XPCVerb.swift` — the verb and its request/reply types.
- `BattyKit/Sources/BattyKit/Runtime/WindowRuntime.swift` — `PaneCloseOutcome` and `closePane(id:refuseIfAppsLastPane:)`, including the hidden-focus repair.
- `BattyKit/Sources/BattyKit/Runtime/AppStateStore.swift` — `closePane(id:)` and the direct total-pane count.
- `BattyKit/Sources/BattyKit/XPC/AppXPCService.swift`, `BattyKit/Sources/batty/AppServiceClient.swift`, `PaneCommand.swift` — dispatch, client, and the CLI verb; `--pane` resolution extracted into a helper shared with `pane split`.
- `BattyKit/Tests/BattyKitTests/PaneClosePayloadTests.swift`, `AppStateStoreClosePaneTests.swift` — 25 tests.
- `docs/batty-cli-install.md` — the verb, its three failure modes, and the pane-vs-tab distinction.

## Gotchas

- **`pane close` is not `tab close`.** It ends every tab in the pane. An agent wanting the smaller operation needs `tab close`, which is specified in #0257 but has **no child filed and does not exist**.
- **Three distinct failures all map to exit 4** — unknown pane id, confirmation required, and last-pane refusal. The stderr message distinguishes them; the exit code does not. A script branching on behavior must read stderr.
- **The confirmation branch is currently unreachable** because the polyfill hardwires `needsConfirmClose` to `false`. It will start firing when the libghostty fork gap closes — untested at that point.
- **`firstLeafPaneID` is positional, not structural.** `removePane` reassigns focus to the tree's *leftmost* leaf, which is not necessarily the pane that absorbed the closed pane's space. The hidden-pane repair works regardless, but any future code reasoning about "the pane that took over" must not assume `firstLeafPaneID` identifies it.
- **The last-pane guard now counts panes directly** rather than inferring from `windows.count`. Keep it that way — a `WindowRuntime` can linger in `windows` with no sessions (see `closeWindowCallback`'s own doc comment), which is exactly what defeated the earlier proxy.
- **#0217 remains open and adjacent.** This verb refuses to trigger the silent-quit cascade, but the underlying bug — all tabs gone before `applicationShouldTerminate`, so `QuitConfirmation` never prompts — is untouched.

## Grammar

```
batty pane close [--pane <id>]
```

- `--pane <id>` targets an explicit pane; otherwise the target resolves via #0281's chain (explicit flag → `BATTY_PANE_ID` env → focused pane).
- Exit codes per the `BattyXPCCore/XPCExitCode.swift` vocabulary (verified): `0` success, `1` CLI input error, `2` broker unreachable, `3` app unavailable, `4` request failed, `5` app terminated. **A nonexistent or stale pane id is a `4`, with a message on stderr.**

## Behavior rules (from #0257, binding)

- **Closing a pane in a background session must not steal focus or switch the active session.** Only the `focus` verbs do that.
- The result reported must reflect what the app actually did — success only when the pane existed and was closed.

## As-built starting points (verified against source 2026-07-26)

- **No pane-level close entry point exists today.** Close is a *tab* cascade: `WindowRuntime.closeTab(id:)` (`BattyKit/Sources/BattyKit/Runtime/WindowRuntime.swift`) removes the tab (with `cleanUpBellState`), and when the pane's tab list empties it calls `session.tree.removePane(id: pane.id)`; when that reports the tree emptied, it calls `removeSession(id:)`. A pane may hold multiple tabs, so "close pane" means ending every tab's Terminal Session in that pane — reuse or extract the existing cascade so surface teardown and bell-state cleanup go through the same path (the Surface registry is the single source of truth; leaked surfaces are the failure mode to guard).
- **`SplitTree.removePane(id:)` exists** (`BattyKit/Sources/BattyKit/Model/SplitTree.swift`, ~line 205) and returns `true` when the tree became empty; its doc comment says callers should treat that as "session is now empty and should be closed." It also reassigns the tree's `focusedPaneID` when the removed pane was focused — within-session, which is fine, but it must not propagate to `selectedSessionID` or app-level focus for a background target.
- **A confirmation path exists for the interactive close** and this verb must take a position on it: `WindowRuntime.requestCloseTab(id:)` checks `terminal.needsConfirmClose` and raises a `pendingCloseRequest` dialog when a process is still running. Whether an agent-driven `pane close` honors that confirmation (reply "confirmation pending"? fail? force-close?) is a design decision for the implementer to make explicitly and record — silently bypassing a guard the UI enforces should not happen by accident.
- **XPC wiring points** (all verified): verb in `BattyXPCCore/XPCVerb.swift`, dispatch in `AppXPCService.perform(_:reply:)`, CLI via the generalized `AppServiceClient.perform<Payload>` dance from #0274. The `pane` noun itself is introduced by #0282.

## Raised, not decided: closing the *last* pane

What happens when the target is the last pane in a session — does the session close (the existing cascade says yes: `removePane` → tree empty → `removeSession`)? And when it is the last pane of the last session? Is there a confirmation path an agent should trip? Note that **#0217** (open) records the app *silently quitting* when all shell processes exit, bypassing quit confirmation — an agent that closes panes one by one could walk the app into exactly that adjacent behavior. This issue must surface the chosen behavior in its resolution, but the decision is left to the implementer/user, not pre-made here.

## Binding constraints (copied — do not merely cross-reference)

- **Payloads are `Codable, Sendable` structs carried as JSON-encoded `Data`** inside the `@objc` protocols, `nonisolated` at type level; protocol requirements are `nonisolated func`; reply blocks `@escaping @Sendable`. Guard new payload types with off-actor round-trip helpers as `XPCContractTests` does, and assert raw decoded JSON keys (the #0274 `_0` lesson), not just round-trips.
- **Every Foundation-supplied handler closure gets `@Sendable`** — there is no compiler warning, and the failure is a silent `SIGTRAP` inside the very handler that would have logged the error.
- **`os.Logger` interpolation of a dynamic `String` defaults to `.private`** — add `privacy: .public` explicitly.
- **Verify isolation guards under a forced rebuild** (`rm -rf .build`); stale targets produced false "still builds" results twice on the XPC track.
- **Code-signing protocol:** never run `scripts/build.sh` for iteration (its `unit` path can't disable signing); use `cd BattyKit && swift test` and `xcodebuild … CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO`. The user's Developer ID certificate was revoked once by repeated signed local builds.
- **Mutations aimed at a background session must not steal focus or change the active session** — assert against the store (`selectedSessionID`, focused pane, active tab), not merely absence of a crash.

## Verification expectations

Follow the #0271/#0274 pattern:

- **Unit tests for the pure parts:** argument parsing (`--pane` UUID validation → exit 1 client-side); request/reply payload round-trips plus raw-JSON key assertions; close on a constructed tree (named pane removed, siblings' fractions correct, unknown id yields the failure result); the last-pane cascade behavior, whatever is decided, asserted explicitly.
- **Cross-session store assertions:** closing a pane in a non-active session mutates only that session's tree; `selectedSessionID`, the active session's focused pane, and every other session are unchanged.
- **Failure result:** `batty pane close --pane <nonexistent-uuid>` → CLI exit `4`, message on stderr, app-side public log line. This is the acceptance test for the transport decision itself.
- `cd BattyKit && swift test` and `xcodebuild … CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` green; `otool -L` on `batty` still shows zero `@rpath` dependencies.
- **Needs the user's runtime sign-off** (do not disturb the running app; coordinate): from inside a pane, `batty pane split` then `batty pane close --pane <new-id>` round-trips; closing a background session's pane leaves the current view untouched; the stale-id exit `4` is observable; the last-pane behavior matches what was decided.

## Out of scope

- Fixing **#0217** (silent quit when all shells exit) — adjacent, referenced, not owned here.
- The remaining #0257 verbs (`pane hide/show/focus/zoom`, tab and session verbs, `notify`, `open`).
- Streaming/events — explicitly deferred at the #0265 umbrella level.

## Relation

- Child of the **#0257** umbrella; implements its `pane close` verb under the transport amendment (XPC, not `batty://`). Blocked by **#0281** (resolution chain); recommended after **#0282**, whose mutation-verb pattern (payloads, dispatch, store assertions, `pane` noun) this reuses.
- Rides the XPC machinery from **#0269–#0272** and #0274's generalized dance; exit codes per **#0269**'s vocabulary.
- Adjacent: **#0217** (app silently quits when all shell processes exit) — the last-pane question above.

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-07-27 | claude-sonnet-5 | 7,974 | 9,136 | 11,229,956 | 213,201 | $2.89 |
| 2026-07-27 | claude-opus-5 | 58 | 1,613 | 3,031,807 | 150,139 | $2.49 |
| 2026-07-27 | claude-sonnet-5 | 50 | 3,946 | 5,812,612 | 263,625 | $1.86 |
| 2026-07-27 | claude-opus-5 | 42 | 855 | 880,474 | 62,673 | $0.85 |
| 2026-07-27 | claude-sonnet-5 | 28 | 1,603 | 3,544,212 | 278,259 | $1.42 |
| 2026-07-27 | claude-opus-5 | 32 | 785 | 593,437 | 39,860 | $0.57 |

**Total: $10.08**
