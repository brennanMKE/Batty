# 0302 — Every layer of the Pane path presumes a Terminal Session; explore a pane-kind abstraction that supports any view type without regressing terminals

| | |
|---|---|
| **Status** | resolved |
| **Module** | Pane / Split / Persistence |
| **Platform** | macOS |
| **First seen** | 2026-08-04 |
| **Closed** | 2026-08-08 |
| **Branch** | issue/0302 |

## Description

Batty cannot represent a Pane whose content is not a Terminal Session — the assumption is baked in at every layer of the model and view path. Before any concrete non-terminal view (#0304 Git Status, #0305 Process Status) can exist, a **pane kind** abstraction has to be designed. The deliverable of this issue is an **exploration and a written proposal**, not a finished refactor: where *kind* lives, how the view layer branches on it, how the terminal-host boundary is drawn, and how the Codable model carries it — with explicit evidence that terminal panes lose nothing.

**"Prevent regressions" is the operative constraint.** `docs/view-hierarchy.md`, `docs/terminal-pane-requirements.md`, and `docs/swiftui-observation-rules.md` are binding for any change on this path, and the manual pane checklist (pointer input, keyboard, file drop, text drop, IME) must still pass on terminal panes after any spike or restructuring. #0143 is the recorded example of what breaks when that checklist is skipped — file drops and clicks both regressed.

## Resolution notes

> 🟢 Resolved 2026-08-08 — `docs/pane-kinds.md` settles all six Expected-behavior items: kind lives on the **Pane**, `PaneView.body` branches on it so a non-terminal pane never mounts a `TerminalPlaceholderView`, `TerminalHostStore` stays terminal-only, and the Codable `Pane` gains `kind` with an absent-means-terminal decode.

No Swift changed — the deliverable is the written proposal, as the issue specifies. Two findings landed alongside it: the doc contradiction this issue flagged was resolved (workspace persistence really was deleted, so `docs/view-hierarchy.md` and `CLAUDE.md` were the stale docs, not `Concepts.md`), and review found that a non-terminal pane **cannot** simply use SwiftUI `.onDrop` — the window-spanning `TerminalHostView` intercepts file drags. That correction is the most valuable thing in the document for #0304/#0313/#0314. Three adjacent doc-accuracy nits the review declined to hold the merge for are filed as **#0321**.

## Root cause

Starting state, not a defect: the model, the view layer, and the CLI all presumed a Terminal Session at every level. A split-tree leaf *is* a `PaneRuntime`, `PaneRuntime` preconditions on at least one Tab, `TabRuntime.terminal` is non-optional, and `PaneView.body` unconditionally mounts `TerminalPlaceholderView` — whose `body` spawns a PTY as a rendering side effect. There was no seam at which a Pane could render anything else.

## Fix

A design document, `docs/pane-kinds.md`, deciding rather than surveying:

1. **Kind lives on `PaneRuntime`/`Pane`, not on Tab.** A non-terminal pane has `tabs: []` and no Tab bar. Kind-on-Tab was rejected because it forces `TabRuntime.terminal` optional across ~15 call sites; the capability cost of the choice (no per-view duplicate, no multi-instance per pane) is stated up front rather than buried.
2. **`PaneView.body` gets one `switch pane.kind` at the top**, with `TerminalPlaceholderView` only in the `.terminal` arm. Pane chrome (focus border, bell flash, pane-swap drop zone, frame reporting) stays outside the switch so it applies uniformly.
3. **`TerminalHostStore` is terminal-only.** A table walks every `docs/terminal-pane-requirements.md` guarantee and says which extend to non-terminal panes.
4. **`Pane.kind: PaneContentKind = .terminal`**, custom `init(from:)` so an absent key decodes to `.terminal`.
5. **CLI composition (#0315):** `closePane` already degrades correctly for an empty-`tabs` pane, so no new verb is needed. `TopologyPanePayload.activeTabID` going optional needs a hand-written `encode(to:)` that keeps the key present as explicit `null`, or it breaks the wire contract frozen by #0274.
6. **Regression evidence:** none owed here (no code changed); #0303/#0304 own the manual terminal-pane checklist.

Also corrected the stale docs: removed the dead `WorkspaceManager.swift` / Workspace-root references from `docs/view-hierarchy.md` and the matching sentence in `CLAUDE.md`, and indexed the new doc in `docs/README.md`.

## Review

Reviewer: `claude-opus-5`, two rounds, a fresh agent each round.

Round 1 requested changes on three blocking items. The substantive one: §3 claimed a non-terminal pane could use `.onDrop` directly because no `TerminalHostView` sat above it. False — there is one host per content window filling the entire detail area (`SessionDetailView.swift:183-195`), registered window-wide for `.fileURL` (`TerminalHostView.swift:58-59`), and AppKit drag dispatch does not fall through to a SwiftUI sibling beneath it. Pointer fall-through *is* safe, but only because `hitTest` returns `nil` with no terminal under the point — which does not generalize to drags. Left uncorrected this would have shipped the #0143 failure class into every later view child. Also: `activeTabID` going optional was mischaracterized as a non-breaking wire change, and `CLAUDE.md` still named `Session`/`Tab`/`SplitNode` as Codable layout types that #0172 deleted.

Round 2 verified each fix against source rather than against the implementer's narrative, checked the new-in-round-2 `PaneFrameTracker` coordinate-space claim (the `"session"` and `"terminal-host"` named spaces really are distinct), and confirmed that the implementer was right and round 1 wrong on a disputed line citation. Approved.

## Verification

`scripts/build.sh unit` — **933 tests in 107 suites passed**, `** TEST SUCCEEDED **`, run after each round. `Configuration/Active.xcconfig` confirmed `#include "Prod.xcconfig"` before and after both runs.

This is a **baseline-green check, not proof of behavior**: no Swift source changed in either round, so the built product is byte-identical to `main`. Nothing here is verified UI work — the drag-interception conclusion in §3 is reasoned from source and is explicitly marked in the document as extrapolated and not empirically tested. Whichever child first needs a drop target on a non-terminal pane owes that experiment.

## Files changed

- `docs/pane-kinds.md` — new; the proposal, 6 sections answering the six Expected-behavior items, plus a round-2 changelog.
- `docs/view-hierarchy.md` — removed the `Workspace` root node and dead table row from the hierarchy diagram; corrected the serialization sentence; amended the version footer.
- `CLAUDE.md` — corrected the architectural rule that named deleted Codable types and described workspace serialization that no longer exists.
- `docs/README.md` — index entry for `pane-kinds.md`.
- `issues/0302.md` — status flip, resolution sections, work log.

## Gotchas

- **A non-terminal pane cannot assume SwiftUI `.onDrop` works.** One `TerminalHostView` spans the whole detail area and is registered for `.fileURL`; `draggingEntered` returns `.copy` for any file drag *before* checking what is under the cursor, and `performDragOperation` then fails with `reason=no-terminal-under-point`. The user-visible result is a copy cursor followed by silent nothing. Pointer clicks are fine — `hitTest` returns `nil` with no terminal beneath — but drag dispatch and hit-testing take different paths in AppKit.
- **`PaneFrameTracker` frames and terminal-host placements live in different coordinate spaces** (`"session"` vs `"terminal-host"`). Any future work that needs to correlate a pane's frame with host geometry has a real bridge to build; it is not a solved extension point.
- **Workspace persistence is gone, not dormant.** #0172 deleted it on 2026-05-20. Docs that describe `workspace.json` or `WorkspaceManager.swift` are stale, and `PaneRuntime.snapshot()`'s only remaining caller is a test. The pane-kind field is designed as if persistence returns, but nothing serializes it today.
- **`TopologyPanePayload` is a frozen wire contract** (#0274), pinned by an exact key-set assertion in `TopologyPayloadTests.swift`. Making a field optional silently drops the key via `encodeIfPresent`. #0315 must settle null-vs-absent with the user before changing it.

## What stands in the way (verified in source)

Everything below is why "add a view" is really "introduce a pane kind".

**1. The model presumes a terminal at every level.**

- A split-tree leaf *is* a `PaneRuntime`: `SplitTreeNode` is `case leaf(PaneRuntime)` / `case split(…)` (`BattyKit/Sources/BattyKit/Model/SplitTree.swift:27-30`).
- `PaneRuntime` requires at least one Tab: `precondition(!initial.isEmpty, "PaneRuntime must contain at least one Tab")` (`BattyKit/Sources/BattyKit/Runtime/PaneRuntime.swift:31`).
- `TabRuntime.terminal` is a non-optional `public let terminal: TerminalViewState` (`BattyKit/Sources/BattyKit/Runtime/TabRuntime.swift:12`), constructed unconditionally in `init` (`TabRuntime.swift:117`). Every Tab **is** a Terminal Session; there is no way to represent a Tab or Pane without libghostty state today.
- Every pane-creation path funnels through `SplitTree.makePane` (`SplitTree.swift:208-215`), which always builds `PaneRuntime`/`TabRuntime` and inherits the source Tab's terminal cwd.

The central design decision this issue must produce an answer for: **where does *kind* live?** Options include a kind on the Pane (a non-terminal pane has no Tab bar at all) versus a kind on the Tab (a Git Status tab could sit alongside terminal tabs in one Pane). The user's phrasing — "create a new pane with the Git Status view" — suggests pane-level, but the model consequence differs a lot and the proposal should make the choice and its consequences visible.

**2. The view layer's pane body is unconditionally terminal placeholders.**

`PaneView.body` renders the tab bar plus `ZStack { ForEach(pane.tabs) { TerminalPlaceholderView(…) } }` (`BattyKit/Sources/BattyKit/Views/PaneView.swift:122-129`). There is no branch point where a pane could render anything else. Worse, mounting a `TerminalPlaceholderView` is not passive: its `body` eagerly calls `TerminalHostStore.shared.terminalView(for:windowID:)` to spawn the PTY (`BattyKit/Sources/BattyKit/Views/TerminalPlaceholderView.swift:36-43`). **A non-terminal pane must never mount one, or it starts a shell as a rendering side effect.**

**3. The terminal-host architecture applies only to terminal kinds — which is a simplification, if the boundary is drawn right.**

Per `docs/view-hierarchy.md` §3, terminal content is not hosted by SwiftUI at all: `TerminalHostStore` keeps one persistent `TerminalHostView` per content window with one `AppTerminalView` subview per live Tab, placed over placeholder-reported frames. A non-terminal view is plain SwiftUI — it needs **none** of that (no placement, no host subview, no geometry probe), and SwiftUI's rebuild churn that kills PTYs is harmless to it. The abstraction boundary must become: *terminal* panes get placeholders + host placement; *non-terminal* panes render their SwiftUI body directly. Note the z-order constraint from `view-hierarchy.md` §4 ("AppKit-on-top"): the host layer floats above the SwiftUI tree, but it only occupies frames reported for terminal Tabs, so a non-terminal pane's region has no host subview over it — pointer input should reach the SwiftUI view normally. The `docs/terminal-pane-requirements.md` checklist stays scoped to terminal panes; the proposal should say explicitly which of its guarantees (if any) a non-terminal pane must still honor.

**4. Persistence: the Codable model needs a kind — but whether anything serializes it today can't be settled from the docs.**

The one Codable layout type is `Pane` carrying only `id` and `isHidden` (`BattyKit/Sources/BattyKit/Model/LayoutModel.swift:15-23`, produced by `PaneRuntime.snapshot()`, `PaneRuntime.swift:39-41`). A pane kind must be added there with a backward-compatible decode (absent field = terminal). However, the documentation contradicts itself about workspace persistence: CLAUDE.md and `docs/view-hierarchy.md` describe `workspace.json` serialization and link a `WorkspaceManager.swift` (`docs/view-hierarchy.md:54`) **that does not exist in the tree**, no code anywhere encodes `Pane`, and `Concepts.md:270`'s version note says Workspace persistence was "never a planned feature" — while per-concept "Persisted:" rows remain (`Concepts.md:37,53,75,94,111`). Can't tell from source which way this is headed; the pane-kind field should be designed as if persistence lands, and the doc inconsistency is worth resolving alongside the `Concepts.md`/`PRD.md` vocabulary updates the umbrella already requires.

## Expected behavior

A written proposal (design doc or amendments to this issue and the umbrella) that settles:

1. Where kind lives (Pane vs Tab) and the model changes it implies (`SplitTreeNode`, `PaneRuntime`, `TabRuntime`, `SplitTree.makePane`).
2. How `PaneView` branches so a non-terminal pane never mounts a `TerminalPlaceholderView`.
3. The terminal-host boundary: `TerminalHostStore` host/placement machinery applies to terminal kinds only, and which `docs/terminal-pane-requirements.md` guarantees (if any) extend to non-terminal panes.
4. The pane-kind field in the Codable `Pane` value type with a backward-compatible decode, designed as if workspace persistence lands.
5. How the proposal composes with the umbrella's kind-aware CLI deltas (create / close / `list` payloads) without colliding with in-flight #0257-umbrella payload changes.
6. Evidence that terminal panes regress nothing: any spike that touches the terminal/pane path re-runs the manual checklist (pointer input, keyboard, file drop, text drop, IME) on terminal panes.

## Relation

- **Child of #0301** (umbrella: expand Batty beyond terminal-only Panes).
- **Blocks #0303, #0304, #0305** — lifecycle and both concrete views build on where kind lives.
- **#0143** — the recorded regression example for skipping the terminal-pane checklist.
- **#0321** — three doc-accuracy nits surfaced by this issue's review round 2 and filed rather than folded into this merge.

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-08-08 | claude-sonnet-5 | 64 | 2,747 | 4,520,520 | 181,951 | $1.39 |
| 2026-08-08 | claude-opus-5 | 40 | 281 | 1,391,646 | 103,125 | $1.35 |
| 2026-08-08 | claude-sonnet-5 | 82 | 5,055 | 9,294,555 | 458,397 | $3.06 |
| 2026-08-08 | claude-opus-5 | 36 | 518 | 1,109,079 | 82,356 | $1.08 |

**Total: $6.88**
