# 0048 — Tab chip titles don't truncate, so the "+" button and inactive tabs are pushed out of narrow panes

| | |
|---|---|
| **Status** | resolved |
| **Module** | Tabs |
| **Platform** | macOS |
| **First seen** | 2026-05-11 |
| **Closed** | 2026-05-11 |
| **Commit** | 7569183 |

## Description

Tab chips render at their natural text width — long titles like `brennan@macbook-air-m4-brennan-2:~` (the default shell title) consume the full chip width with no truncation. When a pane is narrower than the sum of its tabs at full width, the chips overflow and the trailing affordances (the "+" button, the rightmost tab chip) get pushed off the visible row.

In the attached screenshot the bottom-right pane is split to roughly half the window width. Its two tabs at their natural widths exceed that available width; the active (rightmost) chip is truncated against the window edge, and that pane's "+" button isn't visible at all. The user has no way to add a tab to that pane without resizing the split.

## Steps to reproduce

1. Open a window with two side-by-side panes (horizontal split).
2. In the smaller pane, add a second tab (`Cmd-T`).
3. Observe: the chip widths consume the full pane width; the "+" button is no longer visible; the active chip is clipped at the right edge.

## Expected behavior

Tab chip widths adapt to the available row width so every tab plus the "+" affordance remain visible. Options (any/all may be acceptable):

- Truncate chip titles with an ellipsis when the chip can't render at full width.
- Establish a minimum per-chip width (e.g. ~80pt of title space) below which the bar scrolls horizontally with an indicator.
- Always reserve space for the "+" button at the trailing edge so it never disappears.

## Actual behavior

- Each chip renders at the title's natural width.
- The "+" button is hidden when chips collectively exceed the pane width.
- The trailing chip is clipped by the pane's right edge.

## Attachments

![No truncation, "+" off-screen, trailing chip clipped](0048/screenshot.png)

## Notes

- This is the root cause of which `#0047` (tab bar leaking into adjacent pane) is the visual symptom. Fixing one without the other leaves the user worse off: clipping without truncation hides accessible tabs; truncation without clipping still risks leaking content.
- The shell default title (`user@host:cwd`) is long but typical — Batty shouldn't assume short titles. The cwd-basename fallback from `#0016` produces shorter titles when shell-integration is active, but the OSC 0/1/2 path commonly sets the long form.
- Implementation hint: `SlidingTabBar` is the third-party SwiftUI component from the `SlidingTabs` package. Truncation policy probably lives inside that package; check whether it exposes a sizing/truncation knob before reaching for a fork.
- Related: `#0046` (close-the-last-tab cascade). With both fixes in, narrow panes become tractable to manage.

## Root cause

`DefaultTabChip` doesn't impose a max width, and `SlidingTabBar` doesn't enforce a per-slot width — chips grow to whatever their title needs. With long shell-default titles like `brennan@macbook-air-m4-brennan-2:~/some/path`, two or three chips quickly exceed any reasonable pane width.

## Fix

Truncate titles at the source in `PaneView`, with a middle-ellipsis pattern that keeps both the meaningful head (user@host) and tail (cwd basename) of typical shell titles visible.

- New `PaneView.truncate(_:limit:)` static helper. Cap is 24 characters. Above the cap, returns `head…tail` keeping the head/tail split roughly even (head gets `limit/2`, tail gets the remainder minus 1 for the ellipsis).
- The `DefaultTabChip` title is now always the truncated form; the chip's intrinsic width stays bounded regardless of the underlying shell title.

The clip-on-overflow from `#0047` is the safety net for the cases where truncation isn't enough (many tabs, a very narrow pane).

## Files changed

Same commit as `#0047` (`7569183`):

- `BattyKit/Sources/BattyKit/PaneView.swift` — `Self.truncate(chipTitle(for: tab), limit: 24)` wraps the title; `truncate` helper added.
- `BattyKit/Tests/BattyKitTests/PaneViewTruncateTests.swift` (new) — 3 tests: short-untouched, long-with-middle-ellipsis, exactly-at-limit.

## Gotchas

- **24-char cap is a heuristic.** Wide enough that typical shell titles read clearly (e.g. `brennan@…/Batty` survives), narrow enough that ~5 chips plus the "+" fit a 600pt-wide pane at natural widths. If users complain that it's too aggressive (or not aggressive enough), expose it as a Settings preference (`Appearance` pane) rather than tweaking the constant.
- **Truncation runs on the resolved chip title,** which already prefers `titleOverride` → `terminal.title` → cwd basename → `"Tab"`. The user's titleOverride is also truncated; if they want a long persistent label they'll need to make peace with the ellipsis. Inline rename (`#0049`) would naturally use a wider editing affordance but display the truncated form when not editing.
- **Middle ellipsis (`head…tail`) is intentional** because terminal titles often have meaningful information at both ends: `user@host` at the start and the cwd basename at the end. A tail truncation would hide the cwd; a head truncation would hide the user.
- **DefaultTabChip.text has `.lineLimit(1)`** so even if truncation here ever produces a long enough string to overflow the chip frame, it won't wrap to two lines.
- **The fix doesn't add a min-width per chip.** Very short titles produce very narrow chips. That's fine for now (they all share the same minimum padding inside DefaultTabChip), but if it looks weird at scale, wrap the chip in `.frame(minWidth: 80)` from `PaneView`.

## Verification

- `xcodebuild -scheme Batty -destination 'platform=macOS' build` ✅
- `swift test --package-path BattyKit` 79/79 ✅
- Manual visual sign-off (open a tab where the shell sets the full hostname-cwd title, verify the chip shows the truncated form with the "+" button visible to its right) is **pending user confirmation** — visual, marked `resolved` not `closed`.
