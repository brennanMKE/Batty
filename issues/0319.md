# 0319 — Adopt the merged upstream per-surface command / wait_after_command API (libghostty-spm PR #45)

| | |
|---|---|
| **Status** | open |
| **Module** | Build |
| **Platform** | macOS |
| **First seen** | 2026-08-07 |

## Description

Batty's upstream ask from #0292 was **merged**: <https://github.com/Lakr233/libghostty-spm/pull/45> ("Expose per-surface `command` and `wait_after_command` via `TerminalSurfaceOptions`"), merged 2026-08-06T13:12:58Z as merge commit `31884f5fa8e3d531b01495fc1929cbce8013fc4c`; the change itself is `d9b3c74`. This issue tracks adopting it in Batty. The verified facts below are summarized from #0292's `### 2026-08-07` note, which is the primary record.

**No tagged release contains it.** The latest upstream release is 1.3.2 (2026-07-27), which predates the merge. Batty pins by exact tag revision — `revision: "b146b73a8ba3ed2678a22a9de5feecfcbf298d48"` (= tag 1.3.2) in `BattyKit/Package.swift:32`. Two options, neither decided here:

1. **Pin an untagged commit now** (`31884f5f...` or `d9b3c74`) — departs from the exact-tag-revision convention #0286 established.
2. **Wait for a 1.3.3 tag** — keeps the convention; adoption cost is a delay of unknown length.

**A bump is not just this PR.** The merge is **11 commits ahead of the pin**, pulling in:

- `d9b3c74` — PR #45, this API.
- **PR #44, host-managed resize coherence** — `f32ba80`, plus `ff1d4a2` (resize-coalescing window in `TerminalSurfaceOptions`), `959e5f1` (opt-in suppression of pixel-only resize dispatches), `18a2018` (tests). **Potentially relevant to #0294**, whose leading hypothesis is a scale-factor fallback in `synchronizeMetrics` sizing a surface to `bounds x 2` — the resize work should be read *before* a bump, since it could fix #0294, change its behavior, or invalidate its repro protocol.
- **PR #43, host paste confirmation** — `fa65914`, `305daeb`. Batty has its own paste-confirmation feature (`PasteConfirmation.swift`); check for interaction.
- `dd49535` — "Fix embedded Ghostty terminfo layout".

**The original justification is void — do not inherit #0292's memory framing.** The API was requested so `-c` panes could share a controller and collapse per-Tab `ghostty_app_t` duplication. **#0287 refuted that**: the glyph atlas is per-*surface*, so surfaces on a shared controller each still added atlases. The remaining value is architectural — per-surface configuration replacing the global `command` / `wait-after-command` config directives in `TabRuntime.swift` (currently `builder.withCustom("command", command)` at `:195`, `builder.withCustom("wait-after-command", "true")` at `:213`, and the shell-fallback `command` line at `:215`; verified 2026-08-07, matching the lines #0292's note cites).


## Decision (2026-08-08)

**Wait for a 1.3.3 tag.** The user chose option 2 from the Description: keep the exact-tag-revision convention #0286 established rather than pinning the untagged merge commit `31884f5f`.

Consequences, recorded so the next session doesn't re-litigate:

- This issue stays `open` until upstream tags a release containing `d9b3c74` (PR #45). It is **not** blocked on anything in this repo.
- The 10 other commits the bump would pull in are not adopted either. **PR #44's host-managed resize work stays unread** — it remains a live question for #0294, whose leading hypothesis is a scale-factor fallback in `synchronizeMetrics`. If #0294 is worked before the tag lands, read those commits then; do not assume this decision settled anything about #0294.
- Nothing here changes `BattyKit/Package.swift:32`'s pin.

## Expected behavior

Batty's pin includes PR #45 (via whichever option above is chosen), `-c` panes express `command` / `wait_after_command` per surface through `TerminalSurfaceOptions` instead of app-level config directives, and the bump's other 10 commits have been read and runtime-verified — not just compiled.

## Actual behavior

Pin sits at 1.3.2 (`b146b73a`); `-c` panes express their command override only through controller-level config, which is why they need dedicated per-pane controllers.

## Notes

**Acceptance criterion — #0286's lesson is the governing risk.** That bump looked mechanical and silently flipped `copy-on-select`, clobbering the system clipboard on mouse selection; a runtime test caught it, not the build. An 11-commit bump spanning resize and paste-confirmation changes needs:

- the diffs of all 11 commits actually read (titles are not trustworthy — #0286's regression shipped inside a "leak fix"),
- runtime verification of the touched behavior areas (resize/scale behavior, paste confirmation, `-c` pane semantics including wait-after-command survival), not just `scripts/build.sh` green,
- a cross-check against #0294's repro protocol before and after, given PR #44.

## Relation

- Follows **#0292** (the upstream ask; its `### 2026-08-07` note records the merge facts summarized above) — this issue is the adoption half #0292 explicitly left open.
- Follows **#0286** (bump precedent, exact-tag pin convention, and the copy-on-select lesson).
- **#0287** — refuted the memory rationale; source of the per-surface glyph-atlas finding.
- **#0294** — PR #44's resize work may interact with that investigation; coordinate before bumping.
