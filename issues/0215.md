# 0215 — BattyKit swift test target is broken; stale tests reference removed types

| | |
|---|---|
| **Status** | resolved |
| **Module** | Build / Persistence |
| **Platform** | macOS |
| **First seen** | 2026-05-25 |

## Description

`swift test` in `BattyKit/` does not compile. Four test files reference types that no longer exist:

- `BattyKit/Tests/BattyKitTests/LayoutModelTests.swift`
- `BattyKit/Tests/BattyKitTests/WorkspaceTests.swift`
- `BattyKit/Tests/BattyKitTests/WorkspaceConversionTests.swift`
- `BattyKit/Tests/BattyKitTests/WorkspaceStoreTests.swift`

They reference `Pane`, `Tab`, `SplitNode`, and `Workspace` (the pure-value-type layout model). Those types appear to have been removed or renamed in favor of the current runtime types but the tests were not updated.

The same target is also **not part of the `Batty (Prod)` xcodebuild scheme**, which is why this hasn't surfaced in `scripts/build.sh` runs — so `BattyKitTests` aren't running in CI at all. Discovered during [[0213]] when the subagent needed `swift test --filter ...` to run new tests and had to move the broken files aside temporarily.

## Expected behavior

`swift test` in `BattyKit/` runs all `BattyKitTests` clean, and the `Batty (Prod)` scheme includes `BattyKitTests` so the test target runs in the canonical `scripts/build.sh test` invocation.

## Notes

- Two possible fixes:
  1. **Port the stale tests** to whatever the current runtime types are. Best for keeping coverage of layout/workspace serialization round-trips.
  2. **Delete the stale tests** if the underlying behavior is now covered elsewhere (or never landed). Cheapest, but loses coverage if no replacement exists.
- Either way, add `BattyKitTests` to the `Batty (Prod)` xcodebuild scheme so `scripts/build.sh test` actually exercises it. Otherwise this rot will recur.
- Worth a quick audit while in there: confirm no other `BattyKitTests/*.swift` file is silently broken. The four named above are the ones the [[0213]] subagent hit; there may be more.

## Resolution notes

> Verified resolved 2026-08-07 by inspection and test run. **No code was
> written for this issue** — both defects were fixed incidentally by other
> work, and the stale tests were taken down the "delete" path (option 2
> below) rather than ported.

**Both stated defects are gone:**

1. `swift test` in `BattyKit/` compiles and runs — `swift build` clean, then
   **915 tests in 104 suites passed**. The four named files are deleted:
   `LayoutModelTests.swift`, `WorkspaceTests.swift`,
   `WorkspaceConversionTests.swift`, `WorkspaceStoreTests.swift` are all
   absent from `BattyKit/Tests/BattyKitTests/`.
2. `BattyKitTests` **is** now in the `Batty (Prod)` scheme (three references
   in `Batty.xcodeproj/xcshareddata/xcschemes/Batty (Prod).xcscheme`), and
   `scripts/build.sh unit` is the documented canonical invocation in
   `CLAUDE.md`. It has been running all session.

**On the lost coverage — it is not a gap, because the subject does not
exist.** The deleted tests covered the pure-value-type layout model. Of the
types they referenced, `struct Workspace` **does not exist anywhere in
`BattyKit/Sources`**, and the surviving `Pane` / `Tab` / `SplitNode` names
now resolve to unrelated things: `BattyXPCCore/PaneClosePayload.swift`,
`Views/PaneView.swift`, and `BattyXPCCore/TopologyPayload.swift` — XPC wire
payloads and a view, not the persisted layout model. The value-type layout
model those tests exercised was never built.

That work is tracked by **#0221** ("Workspace model layer not implemented;
`workspace.json` not restored on launch"), which remains open. Coverage for
the value-type model should be written there, against the model as actually
designed — not restored from tests written for a shape that never shipped.

**Status set to `resolved`, not `closed`** — the user closes.
