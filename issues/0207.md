# 0207 — Failing UI test: WorkspacePersistenceTests.testSessionsPersistAcrossRelaunch

| | |
|---|---|
| **Status** | open |
| **Module** | Tests / Persistence |
| **Platform** | macOS |
| **First seen** | 2026-05-20 |

## Description

UI test `BattyUITests.WorkspacePersistenceTests.testSessionsPersistAcrossRelaunch` (`BattyUITests/WorkspacePersistenceTests.swift:53`) fails with:

```
XCTAssertTrue failed - Alpha must persist across relaunch ([[0030]] regression)
```

This test was created by [[0165]] as part of the UI test automation suite. The test was previously masked by the `Thread.sleep` runloop-blocking bug fixed in [[0170]] — the failure is now visible on every test run.

## What to investigate

Open `BattyUITests/WorkspacePersistenceTests.swift:53` and trace what state the assertion is checking. Cross-reference [[0165]] for the test's design and acceptance criteria.

The failure is one of:

- **App bug** — the behavior the test asserts on is genuinely broken; fix the app.
- **Test bug** — the test's assertion, setup, or accessibility-identifier expectation is wrong; fix the test.
- **Test infra gap** — a required driver intent or accessibility identifier referenced by the test doesn't exist yet; add it.

Look at the lines around `:53` and at the driver script (if any) in the test's launch call to determine which.

## Notes

- Surfaced by the [[0170]] test stabilization pass (centralized `waitFor`, eliminated `Thread.sleep` from `@MainActor` test methods).
- Test creation tracked in [[0165]].
- **Likely obsoleted by [[0172]]** — workspace persistence is being removed entirely. When [[0172]] lands, `WorkspacePersistenceTests.swift` should be deleted and this issue can be closed as won't-fix.
