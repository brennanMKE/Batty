# 0216 — Re-pin libghostty-spm to a release that includes the versioned macOS framework fix

| | |
|---|---|
| **Status** | open |
| **Module** | GhosttyKit / Build |
| **Platform** | macOS |
| **First seen** | 2026-05-27 |

## Description

`Lakr233/libghostty-spm` PR #22 ("Fix XCFramework bundle format validation") has been merged (merge commit `d5af462`). It fixes the upstream packaging bug that caused Xcode's embed-frameworks validation to reject the macOS slice of `libghostty.framework` — the same bug that forced the `scripts/fix-libghostty-framework.sh` workaround introduced in [[0212]].

Once a new upstream release is cut that includes this fix, re-pin `BattyKit/Package.swift` to that release and retire the workaround machinery.

## Acceptance criteria

- `BattyKit/Package.swift` `revision:` is updated to a commit at or after `d5af462` on `Lakr233/libghostty-spm` (or to a new release tag that includes it).
- `scripts/build.sh` — `** BUILD SUCCEEDED **` without needing to reshape the framework.
- Direct Xcode builds (`open Batty.xcodeproj` → Product → Build) succeed without `fix-libghostty-framework.sh` having been run first.
- `scripts/fix-libghostty-framework.sh` is removed.
- `scripts/build.sh` and `scripts/run-ui-tests.sh` are simplified to remove the fixup step and its comment.
- `CLAUDE.md` and `issues/Issues.md` are updated to remove the wrapper-necessity note and restore the direct-Xcode-build guidance.

## Notes

- PR #22 also transitioned the xcframework to the Apple-recommended `-library`/`-headers` form (static-library XCFramework) rather than `-framework`. Verify that `GhosttyKit`, `GhosttyTerminal`, and `GhosttyTheme` still resolve correctly after the re-pin — the product names in `Package.swift` should be unchanged, but confirm the build picks them up.
- The current pin is `6ebd4bd2867f4bb8084e438dfa9a6531ad1f2ca7` (tag `1.1.9`). The new pin should be the first tag on `Lakr233/libghostty-spm` whose `Package.swift` references a binary asset built after `d5af462` merged.
- `scripts/build.sh` wrapper (`-resolvePackageDependencies` → fixup → `build`) was introduced specifically because the fixup can't run as an Xcode build phase (see [[0212]] Gotchas). Once the upstream binary is correct, the wrapper reduces back to a thin convenience script or can be removed entirely.
- `release.sh` calls `xcodebuild archive` directly and was noted as unaffected by [[0212]]'s fix but also not fixed by it. Verify the archive step works after the re-pin; file a follow-up if it does not.
