# 0309 — No keyboard shortcut types exit and Enter into the focused Terminal Session to quickly end the shell

| | |
|---|---|
| **Status** | resolved |
| **Module** | App / TerminalSurface |
| **Platform** | macOS |
| **First seen** | 2026-08-04 |
| **Closed** | 2026-08-08 |
| **Branch** | issue/0309 |

## Description

Feature gap: there is no keyboard shortcut that writes the literal text `exit` followed by a newline into the focused Terminal Session. The user wants to hit Cmd-X in a Terminal pane to quickly end the shell — which, per existing lifecycle rules, closes the tab/pane once the shell process exits.

Requested behavior, verbatim intent: "I'd like to hit Cmd-X to input `exit` with the Enter key in a Terminal pane to quickly exit."

## Resolution notes

> 🟢 Resolved 2026-08-08 — **⌘⇧X** sends `exit\n` to the focused Terminal Session, wired as a rebindable `ShortcutAction` with a Tab-menu item. ⌘X and Edit ▸ Cut are untouched.

The chord collision that blocked this was settled by the user in favour of option 3 (a different chord). ⌘⇧X keeps the "eXit" mnemonic while leaving Cut alone.

## Root cause

Feature gap, not a defect. No path existed to send arbitrary text to a Terminal Session from a shortcut — only the paste flow did that, and it was reachable solely from ⌘V.

## Fix

A new `.exitShell` `ShortcutAction`, defaulting to ⌘⇧X, dispatched through all three of the existing action sites:

- **`ShortcutAction.swift`** — the case, its display name, and its default binding. Being in the catalog means it appears automatically in the Command Palette and in Settings ▸ Shortcuts, and is rebindable there.
- **`ExitDispatcher.swift`** (new) — resolves the target with the *same* expression `PasteDispatcher` uses, then calls `tab.terminal.send("exit\n")`.
- **`BattyCommands.swift`** — a Tab-menu item whose key equivalent comes from `shortcuts.keyboardShortcut(for:)` rather than being hardcoded, so a rebind is reflected in the menu.
- **`BattyShortcuts.swift`** — the live NSEvent-monitor path, which is what actually fires on the keystroke.
- **`CommandPaletteView.swift`** — palette dispatch.

Two behaviors were decided rather than left implicit:

- **No Terminal pane focused → no-op.** No window, no selected session, or a pane transiently without an active tab all resolve to `nil` and the call returns without sending.
- **Non-shell foreground process → sends anyway.** If `vim` or `ssh` is running, `exit\n` is typed into that program — exactly as if the user had typed it. `issues/0309.md` raised this and left it open; libghostty offers no cheap "is this a shell" check from the send path, and matching what typing `exit` yourself would do is the stated intent. Documented on `ExitDispatcher` itself.

## Review

Reviewer: `claude-opus-5`, one round, approved.

It re-enumerated every chord claimant independently rather than trusting the implementer's list — the 20 prior `defaultBinding` cases, `reservedShortcuts`, the hardcoded pasteboard/Help/session-switch equivalents in `BattyCommands.swift`, and libghostty's surface-level bindings. On that last one it went further than asked: `TabRuntime.swift:237` issues `keybind = clear` and re-adds only `cmd+c` and the `super`/`alt` arrow set, and the reviewer confirmed from the test log that the clear actually takes effect (`config has 'keybind = clear', all keybinds cleared`). ⌘⇧X is free on every axis.

It verified the `CommandGroup(replacing: .pasteboard)` block is byte-identical to `main`, that both dispatch `switch`es are exhaustive with **no `default:`** clause (so no fourth site silently swallows the case), and that `ExitDispatcher`'s resolution expression matches `PasteConfirmation.swift:109` exactly — a divergence there would have sent `exit` to the wrong terminal, which is destructive.

## Verification

`scripts/build.sh` — BUILD SUCCEEDED. `scripts/build.sh unit` — **969 tests in 112 suites passed**, `** TEST SUCCEEDED **`, up from 960/111. All 9 new tests observed executing and passing by name by both implementer and reviewer, including `Suite ExitDispatcherTests started`/`passed`:

`focusedTerminalTabResolvesToFocusedPaneActiveTabWhenTerminalIsFocused`, `focusedTerminalTabIsNilWhenNoWindowsRemain`, `sendExitDoesNotTrapWhenNoWindowsRemain`, `sendExitDoesNotTrapWhenStoreIsNil`, `sendExitDoesNotTrapWhenTerminalIsFocused`, `exitShellIsInCatalogWithADistinctDefaultBinding`, `everyActionHasAUniqueDefaultBinding`, `dispatchExitShellNoOpsWhenNoWindowsRemain`, `dispatchExitShellDoesNotTrapWhenTerminalIsFocused`.

**What the tests do not prove.** They cover target *resolution* and no-trap behavior. They cannot exercise a live PTY: `TerminalViewState.send(_:)` guards on a nil surface and returns `false` with no libghostty surface attached, and the surface is `public internal(set)` inside the `GhosttyTerminal` package so BattyKit cannot attach a fake and observe bytes. Two of the tests assert conditions true by construction and say so in their own doc comments — they are honest smoke tests, not coverage.

**Nothing asserts the payload is `"exit\n"`.** Asserting it against a constant would be a pure tautology. The newline is therefore the one behavior with no automated coverage, and it is the one that matters: without it, `exit` sits unsubmitted at the prompt.

**Not verified headlessly — needs your sign-off:** pressing ⌘⇧X in a real Terminal pane and confirming the shell receives `exit\n`, ends, and the tab/pane closes per existing lifecycle rules. That is why this is `resolved` and not `closed`.

## Files changed

- `BattyKit/Sources/BattyKit/Shortcuts/ShortcutAction.swift` — `.exitShell` case, display name, ⌘⇧X default.
- `BattyKit/Sources/BattyKit/Commands/ExitDispatcher.swift` — new; target resolution and the send.
- `BattyKit/Sources/BattyKit/Commands/BattyCommands.swift` — Tab-menu item.
- `BattyKit/Sources/BattyKit/Commands/BattyShortcuts.swift` — live keystroke dispatch.
- `BattyKit/Sources/BattyKit/Views/CommandPaletteView.swift` — palette dispatch.
- `BattyKit/Tests/BattyKitTests/{ExitDispatcherTests,ShortcutsStoreTests,CommandPaletteViewActionTests}.swift` — 9 tests.
- `issues/0309.md` — status flip, resolution sections, work log.

## Gotchas

- **⌘⇧X fires even when a text field has focus.** With a rename sheet or a Settings field focused in the content window, the shortcut still sends `exit\n` to the terminal behind it. This is identical to ⌘W's existing exposure, and adding a responder check was explicitly out of scope under the chosen option — but it is a real edge.
- **A new `ShortcutAction` case must be added at three dispatch sites**, not one. Both `switch`es are exhaustive with no `default:`, so the compiler enforces it — the implementer found the third site (the Command Palette) only because the build failed.
- **`everyActionHasAUniqueDefaultBinding` is narrower than it sounds.** It catches intra-catalog collisions only; a future action defaulting to ⌘X, ⌘C, ⌘V, ⌘A or ⌘? would still pass, because those are hardcoded in `BattyCommands.swift` rather than in the catalog. The specific ⌘X-vs-Cut decision is separately pinned by `exitShellIsInCatalogWithADistinctDefaultBinding`.
- **The menu path logs nothing**, unlike its neighbour Close Tab. Minor observability gap for a destructive action.
- **`docs/shortcuts.md` is stale, independent of this change** — its tables already omit `splitFullHeightColumn`, `splitFullWidthRow`, `commandPalette`, `openQuickly`, `layoutPicker`, `themeSelector` and `newWindow`, and list Toggle Sidebar as `Cmd-Ctrl-S` when it is ⌘B. Pre-existing drift; not touched here.

## Expected behavior

With a Terminal pane focused, a single keyboard shortcut sends `exit\n` to that pane's Terminal Session, ending the foreground shell the same as typing `exit` and pressing Return.

## Actual behavior

No such shortcut exists. ⌘X currently reaches Edit ▸ Cut (see Open questions), which does nothing meaningful in a Terminal pane.

## Open questions

**⌘X collides with the standard macOS Cut shortcut — this needs the user's call before implementation.**

Batty already claims ⌘X: `BattyCommands.swift` replaces the `.pasteboard` command group and defines Edit ▸ Cut bound to `⌘X`, dispatching `NSText.cut(_:)` down the responder chain (`BattyKit/Sources/BattyKit/Commands/BattyCommands.swift`, the `CommandGroup(replacing: .pasteboard)` block). That Cut item is real and useful for editable text fields elsewhere in the window — sidebar Session rename, Tab rename, Settings fields — where `NSText.cut(_:)` resolves to the first responder's text view. Terminal panes themselves have no selection-cut: the terminal surface is not an `NSText`, so in a Terminal pane the existing ⌘X is effectively a no-op today (copy is the terminal-side pasteboard verb; there is nothing to cut from a read-only surface).

Options, laid out neutrally — none chosen:

1. **Claim ⌘X globally** for "type exit" — simplest, but Edit ▸ Cut loses its key equivalent, breaking cut in rename fields and Settings.
2. **Claim ⌘X conditionally** — behave as "type exit" only when a Terminal pane is first responder, fall through to `NSText.cut(_:)` otherwise. Precedent exists: `PasteDispatcher.handlePasteRequest` (`BattyKit/Sources/BattyKit/Views/PasteConfirmation.swift`) already forwards ⌘V to the standard `NSText.paste(_:)` when `isEditingTextField` detects an editable `NSTextView`/`NSTextField` first responder, and only routes to the terminal otherwise. The same responder check would work here. Cost: a system-standard chord silently changes meaning by focus context.
3. **Use a different chord** (e.g. something under ⌘⇧ or ⌃) and leave Cut untouched — least surprising, but not the chord the user asked for.

Secondary decisions to make at implementation time (raised, not decided):

- **Foreground process is not a shell.** If `vim`, `ssh`, a REPL, or any full-screen program is running, `exit\n` is just typed into that program (in vim it inserts text or errors; in ssh it exits the remote shell, not the local pane). Options: send unconditionally and accept that, or gate/confirm somehow. libghostty offers no cheap "is the foreground process a shell" check from the send path used here.
- **Menu item vs invisible key handler.** A real menu item (discoverable, shows the key equivalent, participates in the customizable-shortcut system — `ShortcutAction` / `ShortcutsStore` in `BattyKit/Sources/BattyKit/Shortcuts/` back nearly every existing chord and surface in Settings) versus a bare key handler nobody can discover or rebind. Existing convention strongly favors a `ShortcutAction` case plus a menu item, but ⌘X-vs-Cut interacts with this: a menu item's key equivalent is claimed window-wide by AppKit, which pushes toward option 2's responder-aware dispatch inside the action rather than SwiftUI-level shortcut swapping.

## Notes

Relevant code locations, verified read-only:

- **Menu commands / shortcuts** — `BattyKit/Sources/BattyKit/Commands/BattyCommands.swift` (SwiftUI `Commands` with `CommandGroup`/`CommandMenu`; wired from `Batty/BattyApp.swift`). Customizable chords live in `BattyKit/Sources/BattyKit/Shortcuts/ShortcutAction.swift` + `ShortcutsStore.swift`.
- **Send-text path into a live Terminal Session** — the paste flow is the model: `PasteDispatcher` resolves the focused terminal tab via `(store.keyWindowRuntime() ?? store.windows[0]).selectedSession?.focusedPane.activeTab` and calls `tab.terminal.send(text)` (`TerminalViewState.send(_:)` from the `GhosttyTerminal` product of `libghostty-spm`). `AppTerminalView.sendText(_:)` is the equivalent NSView-level wrapper ("Send raw UTF-8 text directly to the underlying pty, bypassing key translation"). Sending `"exit\n"` through the same path is the whole write side of this feature.
- Terminal lifecycle already handles shell exit → tab close, so no new teardown work is expected; this issue is purely input + shortcut routing.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-04 | file | claude-fable-5 | 48 | 8,673 | 1,079,605 | 57,645 | $2.23 |
| 2026-08-08 | implement | claude-sonnet-5 | 94 | 551 | 4,073,929 | 186,496 | $1.29 |
| 2026-08-08 | review | claude-opus-5 | 3,815 | 73 | 1,312,722 | 101,532 | $1.31 |

**Total: $4.83**
