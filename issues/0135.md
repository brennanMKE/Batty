# 0135 — Window chrome should change with the active theme

| | |
|---|---|
| **Status** | resolved |
| **Module** | Theme / App |
| **Platform** | macOS |
| **First seen** | 2026-05-18 |
| **Closed** | 2026-05-18 |
| **Commit** | 8eaeb41 |

## Description

Switching themes (View → Theme menu) currently only repaints the terminal contents — which makes sense, because terminal colors are controlled by Ghostty and the `.ghostty` theme file. But the window chrome around the terminal (window background, tab bar, sidebar, dividers, focus borders, popovers) stays at the system default appearance regardless of theme.

The visual mismatch breaks the illusion of a themed app — a Mocha or Tokyo Night terminal sitting inside a default-gray macOS window looks like two unrelated apps stacked on top of each other. A themed terminal should have themed chrome.

## Acceptance criteria

- When the active theme changes (View → Theme, or any other entry point that swaps the Ghostty theme), the chrome around the terminal updates to match:
  - Window background color (the area visible behind panes, the gap around splits)
  - Tab bar background and chip background
  - Sidebar background and selected-row tint
  - Split dividers
  - Focus border on the focused pane
  - Pane-swap drop-target highlight (`#0127`) — should still read as an accent, but adapted to the theme palette
- The theme switch is **live** — no restart, no window-close-and-reopen required.
- Default behavior (no theme selected, or a "System" theme option if one exists) falls back to the standard macOS adaptive chrome (light / dark per system appearance).
- The chrome derives from the same `.ghostty` theme file that drives terminal colors — single source of truth. No separate "chrome theme" picker.
- Readable in both light and dark themes; in particular, text and icons on the chrome should maintain contrast against the themed background.

## Out of scope

- Authoring new themes or extending the `.ghostty` theme file format. If the file already exposes a foreground/background/accent that the chrome can read, use those; if it doesn't, this issue stays scoped to "wire what's available" and a separate issue covers extending the schema.
- Theming the macOS title bar / traffic-light buttons themselves. macOS gives limited control there; what we can do is set window background and let the title bar adapt (`titlebarAppearsTransparent`, `backgroundColor`, etc.).
- Theming non-Batty windows (Settings, About). Those can stay system-native; the work here is the main terminal window's chrome.
- The bell-feed popover and other transient surfaces — nice-to-have, but if they're not trivially themable, defer to a follow-up.

## Notes

- The Ghostty theme file format includes colors like `background`, `foreground`, `selection-background`, `cursor-color`, plus the 16 ANSI colors and (sometimes) `palette` entries. Window chrome wants `background` (window/pane fill), `foreground` (chrome text/icons), and probably one of the ANSI colors or a derived accent for highlights.
- Look at where the active theme is currently loaded and applied to Ghostty surfaces — that hook is the natural place to also publish a `ThemeChrome` value (or similar) that SwiftUI views observe.
- SwiftUI's `.preferredColorScheme(_:)` won't be enough on its own — it switches between system light/dark variants but doesn't apply arbitrary background colors. The work is likely to publish a `@Observable` chrome palette and have key chrome views read from it (`SessionSidebarView`, `PaneView`'s tab bar background, `SplitContainerView` dividers, focus-border overlay, etc.).
- Watch out for hardcoded `Color.gray`, `Color(.windowBackgroundColor)`, `Color.accentColor` references in the chrome views — those are the things that need to flow through the new palette instead.
- Test against at least one light theme and one dark theme to confirm contrast holds in both directions. Mocha (dark) and one of the lighter Ghostty themes are a reasonable pair.
- Related: `#0127` pane-swap drop highlight currently uses `Color.accentColor`. It should probably read from the themed accent so it doesn't clash with the rest of the chrome.

## Root cause

What was missing: the theme application path stopped at the libghostty surface. `AppStateStore.applyThemeToAllSurfaces(_:)` walked every tab and called `controller.setTheme(_:)`, which repainted the terminal canvas, but nothing fed the same `.ghostty` color slots into the SwiftUI chrome. Every chrome view that needed a color hardcoded `Color.accentColor`, `Color.gray.opacity(...)`, `Color(nsColor: .windowBackgroundColor)`, or `Color(nsColor: .separatorColor)`, so the chrome stayed on the standard macOS adaptive palette no matter which theme was selected. The fix is plumbing — there was no single-source-of-truth palette to read from.

## Fix

Introduced `ThemeChrome` (`@Observable`, `@MainActor`) that holds a `ChromePalette` derived from a `GhosttyThemeDefinition`. The palette publishes optional colors for window/chrome background, primary and muted chrome foreground, accent, divider, tab active/inactive fills + strokes, and the sidebar selection tint. Every slot is `Color?` — `nil` means "fall back to the system default", which lets chrome views keep their pre-theming look when no theme is selected or a theme file lacks a needed slot.

Source-color mapping:

- `windowBackground` / `chromeForeground` ← theme `background` / `foreground` (required; if missing or malformed, the palette degrades to all-`nil` defaults).
- `chromeBackground` ← `background` shifted 6% toward `foreground` so the chrome reads as a layer above the canvas without sliding into a different hue family.
- `accent` ← `cursor-color` first (the theme author's explicit highlight), then ANSI 4 (blue, the conventional accent), then ANSI 6 (cyan). Skips red/green/yellow because those carry semantic meaning in terminal output. A WCAG contrast check (≥ 1.5 against the background) skips washed-out candidates; falls back to foreground if every option is too low-contrast.
- `divider` ← foreground at 18% alpha — legible across both dark and light themes without a separate tone choice.
- Tab strokes pick the higher-contrast of (accent, foreground@50%) against the active-tab fill so a beige accent over a beige tab on a light theme still has a visible outline.

`AppStateStore.applyThemeToAllSurfaces(_:)` now calls `themeChrome.update(from: theme)` after pushing the libghostty repaint, so the chrome and the terminal flip in lockstep with no view rebuilds and no surface teardown. The chrome is seeded at `AppStateStore.init` from the persisted theme name in `UserDefaults`, so the first window already has the right chrome at launch.

`RootWindowView` publishes the `ThemeChrome` via `EnvironmentValues.themeChrome`, and a small `WindowChromeApplier` `NSViewRepresentable` pushes the themed `windowBackground` onto the hosting `NSWindow` with `titlebarAppearsTransparent = true` so the title bar adapts; reverting to a non-themed palette restores the system title-bar material. Chrome views (`SessionSidebarView`, `SessionDetailView`, `PaneView`, `BattyTabChip`, `SplitContainerView`) read the palette and substitute themed colors for the hardcoded ones, falling back to their previous defaults when the env value is `nil`.

The pane-swap drop highlight from `#0127` was rewritten to thread `accentColor` through the `PaneSwapDropTarget` modifier so it shares the themed accent with the rest of the focus chrome.

## Files changed

- **`BattyKit/Sources/BattyKit/Theme/ThemeChrome.swift`** — new. `ThemeChrome` `@Observable`, `ChromePalette` value type, `RGBColor` helper (hex parse, blend, alpha, WCAG luminance + contrast), and `EnvironmentValues.themeChrome` key.
- **`BattyKit/Sources/BattyKit/Theme/ThemePreference.swift`** — adds `ThemePreference.activeTheme()` so the store can seed its palette at launch; `applyThemeToAllSurfaces(_:)` now updates `themeChrome` after the surface repaint.
- **`BattyKit/Sources/BattyKit/Runtime/AppStateStore.swift`** — owns a `let themeChrome: ThemeChrome`, seeded from the persisted theme on init.
- **`BattyKit/Sources/BattyKit/Views/RootWindowView.swift`** — injects `themeChrome` into the environment and installs `WindowChromeApplier` to push the themed `windowBackground` and `titlebarAppearsTransparent` onto the hosting `NSWindow`.
- **`BattyKit/Sources/BattyKit/Views/SessionDetailView.swift`** — themed window background fills the detail area behind the terminal host and around splits.
- **`BattyKit/Sources/BattyKit/Views/SessionSidebarView.swift`** — list background, footer ("+" button) background, selected-row tint, and the unseen-bell badge accent read from `ThemeChrome`.
- **`BattyKit/Sources/BattyKit/Views/PaneView.swift`** — tab bar background, focus border, bell-flash border, drag-hover border, pane-swap drop target, and pane drag preview all read themed accent / chrome colors. `PaneSwapDropTarget` modifier takes an explicit `accentColor` so it can be passed the themed accent from the calling view.
- **`BattyKit/Sources/BattyKit/Views/BattyTabChip.swift`** — accent dot, primary / secondary text colors, active / inactive fills + strokes all read from `ThemeChrome` with the previous gray fallbacks.
- **`BattyKit/Sources/BattyKit/Views/SplitContainerView.swift`** — split dividers read `themeChrome.divider` (fallback `Color(nsColor: .separatorColor)`).
- **`BattyKit/Tests/BattyKitTests/ThemeChromeTests.swift`** — new. 12 Swift Testing cases covering the default palette, nil/malformed theme fallbacks, accent picker order (cursor → ANSI 4 → ANSI 6 → foreground), contrast-based legibility picker, WCAG luminance math (black=0, white=1), contrast symmetry, channel blending, and the live `update(from:)` round-trip.

## Gotchas

- Visual contrast in both light and dark themes is the headline check that can only be done interactively — switch between Mocha (dark) and any light-background theme (e.g. one of the `3024 Day` / Solarized Light variants) and confirm the sidebar, tab bar, dividers, focus borders, and window background all follow without any unreadable text. The contrast-pick logic for the active-tab stroke and the accent legibility guard are best-effort but only a human can sign off on the result.
- The **bell-feed popover** (`BellFeedView`) is intentionally untouched — its `Color.accentColor` dot for unseen entries currently still uses the system accent. Popovers are AppKit-vibrant surfaces; theming them properly is a follow-up. Other transient surfaces (CommandPalette, OpenQuickly, RenameTabSheet, settings) are similarly out of scope per the issue.
- The title bar adapts via `titlebarAppearsTransparent = true` + `window.backgroundColor = themed`. The traffic-light buttons themselves stay system-native (out of scope). Reverting the picker to "Default" restores `titlebarAppearsTransparent = false` so the title bar gets its vibrant material back.
- `WindowChromeApplier.updateNSView` dispatches the actual `NSWindow` mutation onto the next runloop tick. The first call happens before SwiftUI has attached the `NSView` to a window, so a synchronous read of `nsView.window` returns `nil` and silently no-ops. The dispatch makes the cold-launch case work without adding a polling loop.
- `ChromePalette(theme:)` returns `.default` (all `nil`) when `background` or `foreground` is unparseable. Every chrome view tolerates `nil` slots, so a partial/broken theme degrades to the existing macOS adaptive defaults rather than rendering a black-on-black surface.
- The catalog default (`TerminalTheme.default`) used by `TabRuntime.activeTheme()` when the picker is on "Default" doesn't flow into `ThemeChrome` — there's no `GhosttyThemeDefinition` to derive from, so the chrome stays in its system-default state. That matches the acceptance criterion that "Default" falls back to the standard macOS adaptive chrome.


## Re-opened

**Reported by user 2026-05-18:** the themed sidebar-selection tint is too aggressive in most themes. In dark themes with a vivid accent (the user's olive-toned theme in screenshot 3) the selected-row block reads as a glaring colored rectangle that fights with the rest of the chrome instead of blending in. The user's words: "the background behind the selected session should just use the background color for the sidebar behind it."

**Where the bug is:** `ChromePalette.sidebarSelectionTint ← accent at 28% alpha`. At 28%, a saturated accent (olive, bright cursor color from some themes) shows through as a strong block — particularly bad on darker sidebar backgrounds where the accent has a lot of contrast.

**Fix direction:** stop overriding the selection chrome with the themed accent. The bar to clear is the default macOS `List` selection appearance — a subtle, system-managed gray that adapts to light/dark without us picking colors. Concretely:

- Set `sidebarSelectionTint = nil` in `ChromePalette` so the slot is unset.
- In `SessionSidebarView`, when the selection-tint slot is `nil`, do not paint a custom background on the selected row — let SwiftUI's `List` (or whichever selection chrome was overridden) use its default selection style.
- Selection still needs to be visible. The default macOS look (screenshot 2) is acceptable. If the leading-edge stripe shown in screenshot 1 was added by the themed chrome work, decide whether to keep it: a thin themed stripe is fine, a 28%-alpha block is not. Default to "no extra chrome" and bring the stripe back as a separate enhancement only if the default selection becomes invisible in some theme.

**Acceptance criteria for the fix:**

- In every theme (including the olive theme that motivated this), the selected sidebar row reads as "subtly highlighted," not as a colored block that dominates the sidebar.
- The unselected rows continue to use the themed sidebar background.
- Switching between themes does not flash a tinted selection.
- The "Default" theme (no themed chrome) selection look is preserved.
- Other themed chrome from the v1 of #0135 stays intact: tab bar bg, focus border, window bg, dividers, drop-target highlight, etc. **Do not regress those.**

**Screenshots:** user-attached screenshots 1–3. Screenshot 3 (olive theme) is the clearest reproduction; screenshot 2 (default) is the look to converge on.

## Re-resolution

Dropped the accent-derived sidebar selection tint. `ChromePalette(theme:)` now always sets `sidebarSelectionTint = nil` (the slot stays in the type so a future enhancement can re-introduce a gentler value), and `SessionSidebarView` only attaches `listRowBackground` when the tint is actually present — when it isn't, the row gets no custom background and SwiftUI's `.sidebar` list style paints its default selection chrome. The rest of the v1 themed chrome (window background, tab bar fills, dividers, focus border, drop-target highlight, sidebar non-selected background, footer accent on the unseen-bell badge) is unchanged.

### Files changed

- **`BattyKit/Sources/BattyKit/Theme/ThemeChrome.swift`** — `ChromePalette(theme:)` no longer derives `sidebarSelectionTint` from the accent; the slot is explicitly assigned `nil` with a short comment explaining why (so a future reader doesn't reintroduce the 28%-alpha block).
- **`BattyKit/Sources/BattyKit/Views/SessionSidebarView.swift`** — `rowBackground(for:)` returns `Color?` instead of an opaque view, and a new `SidebarRowBackground` view modifier only calls `.listRowBackground(_:)` when a non-nil tint exists. With the tint nil for every themed path, the selected row falls back to SwiftUI's default sidebar selection chrome.

### Tests changed

- **`BattyKit/Tests/BattyKitTests/ThemeChromeTests.swift`** — `defaultPaletteIsAllNil` now also asserts `sidebarSelectionTint == nil`, and a new `sidebarSelectionTintIsAlwaysNil` test pins the behavior for a fully-specified theme (including a cursor color and ANSI palette entries) so a regression that re-derives the tint would fail loudly.

### Gotchas

- Visual sign-off is still the user's: confirm the olive-theme reproduction from screenshot 3 is gone, and check that selection is still visible across a light theme and a dark theme. macOS's default `.sidebar` selection chrome is adaptive; in extremely low-contrast theme combinations it may read as fainter than ideal — that's the deliberate trade for not painting a colored block, and any future "stripe / subtle tint" work should be a separate enhancement issue rather than a re-introduction of an accent-derived background.
- Non-selection chrome was deliberately left untouched. The full v1 surface coverage (tab bar background, focus border, window background, dividers, pane-swap drop highlight, tab chip fills/strokes, sidebar unselected background, unseen-bell badge accent) still derives from the active `.ghostty` theme.

## Re-opened (round 2)

**Reported by user 2026-05-18 (after first re-resolution):** the previous fix (drop `sidebarSelectionTint` to `nil`, let SwiftUI default kick in) does not actually look right. In every theme (user-attached screenshots show red, navy, green), the selected sidebar row paints a **dark gray block** that does not adapt to the themed sidebar. SwiftUI's `.sidebar` selection chrome is a fixed grey that ignores the container background.

**What the user actually wants:** the selected row's background should be the themed sidebar background (i.e., visually identical to the unselected rows). Selection has to be indicated by *something*, but not by a colored block at all.

**Fix direction:**

- **Suppress** SwiftUI's default selection chrome on the sidebar. Setting the slot to `nil` was not enough — the system still paints its default selection. Likely needs `.listRowBackground(Color.clear)` on the selected row (or an explicit `.listRowBackground(themed sidebar bg)`), so no system gray peeks through.
- **Indicate selection with theme-neutral chrome** that does not fight the background:
  - A leading-edge accent stripe (3–4pt wide, themed accent at full opacity) along the inner left edge of the selected row. Visible against any sidebar background because it's a thin line, not a fill.
  - Optionally: bold the text and/or tint the icon with the themed accent. Pick whichever combination reads as "selected" without crowding the row.
- The leading stripe was present in early v1 work (visible faintly in earlier screenshots) — bring it back, themed accent, full-opacity, narrow.
- All other themed chrome must stay intact: tab bar bg, focus border, window bg, dividers, drop-target highlight, tab chip strokes, etc. **Only the sidebar selection visual changes.**

**Acceptance criteria for this round:**

- In a red theme: selected row background = red sidebar bg (no gray block). Selection is visible via the leading accent stripe (and bold text if added).
- Same for navy, green, olive (the prior reopen's theme), and Mocha. No gray block in any theme.
- Default theme (no themed chrome): selection still has *some* visible indicator. Falling back to system default selection chrome is acceptable in the "Default" path only.
- No regression on any other themed chrome from the v1 of #0135.

**Screenshots:** user-attached screenshots show the dark gray block over red, navy, and green sidebar backgrounds. Screenshot of the red theme is the clearest reproduction.

## Re-resolution (round 2)

The round-1 re-resolution misread "make the selection not look like a colored block" as "leave the slot empty and let SwiftUI handle it". Setting `sidebarSelectionTint = nil` only told `SidebarRowBackground` not to paint a custom tint; it didn't suppress anything. SwiftUI's `.sidebar` list style still painted its own selection chrome — a fixed dark gray rectangle that ignores the surrounding sidebar background — so on every themed sidebar (red, navy, green, olive) the selected row read as a gray block sitting on top of the themed background.

This round suppresses the system selection chrome by attaching `.listRowBackground(themed sidebar bg)` to every row (not just the selected one) when a theme is active. With every row already declaring its own background, the system has nothing left to paint and the selected row's background visually matches the unselected rows. Selection is now indicated entirely by row content: a 3pt themed-accent stripe along the inner left edge of the selected row, a semibold title, and an accent-tinted icon. On the "Default" theme path (no themed `chromeBackground`), the modifier skips `listRowBackground` entirely so SwiftUI's system selection chrome continues to apply as before.

### Files changed

- **`BattyKit/Sources/BattyKit/Views/SessionSidebarView.swift`** — `SidebarRowBackground` now takes the themed sidebar background and applies `.listRowBackground(_:)` to every row when present (not only the selected row), which is what actually suppresses the system selection paint. `SessionRow` gains `isSelected` / `themed` flags and renders a leading 3pt accent stripe, a semibold title, and an accent-tinted icon when selected on a themed surface. The unused `rowBackground(for:)` helper that returned the v1 selection tint is removed.

### Tests changed

- None. The existing `sidebarSelectionTintIsAlwaysNil` test still passes (the slot in `ChromePalette` is unchanged — the round-2 work moved entirely into `SessionSidebarView`). SwiftUI selection chrome and overlay rendering aren't cleanly unit-testable, so no new test was added for the stripe.

### Gotchas

- Visual sign-off is still the user's. Verify on red, navy, green, olive, and Mocha that the selected row's background matches the surrounding sidebar (no gray block) and the accent stripe + bold title + tinted icon read as "selected". Verify on the "Default" theme that selection still has a visible indicator (system default chrome).
- The suppression mechanism is `.listRowBackground(_:)` on every row, not on the selected row alone. Applying it only to the selected row would let the system selection paint through on the *non-selected* rows of any theme that doesn't perfectly match the system gray — which is what bit round 1.
- `sidebarSelectionTint` is now effectively dead in `ChromePalette` (always `nil`, never read). Left in the type so a future "subtle themed tint" experiment can flip the slot back on without a schema change; if it becomes obvious the slot will never be used again, it can be deleted in a follow-up.

## Re-opened (round 3)

**Reported by user 2026-05-18:** with four horizontally-split panes open, **vertical lines** are visible between adjacent panes in the chrome area above the tab bars (user-attached screenshot). These read as unwanted dividers — the chrome should appear seamless across the splits.

**Where the lines are:** between adjacent panes in a horizontal split, just below the window title bar, above each pane's tab bar. They appear to be drawn by either:

1. `SplitContainerView`'s themed `divider` extending up into the chrome strip (instead of stopping at the terminal-body region).
2. Per-pane chrome borders (tab bar background or focus border) that don't blend with the neighbor — so the seam between two panes shows the boundary.
3. An extra divider rule being drawn at the top edge of each pane's chrome.

**Fix direction:**

- Investigate what's actually drawing those vertical lines (run with the themed chrome on, four horizontal panes — the easiest reproduction).
- The chrome strip above the tab bars should be a single continuous band of `chromeBackground` across the entire window, **without** divider rules between adjacent panes. The split dividers belong to the terminal-body region, not the chrome strip.
- If `SplitContainerView` is the source: have it draw dividers only across the body, not across the full pane height. Or render the chrome strip in a separate layer that spans the window above the splits.
- If per-pane chrome borders are the source: drop the edge stroke between adjacent panes, or render the chrome with no per-pane border at all (let the tab bar bg be the only chrome).
- Whichever fix: the focused-pane border (focus outline) must keep working — that's a separate visual layer and should not be regressed.

**Acceptance criteria:**

- With multiple horizontally-split panes, no vertical lines appear in the chrome strip above the tab bars. The chrome reads as a single continuous band.
- Same for vertically-split panes — no horizontal lines in the chrome strip on the side.
- Split dividers in the terminal-body region still work (resize, hit-test, visible at appropriate weight).
- Focused-pane border still visible.
- All other themed chrome from rounds 1 + 2 stays intact.

**Screenshot:** user-attached screenshot 2026-05-18 at 9.05.42 PM shows four panes side-by-side with the offending vertical lines.

## Re-resolution (round 3)

Hypothesis A was the source: `SplitContainerView`'s `DraggableSplitView` painted a single `Rectangle().fill(themeChrome.divider)` spanning the entire pane height for every horizontal split. Each pane's tab bar (chrome strip) sits at the top of its `PaneView` body, so the full-height divider cut through every chrome strip and read as the offending vertical line above the tab bars.

`PaneView` now publishes its chrome strip height via a new `ChromeStripHeightPreferenceKey` (a `.background` `GeometryReader` on the chrome `HStack`). `DraggableSplitView` observes the preference and renders the horizontal divider as a vertical `VStack` of two rectangles: the top chunk (height = the observed chrome strip height) is filled with `themeChrome.chromeBackground` so it merges seamlessly with the chrome bands on both sides; the rest is filled with the existing divider color. Drag affordance, hit-testing, and resize cursor are unchanged because the gesture/`contentShape` modifiers are applied to the whole divider group. Vertical dividers stay a single body-colored rule — in a vertical split each pane's chrome sits at its own top edge so the horizontal divider never intersects a chrome strip.

### Files changed

- **`BattyKit/Sources/BattyKit/Views/PaneView.swift`** — adds a `.background` `GeometryReader` on the tab-bar `HStack` that publishes the chrome strip height via `ChromeStripHeightPreferenceKey`.
- **`BattyKit/Sources/BattyKit/Views/SplitContainerView.swift`** — defines `ChromeStripHeightPreferenceKey`, observes it on `DraggableSplitView`, and splits the horizontal divider into a top chrome region (painted with `chromeBackground`) and a body region (painted with the divider color). Vertical divider stays a single body-colored rule.

### Tests changed

No new tests. Preference-key bubble-up and divider painting are SwiftUI layout behavior that isn't cleanly unit-testable; all 268 existing tests still pass.

### Gotchas

- Visual sign-off remains the user's. Verify with four horizontally-split panes (no vertical lines through the chrome band above the tab bars), four vertically-split panes (chrome band on each pane still reads cleanly), and at least one nested layout (e.g., a vertical split containing a horizontal split). Re-check focus border and pane-swap drop highlight still render on the focused/targeted pane.
- The preference key uses `max()` to reduce. If a future change introduces panes with different chrome strip heights (it shouldn't — they all use the same `SlidingTabBar`), the divider will paint the taller height as chrome, which is the safer side to err on.
- When no theme is active, `chromeBackground` is `nil` and the chrome region falls back to the divider color — preserving the pre-#0135 look on the "Default" theme so nothing visual changes for that path.
- The chrome strip height is read from the `HStack` that hosts the `SlidingTabBar`, not from the `SlidingTabBar` itself. If a future change adds further padding around the tab bar (or replaces the bar with a taller toolbar), the preference still measures the correct chrome extent because it observes the same view whose background paints `chromeBackground`.

## Re-opened (round 4)

**Reported by user 2026-05-18 (after round-3 resolution):** the lines are still there. New screenshot (red theme, 4 horizontally-split panes) shows thin vertical lines running the full height between every pair of adjacent panes. The round-3 fix (`SplitContainerView` divider's chrome region painted with `chromeBackground` based on a `ChromeStripHeightPreferenceKey` published by `PaneView`) is present in the code but is not producing the intended visual.

**Likely failure modes — investigate with actual instrumentation, do not guess:**

1. **Preference value never reaches `DraggableSplitView.chromeStripHeight`.** The `@State` defaults to 0; `.onPreferenceChange` only fires on a change. If the published value is also 0 (or never published), the state stays at 0, the chrome region of the divider has zero height, and the entire divider paints with the body color (foreground at 18 percent alpha — exactly the thin gray line shape in the screenshot). Possible reasons: the `GeometryReader` inside `.background { ... }` measures 0 because the background layer hasn't been laid out yet; preference doesn't propagate cleanly through the `AnyView` wrappers in `SplitNodeView`; the preference is being reset by an intermediate view.
2. **`themeChrome?.chromeBackground` is nil at `DraggableSplitView`'s `@Environment(\.themeChrome)` read site** even though the rest of the chrome is themed. If the environment value doesn't reach the split container (e.g., scoping issue), `dividerChromeColor` falls back to `dividerBodyColor` — same thin line painted full height.
3. **A separate source of lines** (per-pane stroke, focus border at nonzero idle opacity, an overlooked `Divider()` somewhere in the chrome path).

**Diagnostic first, fix second.** Before writing any fix, add temporary `logger.debug(...)` (or `print(...)` if needed) at:

- `PaneView`'s `GeometryReader` preference publish — log the published height per pane.
- `DraggableSplitView`'s `.onPreferenceChange` callback — log the received value.
- `DraggableSplitView`'s `dividerChromeColor` computed property — log whether `themeChrome?.chromeBackground` is nil.

Run the app, open 4 horizontal panes, observe Console.app or the run log. Confirm which value is wrong before changing anything.

**Fix shape depends on what you find:**

- If the preference never propagates: switch to a different mechanism. Options: pass the chrome strip height down via the environment (set by `SessionDetailView` once for the whole tree); make `chromeStripHeight` a fixed compile-time constant matching the tab bar height (e.g., 30pt) and read it from a single source of truth; restructure so the chrome strip is a sibling layer above the split container rather than per-pane.
- If `themeChrome` is nil at the split container: thread it down explicitly, or read it via `EnvironmentValues` at the divider-color computation site.
- If a separate source: track it down and remove or fix.

**Acceptance criteria** (same as round 3 — re-stated because the visual still doesn't meet them):

- With 4 horizontally-split panes in a themed window, **no vertical lines** in the chrome strip above the tab bars, **and no vertical lines** running through the body region above the tab bars. The chrome band reads as one continuous strip.
- The split divider rule is visible in the **terminal body region only** (between the actual terminals), thin and themed.
- Drag-resize on the divider still works (hit area, cursor change).
- Focused-pane border + pane-swap drop highlight still work.
- All other themed chrome from rounds 1+2 stays intact.
- Remove any debug instrumentation before committing.

**Screenshot:** user-attached screenshot 2026-05-18 at 9.16.08 PM. Red theme, 4 horizontally-split panes, lines clearly visible between every pair.

## Re-resolution (round 4)

The round-3 plumbing (`PaneView` publishes chrome strip height via `ChromeStripHeightPreferenceKey`, `DraggableSplitView` observes it and uses it to size the chrome region of the divider) was the right *shape* of fix but the value never landed: `chromeStripHeight` in `DraggableSplitView` stayed at 0, so the divider's chrome region collapsed to 0pt and the body color (foreground at 18% alpha) painted full-height — the thin vertical lines visible in the round-4 screenshot. The most likely cause is `SplitNodeView.body` returning `AnyView`, since `AnyView` is known to interfere with `PreferenceKey` propagation through the wrapped subtree. Rather than spelunk further into AnyView preference behavior, we replaced the measurement with a constant: `SlidingTabBar.barHeight` is the only vertically-sized element in the chrome `HStack`, and it's fixed at 36pt.

Replaced `ChromeStripHeightPreferenceKey` (struct + `.preference` publication in `PaneView` + `.onPreferenceChange` observer + `@State` in `DraggableSplitView`) with a single file-scope `private let chromeStripHeight: CGFloat = 36` in `SplitContainerView.swift`. `horizontalDivider` continues to paint the top `chromeStripHeight` pt with `dividerChromeColor` (themed `chromeBackground`, falling back to the body color when no theme is active) and the rest with `dividerBodyColor`. Drag-resize, hit-testing, the vertical-split divider, focused-pane border, and the rest of the themed chrome are unchanged.

### Files changed

- **`BattyKit/Sources/BattyKit/Views/SplitContainerView.swift`** — removed `ChromeStripHeightPreferenceKey`, removed the `.onPreferenceChange` observer and `@State chromeStripHeight` from `DraggableSplitView`, added a file-scope `chromeStripHeight: CGFloat = 36` constant with a comment pinning the value to `SlidingTabBar.barHeight` and explaining why a `PreferenceKey` was not used.
- **`BattyKit/Sources/BattyKit/Views/PaneView.swift`** — removed the `.background { GeometryReader { … .preference(key: ChromeStripHeightPreferenceKey…) } }` block on the chrome `HStack`. The themed `chromeBackground` `.background` modifier above it stays.

### Gotchas

- **Visual sign-off remains the user's.** Verify with 4 horizontally-split panes in a themed window (red theme is the easiest reproduction from round 4) — no vertical lines should run between any pair of adjacent panes' chrome strips. Then verify with 4 vertically-split panes (chrome band on each pane still reads cleanly, no horizontal lines). And nested layouts (a horizontal split inside a vertical split, or vice versa). Focus border, pane-swap drop highlight, and the rest of the themed chrome from rounds 1–3 should be unchanged.
- **`SlidingTabBar.barHeight` is now a load-bearing constant.** If a future change adjusts the SlidingTabBar's pinned height (or wraps it in extra vertical padding), the `chromeStripHeight: CGFloat = 36` constant in `SplitContainerView.swift` must change in lockstep, or the divider will paint a body-colored stub at the top (or under-paint the chrome region). The constant carries a comment to flag this.
- **Why not a `PreferenceKey`:** the round-3 attempt failed because `SplitNodeView.body` returns `AnyView`, and preference values from inside `AnyView`-wrapped subtrees do not always reach an `.onPreferenceChange` upstream — they're either reset or measured at 0 before SwiftUI settles. A constant trades the dynamic measurement for a brittle-but-trivial source of truth; in this codebase the chrome strip is intentionally one specific shape, so the constant is the better fit. If a future need genuinely requires per-pane variable chrome heights, the right fix is to remove the `AnyView` from `SplitNodeView` (e.g. with `@ViewBuilder` and an enum-driven helper), not to re-add the `PreferenceKey`.
- **No new tests.** Preference-key propagation across `AnyView` is itself a SwiftUI behavior that isn't cleanly unit-testable; the new constant is checked at the build level (it has to match `SlidingTabBar.barHeight` by inspection) and visually.

## Re-resolution (round 5)

**The combined fix for both the divider lines AND the sidebar selection regression, after rounds 3 and 4 attacked the right symptom with the wrong mechanism.**

**Root cause (both bugs share it):** `SplitNodeView.body` returns `AnyView`, which suppresses SwiftUI environment propagation into the wrapped subtree in just the right way to defeat round 3's `PreferenceKey` (chrome strip height) and round 4's `@Environment(\.themeChrome)` reads at `DraggableSplitView`. The divider's `dividerChromeColor` was therefore falling back to `dividerBodyColor` (foreground at 18% alpha) instead of `chromeBackground` — painting a thin themed line full-height between every pair of horizontally-split panes, exactly the symptom the user reported across three iterations.

**Fix:** thread the palette colors explicitly through `SplitContainerView` → `SplitNodeView` → `DraggableSplitView` as init parameters. `SplitContainerView` reads `@Environment(\.themeChrome)` at its own level (where it does resolve) and passes the two needed `Color?` slots down. Round 4's two-region rendering inside `horizontalDivider` is preserved; what changed is the source of the colors. Also changed the body region's color from `divider` (foreground at 18%) to `windowBackground` so the body slice matches the terminal canvas and is invisible against it.

**Sidebar selection chrome (round 2 + round 3 stripe):** restored a themed background fill instead of the leading accent stripe + bold text. `ChromePalette` now derives `sidebarSelectionTint = foreground at 12% alpha` — a subtle adaptive overlay (slight lightening on dark themes, slight darkening on light) that does not pick up the saturated accent (the v1 round-1 derivation of accent at 28% alpha was the original failure mode). `SidebarRowBackground` paints `sidebarBackground` always and overlays `sidebarSelectionTint` when selected, in a single `ZStack`-based `.listRowBackground` to keep the suppression of SwiftUI's default selection chrome intact.

### Files changed

- `BattyKit/Sources/BattyKit/Theme/ThemeChrome.swift` — `ChromePalette(theme:)` derives `sidebarSelectionTint` from foreground at 12% alpha (was nil).
- `BattyKit/Sources/BattyKit/Views/SplitContainerView.swift` — `SplitContainerView` reads `themeChrome` itself and passes `dividerChromeColor` + `dividerBodyColor` down through `SplitNodeView` and `DraggableSplitView` as explicit init parameters. Body region of horizontal divider paints `windowBackground` instead of `divider`.
- `BattyKit/Sources/BattyKit/Views/SessionSidebarView.swift` — `SidebarRowBackground` takes `selectionTint` and `isSelected` and renders the row background as a `ZStack` of `sidebarBackground` + (optional) `selectionTint`. `SessionRow` reverted to a plain `Label`; dropped the leading accent stripe, semibold-on-selection, and accent-tinted icon.
- `BattyKit/Tests/BattyKitTests/ThemeChromeTests.swift` — renamed `sidebarSelectionTintIsAlwaysNil` to `sidebarSelectionTintIsThemedAndSubtle`; asserts the slot is now non-nil for a themed palette.

### Tests changed

- 1 existing test updated (above). `defaultPaletteIsAllNil` continues to assert `sidebarSelectionTint == nil` for the no-theme path, which is still true (the `.default` palette has nils across the board).

### Gotchas

- **`AnyView` suppresses environment propagation** in subtle ways. This bit us twice in a row (rounds 3 and 4) before we routed around it. Any future view that needs to read `themeChrome` from inside a `SplitNodeView`-wrapped subtree should take its dependencies as explicit parameters instead of `@Environment`, or the same class of bug will recur.
- **The two divider rendering bands stay**: the chrome region (top 36pt) paints `chromeBackground` (matches the pane chrome strip), the body region paints `windowBackground` (matches the terminal canvas). Both are invisible against their surroundings. The 36pt constant remains pinned to `SlidingTabBar.barHeight`.
- **For interactive verification** (cannot be exercised headlessly): in a vivid themed window (red, olive, the screenshots that motivated this round), confirm that (a) there are no vertical lines between horizontally-split panes — chrome or body, (b) the selected sidebar row has a subtle themed highlight (slight lightening for dark themes, slight darkening for light), (c) the "Default" theme path is unchanged (system chrome + system selection chrome). The build passes (`xcodebuild -scheme Batty -destination 'platform=macOS' build`) and all 268 BattyKit tests pass.

## Re-resolution (round 7, the actual fix)

**Rounds 3-6 all chased the wrong thing.** The thin vertical lines between horizontally-split panes were not the divider's color choice and not off-screen buffer compositing edges. They were a **vertical-region misalignment**: the divider's "chrome region" was painting starting at y=0 of the split container, but the pane chrome strip (tab bar) doesn't actually sit at y=0 — the unified titlebar set transparent by round 1 of #0135 overlaps the content view by ~28-48pt, pushing the tab bar down by that overlap.

Result: the divider's top 36pt was painting `chromeBackground` *in the toolbar overlap area*, while `dividerBodyColor` (foreground@18% in rounds 3-4, `windowBackground` in round 5) painted *through the actual tab bar strip* — manifesting as the thin vertical line full-height between adjacent panes.

**Fix (Cursor diagnosed it):**

- New `\.splitDetailToolbarInsetTop` env value (`BattyKit/Sources/BattyKit/Commands/FocusedValues.swift`).
- `SessionDetailView` measures `geo.safeAreaInsets.top` via a background `GeometryReader` (positioned outside the nested split `GeometryReader`s, so it observes the real detail-column inset rather than an inner zero) and publishes it through that env value.
- `DraggableSplitView` reads the env value and merges it with its own local `geo.safeAreaInsets.top` (defensive — either source can underestimate depending on configuration). A capped fallback (48pt minimum) covers the case where the reader's inset reads suspiciously small while the divider is themed.
- The horizontal divider's chrome region is now `insetTop + chromeStripHeight` (toolbar overlap + 36pt tab bar height), spanning the full chrome band across split panes. The body region paints below it as before.

### Files changed

- `BattyKit/Sources/BattyKit/Commands/FocusedValues.swift` — new `SplitDetailToolbarInsetTopKey` and `splitDetailToolbarInsetTop` env value (default `-1` meaning "not populated").
- `BattyKit/Sources/BattyKit/Views/SessionDetailView.swift` — `@State splitDetailToolbarInsetTop`, background `GeometryReader` that measures and publishes it via `.environment(\.splitDetailToolbarInsetTop, ...)`.
- `BattyKit/Sources/BattyKit/Views/SplitContainerView.swift` — `unifiedToolbarOverlapFallbackForThemedSplits` constant (48pt), `toolbarOverlapForDivider(...)` helper, `mergedToolbarOverlap(readerTopInset:)` on `DraggableSplitView`. `horizontalDivider` extended to take a `safeInsetTop` parameter and grow the chrome region to `insetTop + chromeStripHeight`.

### Gotchas

- **The lesson across rounds 3-6:** the visual symptom shape (1-2px thin lines vs. 4pt divider) was the clue I kept missing. A 4pt divider painted with the wrong color would have been visibly thicker than what the screenshots showed. The thin lines were the body color leaking through the tab-bar strip, which the chrome region of the divider was supposed to cover but wasn't because the y-origin was wrong.
- **Why GeometryReader, not onGeometryChange:** `DraggableSplitView` uses `GeometryReader` because it drives layout (`geo.size.width * ratio` → child frame widths). `onGeometryChange` is for observation, not layout. The geometry-observation usage in `SessionDetailView`'s background is intentionally a `GeometryReader { Color.clear.onAppear/.onChange }` — could be `.onGeometryChange` but is equivalent here.
- **Round 6's `.opacity` removal in `PaneView` was a wrong guess** based on a hypothesis about off-screen buffer compositing. It's a separate change from this fix and could be reverted if the unfocused-pane dimming was desired UX. Currently the only focus indicator is the accent border overlay.
- **For interactive verification:** the actual bug should be gone in red, olive, green, and any vivid themed window with multiple horizontally-split panes. The fix also handles 1-pane→split transitions and nested splits correctly because each `DraggableSplitView` reads the env value independently and merges with its own local inset.
