# 0330 — README's build instructions name a nonexistent scheme and open the project instead of the workspace

| | |
|---|---|
| **Status** | resolved |
| **Module** | Docs |
| **Platform** | macOS |
| **First seen** | 2026-08-08 |
| **Closed** | 2026-08-08 |
| **Branch** | issue/0330 |

## Description

`README.md:27` tells a new contributor to run `xcodebuild -scheme Batty -destination 'platform=macOS' build`. There is no scheme named `Batty` — the shared schemes are `Batty (Prod)` and `Batty (Beta)` (`Batty.xcodeproj/xcshareddata/xcschemes/`), and `CLAUDE.md` warns about this exact trap ("The actual scheme is `Batty (Prod)`, not `Batty`"). Verified by actually running the README's command from the repo root: it exits 65 with

```
xcodebuild: error: The project named "Batty" does not contain a scheme named "Batty".
```

So the very first command the project's front page gives a reader fails.

The next line is stale too: `README.md:28` says `open Batty.xcodeproj`, but since #0220 the guidance is to open `Batty.xcworkspace` — the workspace owns the BattyKit package, and opening the bare project leaves `BattyKitTests` missing from the Test Navigator (the workspace-sole-owner rule in `CLAUDE.md`). And the README never mentions `scripts/build.sh`, the documented canonical build wrapper, which picks the right scheme and the workspace for you.

## Steps to reproduce

1. Clone the repo, `cd` into it.
2. Run the README's build command: `xcodebuild -scheme Batty -destination 'platform=macOS' build`.

## Resolution notes

> 🟢 Resolved 2026-08-08 — the README now says `scripts/build.sh` and `open Batty.xcworkspace`. Both commands were run exactly as written and both work.

The old instructions failed outright: `xcodebuild -scheme Batty` exits 65 because no such scheme exists.

## Root cause

`README.md` was written before the project gained Prod/Beta scheme variants and before #0220 made the workspace the sole owner of the BattyKit package. Nothing checks that a README command still runs, so both instructions rotted silently — the build one into an outright failure, the Xcode one into a subtler trap where the project opens but BattyKitTests never appear in the Test Navigator.

## Fix

```diff
-xcodebuild -scheme Batty -destination 'platform=macOS' build
-open Batty.xcodeproj
+scripts/build.sh
+open Batty.xcworkspace
```

Plus one sentence explaining why each is the right form — that `scripts/build.sh` wraps `xcodebuild` with the correct scheme and workspace so it can't get either wrong, and that the workspace owns the BattyKit package so its tests appear in the Test Navigator.

**No raw `xcodebuild` alternative was kept.** A hand-typed invocation is exactly what rotted here, and it carries a second failure mode a README can't warn about concisely — the Prod/Beta `Active.xcconfig` trap. A reader who wants the underlying command has a 44-line script one `cat` away. The reviewer independently agreed with this call.

## Review

Reviewer: `claude-opus-5`, one round, approved.

It ran **the literal text of the README** rather than a plausible variant — including checking whether the bare `scripts/build.sh` form (no `./`) works for a new reader. It does: a path containing a slash is executed directly without a `PATH` lookup, and `git ls-files -s` confirms mode `100755`, so a fresh clone gets the executable bit.

It read `scripts/build.sh` to verify the new explanatory sentence rather than trusting it, confirming the script really passes `Batty (Prod)` and really uses `-workspace` — if it had used `-project`, both the sentence and the Test Navigator reasoning would have been wrong.

It also re-verified the README's shortcut lines independently rather than accepting the implementer's byte-check, and traced `Cmd-Option-1–9` separately on discovering it is **not** in `defaultBinding` at all — it's hardcoded positional dispatch in `BattyShortcuts.swift:67` and `BattyCommands.swift:118`. The README states the default correctly.

## Verification

Every command in the new block was executed as literally written:

- `scripts/build.sh` → exit 0, `** BUILD SUCCEEDED **`
- `scripts/build.sh unit` → **988 tests in 114 suites passed**, `** TEST SUCCEEDED **`
- `open Batty.xcworkspace` → exit 0, Xcode launched; `xcodebuild -workspace Batty.xcworkspace -list` resolved the package graph and listed `Batty (Prod)`, `Batty (Beta)`, `BattyKit`

Also confirmed: `MACOSX_DEPLOYMENT_TARGET = 15.6` matches the stated requirement, no `ARCHS` restriction so "Apple Silicon or Intel" holds, and the only schemes in `xcshareddata/xcschemes/` are `Batty (Prod)` and `Batty (Beta)` — so the original defect was real.

`Configuration/Active.xcconfig` on Prod throughout, tree clean, no `project.pbxproj` residue.

## Files changed

- `README.md` — build command, Xcode command, and one explanatory sentence.
- `issues/0330.md` — status flip, resolution sections, work log.

## Gotchas

- **`README.md` is the one shortcut-bearing document with no test coverage.** `ShortcutDocsSyncTests` (#0329) pins the Help catalog and `docs/shortcuts.md` to `ShortcutAction.defaultBinding`, but never reads the README — which is precisely why its line 12 drifted far enough that #0328 had to catch it by hand. Extending that test to the README would close the last unguarded surface.
- **`Cmd-Option-1–9` is not in `ShortcutAction.defaultBinding`.** The positional pair is hardcoded dispatch, so anyone verifying README or doc shortcut claims against the catalog alone will not find it and may wrongly conclude it's stale.
- **Line 17's "fully customizable in Settings" is marginally imprecise** — `Cmd-1..9`, `Cmd-Option-1..9`, `Cmd-C` and `Cmd-V` are hardcoded by design. Left as README-level copy; "most actions are remappable" would be the exact version.
- **The Building section still doesn't say how to run tests.** `scripts/build.sh unit` would help a contributor; out of scope here.

## Expected behavior

The Building section gives commands that work: `scripts/build.sh` as the primary path (it wraps `xcodebuild` with the `Batty (Prod)` scheme and the workspace), and `open Batty.xcworkspace` for Xcode work.

## Actual behavior

`xcodebuild` fails with "does not contain a scheme named 'Batty'" (exit 65), and the `open` line steers readers to the `.xcodeproj`, which hides `BattyKitTests` from the Test Navigator.

## Notes

- **A near-miss that does not rescue the command:** a lowercase `batty` scheme does exist — it's the BattyKit package's CLI executable product (`BattyKit/Package.swift:20-21`) and shows up in a workspace-level `xcodebuild -list`. But the README's command (no `-workspace`/`-project`) resolves the bare `Batty.xcodeproj`, whose only schemes are `Batty (Prod)` and `Batty (Beta)`, so it fails regardless.
- **Everything else in the README checked out.** Requirements are current: "macOS 15.6 or later" matches `MACOSX_DEPLOYMENT_TARGET = 15.6` in `Configuration/Build.xcconfig`; no architecture restriction is set, so "Apple Silicon or Intel" holds. The shortcut claims at lines 12, 14, and 15 (`Cmd-Option-1–9` for tabs, `Cmd-Shift-L` layout, `Cmd-Shift-T` theme) all match `ShortcutAction.defaultBinding` — #0328 already corrected this file. No missing first-run prerequisite: GhosttyKit resolves as a package dependency (`libghostty-spm`), so clone-and-build needs no extra fetch step.
- **One marginal line, flagged not asserted as a defect:** `README.md:17` says "every action has a shortcut; fully customizable in Settings." The positional `Cmd-1..9` / `Cmd-Option-1..9` pair is swappable but not freely rebindable, and `Cmd-C`/`Cmd-V` are fixed — the in-app Help's own shortcuts page opens by disclaiming "the few combos that aren't yours to remap." Marketing shorthand vs. inaccuracy is the implementer's judgment call.

## Relation

- Surfaced by #0328's review (its Round 1 caught a different stale line in this same file, `README.md:12`, hiding behind an en-dash).
- Same documentation-drift class as #0321, #0324, #0325, #0326, and #0328.
- The workspace-vs-project guidance stems from #0220.

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-08-08 | claude-sonnet-5 | 42 | 3,156 | 918,739 | 55,849 | $0.36 |
| 2026-08-08 | claude-opus-5 | 18 | 32 | 294,756 | 33,348 | $0.36 |

**Total: $0.72**
