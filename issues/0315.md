# 0315 — Add batty CLI verbs to open and close non-terminal view Panes so agents can surface relevant views

| | |
|---|---|
| **Status** | resolved |
| **Module** | Pane / App |
| **Platform** | macOS |
| **First seen** | 2026-08-07 |
| **Closed** | 2026-08-09 |
| **Branch** | issue/0315 |

## Description

The `batty` CLI can split and close terminal panes over XPC, but once non-terminal view kinds exist (#0304 Git Status, #0305 Process Status, #0313 LM Studio dashboard, #0314 system metrics/thermals) it has no way to create a pane *of a given kind* or to establish that closing one works. In the user's words:

> "The CLI tool should be able to add and remove panes with the new views for Git Status, Process Status, Thermals and others so that agentic workflows can show the user what is relevant at the time."

The motivating scenario: an **agent** working in a Terminal Session decides that something visual would help the user right now — it opens a Git Status pane before a commit, a thermals pane during a long build, an LM Studio dashboard while dispatching local inference — and closes it when no longer relevant. The pane is a *presentation channel the agent controls*, which is exactly why the create/close verbs must report real results: an agent must be able to distinguish a real mutation from one that hit a stale id.

This is **not new IPC work**. It extends an existing, working mutation-verb pattern — #0282 (`pane split`) and #0283 (`pane close`), both resolved — to typed panes. Most of what is written below is open design questions, deliberately: the verb surface is a public contract for agents and should be settled with the user before implementation. (The #0301 design-first gate applies to the *views'* visual designs, not to this CLI verb — but the verb-shape and naming decisions below are their own one-way doors.)

Module note: the sibling view issues use `Pane / App`; this issue also touches the CLI, which has no module of its own — the CLI/XPC verb issues (#0282, #0283) file it under `App`, so `Pane / App` is the closest existing combination and no new module is invented.

## What already exists (verified against the tree, 2026-08-07)

- **`batty pane split` (#0282) and `batty pane close` (#0283) are resolved, and both go over XPC, not `batty://`** — the user's pinned decision, recorded in #0257's transport amendment (2026-07-26). `pane split` replies with the **real new pane id** (`PaneSplitReply.paneID`, `BattyKit/Sources/BattyXPCCore/PaneSplitPayload.swift`); `pane close` reports a real result, with a stale id exiting `4` (`XPCExitCode.requestFailed`, `BattyKit/Sources/BattyXPCCore/XPCExitCode.swift:25`). The two-way reply is the whole reason those verbs moved to XPC. The verbs are `paneSplit` / `paneClose` in `BattyKit/Sources/BattyXPCCore/XPCVerb.swift:27,37`.
- **`batty session new` stays on `batty://`** because it must be able to launch the app when it isn't running (#0257 transport amendment).
- **Target resolution is shared and settled (#0281):** explicit `--session/--pane/--tab` flag → else `BATTY_SESSION_ID`/`BATTY_PANE_ID`/`BATTY_TAB_ID` env injected at spawn → else the app's focused element. A typed-pane verb must follow the same chain, and the standing behavior rule is binding: mutating a background target must **not** steal focus or switch the active session (only the `focus` verbs do that).
- **`PaneSplitRequest` carries only `paneID` / `direction` / `command`** (`BattyKit/Sources/BattyXPCCore/PaneSplitPayload.swift:25-28`); `command` presumes a shell. There is no kind field anywhere on the wire.
- **The topology read verbs exist over XPC (#0274)** — `batty list` / `batty session info` — but `TopologyPanePayload` / `TopologyTabPayload` (`BattyKit/Sources/BattyXPCCore/TopologyPayload.swift:48,66`) have **no kind field**: an agent reading the layout today cannot tell (a future) non-terminal pane from a terminal pane. The #0301 umbrella already records kind-aware `list` as a required delta.
- **The close cascade is terminal-shaped.** `AppStateStore.closePane(id:)` (`BattyKit/Sources/BattyKit/Runtime/AppStateStore.swift:418`) delegates to `WindowRuntime.closePane(id:refuseIfAppsLastPane:)` (`BattyKit/Sources/BattyKit/Runtime/WindowRuntime.swift:314`), which tears down bell state and calls `TerminalHostStore.releaseTerminalView(forTabID:)` per tab before removing the pane. A non-terminal pane has none of that state. (#0301 cites this cascade at `AppStateStore.swift:329`; the code has since moved to the lines given here.)

## Scope — open design questions, mostly deliberately undecided

**1. Creating a pane with a view kind — verb shape (open question).** Candidates:

- A new verb, e.g. `batty pane new --view <kind>`.
- Extending `pane split` with a `--view <kind>` flag — since a new pane in a split layout *is* a split, and `pane split` already exists, already resolves targets, and already returns the new pane id, extension may be the cheaper build. Against it: a Git Status pane is not conceptually "splitting a terminal", and #0282's grammar note pinned `pane` as the noun precisely because splitting subdivides a *region* — that argument cuts both ways here.
- Something else (e.g. a `view` noun).

Whatever the shape, it is XPC request/reply carrying the new pane id back, per the transport amendment — an agent chains on the id (`open → later close`). Extending `PaneSplitRequest` versus adding a new payload is part of the same decision; XPC payloads are a versioned wire format between the CLI and the app, so this should not collide with in-flight #0257-umbrella payload changes (e.g. the still-unfiled `tab close`). **Do not pick a shape in this file — settle it with the user before implementation.**

**2. Removing a typed pane — does `pane close` already suffice? (open question).** `batty pane close` targets by pane id, so the *verb shape* generalizes. Whether the *implementation* does depends entirely on where #0302 puts the terminal-host boundary: today's cascade assumes every pane's tabs are Terminal Sessions (see the close-cascade citation above). If #0302/#0303 give non-terminal panes their own teardown path behind a common close entry point, `pane close` may work unchanged; if not, it needs a kind-aware branch. This issue must establish which, not assume either. No new close verb is expected in any case.

**3. View-kind identifiers are a public contract (constraint, and a one-way door).** The CLI-visible names (`git-status`, `process-status`, `thermals`, `lm-studio`, …) become a stable API that agents and scripts hard-code — renaming later breaks callers silently. They should be settled alongside #0302's pane-kind model (where kind lives and what values it takes), not invented per-view as each ships. The Codable/persistence spelling (#0302 question 4), the topology-payload spelling, and the CLI spelling should be decided together, even if they are not the same string.

**4. Discoverability for agents (open question).** An agent cannot hard-code a list it does not know, and the set of kinds grows over time (#0313, #0314 arrived after #0304/#0305). Should there be a read verb — e.g. `batty pane views` — listing the available kinds, so an agent queries capability at runtime instead of failing on an unknown `--view`? This fits the existing read-verb precedent: topology reads landed over XPC in #0274. Related: the kind-aware `list` delta (#0301) is what lets an agent see which typed panes are *already open*, which question 5 depends on.

**5. Duplicate / idempotency behavior (open question).** Agents retry, and two agents can race. If an agent asks for a Git Status pane and one is already open for that repo, what happens — a second pane, success reporting the existing pane's id, or an error? Each is defensible; "focus the existing one" is *not* an option in its plain form, because the no-focus-steal rule binds mutations. The answer probably differs per kind (two Process Status panes watching different pids are sensible; two thermals panes are not) — which suggests the abstraction (#0302) may need to say whether a kind is singleton-per-scope. Decide before implementation; retry-driven pane accumulation is the failure mode.

**6. Error contract (must be specified, not discovered).** XPC can report real failures — that is why this surface exists. The agent-visible outcomes to define, against the existing exit-code vocabulary (`0`/`1`/`2`/`3`/`4`/`5`, `BattyXPCCore/XPCExitCode.swift`):

- Unknown view kind (likely CLI-side `1` if validated client-side against a queried list, or `4` from the app — decide which layer owns validation).
- Stale/unknown target pane id — `4`, matching #0282/#0283.
- Kind not available on this machine — e.g. LM Studio not installed/running/version-drifted; #0313's Gotchas require a degraded state in the *view*, but the *create verb* must still decide whether creating such a pane succeeds (showing the degraded state) or fails.
- Layout cannot accommodate another pane (if any such limit exists or is introduced).

#0283's lesson applies: distinct failures collapsing onto exit `4` are distinguished only by stderr, so the messages are part of the contract too.

## User directive (2026-08-09) — omitting the view kind means Terminal

> "It should default to a Terminal view when not specified but support each of the new views."

This settles part of open question 1 and constrains the rest:

- **The kind argument is optional, and its absence means Terminal.** Every existing `batty pane split` invocation keeps working unchanged and keeps producing a terminal pane. No caller has to be updated when typed panes ship, and no agent script breaks.
- **It argues for extending `pane split` over a new verb.** Question 1 weighed a new `pane new --view <kind>` against a `--view` flag on the existing verb. A default-to-Terminal flag is backward-compatible by construction on the existing verb; a separate verb would leave two ways to make a pane and force callers to choose. The counter-argument in question 1 stands (a Git Status pane is not conceptually "splitting a terminal"), but the compatibility win is now concrete rather than hypothetical.
- **The wire format inherits the same rule.** Whatever carries the kind — an added field on `PaneSplitRequest` or a new payload — an absent or null kind must decode to Terminal, matching #0302's design decision that the Codable `Pane`'s absent `kind` decodes to `.terminal`. The CLI default, the wire default and the model default should be the same default, established in the same place.
- **Every kind must be reachable**, not just the first one shipped. The identifier set (question 3) is a public contract settled alongside #0302, and question 4's discoverability verb matters more under this directive: an agent that can omit the flag safely will also want to ask which values are valid rather than guess.

Still open and still needing the user's answer: the verb shape itself, duplicate/idempotency behavior (question 5), and the error contract (question 6). This directive does not settle those.

## Orchestrator decisions for this implementation round (2026-08-09)

The user said "Work on #0315" without answering questions 1, 5 and 6 above. These are the orchestrator's calls, made so the work could proceed, **not the user's answers**. All three are reversible before anything ships, and questions 5 and 6 remain formally open.

1. **Verb shape (question 1): extend the existing verb** — `batty pane split [--view <kind>]`, not a new verb. The user's default-to-Terminal directive makes extension backward-compatible by construction.
2. **Duplicates (question 5): always create a new pane.** No singleton enforcement, and `PaneContentKind.isSingletonPerSession` deliberately not added. This matches `pane split`'s behaviour today. Singleton-per-kind can be layered on once real views exist and the per-kind answer is obvious — two Process Status panes watching different pids are sensible, two thermals panes are not.
3. **Unavailable kind (question 6): create the pane and let the view show a degraded state**, rather than failing the verb. Matches #0313's design, and a create verb that fails because LM Studio happens to be stopped is harder for an agent to reason about than one that always succeeds and renders its own status.

Not decided here, and still open: the rest of question 6's error contract beyond the unavailable-kind case.

## Resolution notes

> 🟢 Resolved 2026-08-09 — `batty pane split [--view <kind>]` creates a pane of a given kind; omitting `--view` produces a Terminal pane exactly as before. `pane close` needed no changes.

This is the first code in the #0301 series to touch the live pane render path — everything before it was designs and tests. **The Git Status view itself is deliberately not implemented**: its design is unapproved, so every non-terminal kind renders a provisional placeholder. The pipeline is real; the content is next.

## Root cause

Feature gap. `PaneSplitRequest` carried only `paneID`/`direction`/`command`, and `command` presumes a shell. There was no kind anywhere — not on the wire, not in the model, not in the view. A split-tree leaf *was* a `PaneRuntime` and every Tab *was* a Terminal Session.

## Fix

- **`PaneContentKind`** in `BattyXPCCore` — deliberately there rather than `BattyKit`'s Model layer, because the `batty` executable links only `BattyCLICore`/`BattyXPCCore`, so the kind had to be the CLI's own vocabulary rather than a translated copy. Identifiers: `terminal`, `git-status`, `process-status`, `lm-studio-dashboard`, `system-metrics`, reconciled against all four design docs.
- **Model** — `kind` on `PaneRuntime` and the Codable `Pane`, with a custom `init(from:)` so an absent key decodes `.terminal` while an unknown value still throws.
- **View** — a single exhaustive `switch pane.kind` at the top of `PaneView.body`, no `default:`, so a future kind is a compile error. The old body was extracted byte-for-byte into `terminalBody`; pane-level chrome stays outside the switch. A non-terminal pane never mounts a `TerminalPlaceholderView`, whose `body` spawns a PTY as a rendering side effect. The drag handle and eye button were relocated into the non-terminal arm's header, since the tab bar that carried them is now terminal-only.
- **Wire and CLI** — `PaneSplitRequest.kind` optional, `nil` meaning terminal, so the JSON key is genuinely absent for old clients. `--view <kind>` validated client-side against `allCases`, rejecting an unknown kind at exit `1` before any XPC round trip. `--command` with a non-terminal `--view` is rejected the same way.

Per the user's directive, **omitting the kind means Terminal at all three layers** — CLI flag, wire field, and Codable decode.

## The scope decision, and what it cost

`docs/pane-kinds.md` §1 designs a non-terminal pane as `tabs: []` with `activeTabID: UUID?`, and calls that migration "real, mechanical churn across a double-digit number of call sites." This issue **declined it**: a non-terminal `PaneRuntime` keeps one structural `TabRuntime` that no view ever renders. Review verified end to end that this spawns no PTY — `ghostty_surface_new` is reachable only through `TerminalPlaceholderView.body`, which exists only in the `.terminal` arm — and as a direct consequence `pane close` needed zero changes.

But the phantom tab was **not** invisible. Review enumerated ten consumers that counted or listed it, and the fix kind-gated each: the quit-confirmation count, `batty status`'s `tabCount`, `batty list --tabs`, Open Quickly, the sidebar row (which rendered `"2/1 — Tab"`), the drag preview, and theme/settings reconfiguration. Two testable helpers were extracted along the way so the gating could be covered.

## Review

Reviewer: `claude-opus-5`, three rounds. It confirmed the byte-for-byte `terminalBody` extraction by direct diff, traced the no-PTY claim end to end, and mutation-tested both the absent-key decode and the kind gate.

The recurring finding was the same defect three times: **`ShortcutAction` is dispatched by three independently hand-copied switches**, and each round found another one ungated. Round 1 flagged Cmd-T on a focused non-terminal pane silently adding an invisible tab. Round 2 found the implementer had gated `BattyCommands` and `BattyShortcuts` — correctly noting the NSEvent monitor bypasses SwiftUI's `.disabled` — but missed `CommandPaletteView.dispatch`, the *most* reachable path, since `SplitTree.splitPane` leaves the new pane focused after this issue's own headline command.

Round 3's fix was structural rather than another patch: a single `SessionRuntime.focusedTerminalPane` accessor that all three paths route through, plus internal gating on the shared choke points `WindowRuntime.closeFocusedTab()`/`.requestCloseFocusedTab()` and `ExitDispatcher.focusedTerminalTab`. Round 1's hand-rolled per-path checks were removed in favour of it, and review verified each removal was logically equivalent rather than an accidental un-gating.

Round 1 also caught a `TerminalHostStore.placements` leak — `hidePane`'s per-tab `setPlacement` loop wrote an entry that `releaseTerminalView`'s guard returned before removing, growing one dead entry per open/hide/close cycle for the app's lifetime.

## Verification

`scripts/build.sh` — BUILD SUCCEEDED. `scripts/build.sh unit` — **1025 tests in 115 suites passed**, `** TEST SUCCEEDED **`, up from a 988/114 baseline. `Configuration/Active.xcconfig` confirmed `#include "Prod.xcconfig"` immediately before and after every quoted run — review caught it flipped to Beta mid-work once, the trap `CLAUDE.md` documents.

**Mutation-tested twice.** Breaking the absent-key decode fails `paneDecodesAnAbsentKindKeyAsTerminal`; making `focusedTerminalPane` return unconditionally fails 5 of the 7 gate tests. Both restored exactly with a clean tree.

**Not verified, and owed by this change:** `docs/terminal-pane-requirements.md` §6's manual checklist — pointer input, keyboard, file drop, text drop, IME — on a **terminal** pane, and again in a **mixed-kind** session. `docs/pane-kinds.md` §6 binds it for any issue implementing the kind switch, and this diff wraps the terminal path in a new `_ConditionalContent`. #0143 is the recorded case of what breaks when it is skipped: file drops and clicks both regressed. It cannot be run headlessly. **This is why the issue is `resolved` and not `closed`.**

## Demo

With the app running:

```
batty pane split --view git-status     # prints the new pane id; a non-terminal pane appears
batty pane close --pane <that-id>      # removes it; the sibling absorbs the space
batty pane split                       # unchanged — still a terminal pane
```

The new pane shows a **provisional placeholder**, not the designed Git Status view.

## Files changed

`PaneContentKind.swift` (new), `PaneSplitPayload.swift`, `LayoutModel.swift`, `PaneRuntime.swift`, `SessionRuntime.swift`, `SplitTree.swift`, `AppStateStore.swift`, `WindowRuntime.swift`, `AppXPCService.swift`, `PaneView.swift`, `BattyCommands.swift`, `BattyShortcuts.swift`, `CommandPaletteView.swift`, `ExitDispatcher.swift`, `QuitConfirmation.swift`, `OpenQuicklyView.swift`, `SessionSidebarView.swift`, `TopologyPayloadBuilder.swift`, `ThemePreference.swift`, `SettingsPreferences.swift`, `AppServiceClient.swift`, `PaneCommand.swift`, plus tests, `Concepts.md`, `docs/pane-kinds.md` and `docs/batty-cli-install.md`.

## Gotchas

- **`ShortcutAction` has three independent dispatch switches** — `BattyCommands`, `BattyShortcuts`' NSEvent monitor (which bypasses SwiftUI's `.disabled` entirely), and `CommandPaletteView.dispatch`. A new action or a new guard must be applied in all three. Three review rounds found three violations of exactly this.
- **A non-terminal pane still holds one structural `TabRuntime`.** It renders nothing and spawns no PTY, but anything that counts or lists tabs must gate on `kind == .terminal`. Ten consumers needed it; **#0332** tracks making that invariant structural rather than remembered.
- **`TopologyPanePayload.activeTabID` still names the unlisted structural tab** while `tabs` reports `[]`. Deliberate — the payload's schema is frozen by #0274 and pinned by an exact-key-set test. Review judged it acceptable: no mutation verb takes a tab id, `BATTY_TAB_ID` is never injected for a non-terminal pane, and `tabs: []` is itself a legible signal since zero tabs was previously impossible. A later fix should make `activeTabID` a `UUID?` with an explicit `encode(to:)` calling `container.encode` so the key stays present as `null`.
- **`PaneContentKind.isSingletonPerSession` was deliberately not added.** Question 5 is still the user's to answer.
- **The placeholder has no click-to-focus.** Fine for a placeholder; #0304 owes it.

## Dependencies

- **#0332** — carved out of this issue's review round 3: the pane-kind gate is opt-in at every call site, and three focused-pane consumers bypass it.
- **Blocked by #0302.** There is no "pane kind" to name until that abstraction exists — today a split-tree leaf *is* a `PaneRuntime` and every Tab *is* a Terminal Session (verified citations in #0302). The kind identifiers (question 3) and the close answer (question 2) both fall out of it.
- **Blocked by #0303.** A CLI-created view pane must obey the lifecycle contract — stop work when hidden, release resources when closed, **observably**. An agent that opens and closes panes repeatedly is exactly the workload that would expose a leak, and #0285 was an unbounded-memory-growth umbrella; this verb multiplies whatever discipline #0303 does or does not enforce.
- **Does not require all four views.** This is implementable against the first view kind that ships and extends as others land — it is gated on #0302 + #0303 plus *one* of #0304/#0305/#0313/#0314, not on all of them collectively.

## Relation

- **Child of #0301** (umbrella: expand Batty beyond terminal-only Panes) — this issue implements the umbrella's "kind-aware create" and settles its "kind-aware close" question; the kind-aware `list` delta recorded there is adjacent (question 4).
- **Blocked by #0302** (pane-kind abstraction) and **#0303** (non-terminal lifecycle contract).
- **Serves #0304 / #0305 / #0313 / #0314** — the concrete view kinds an agent would open and close; requires only the first of them to exist.
- **#0257** — the CLI verb-surface umbrella whose transport amendment (agent-facing pane mutations over XPC) and grammar this extends; cross-referenced from its child index.
- **#0265** — the hybrid `batty://` + XPC transport umbrella the XPC machinery rides on.
- **#0282 / #0283** — the resolved mutation verbs whose pattern (payloads, dispatch, target resolution, store assertions, exit codes) this reuses.
- **#0281** — the shared target-resolution chain every context-sensitive verb follows.
- **#0274** — the XPC read-verb precedent for a `pane views` capability query, and the owner of the topology payloads that need a kind field.

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-08-09 | claude-sonnet-5 | 10,745 | 2,775 | 16,051,974 | 282,583 | $3.97 |
| 2026-08-09 | claude-opus-5 | 2,748 | 7,068 | 6,449,495 | 173,955 | $4.50 |
| 2026-08-09 | claude-sonnet-5 | 206 | 11,911 | 34,907,795 | 402,528 | $8.11 |
| 2026-08-09 | claude-opus-5 | 32 | 190 | 1,133,024 | 110,342 | $1.26 |
| 2026-08-09 | claude-sonnet-5 | 140 | 10,700 | 30,219,615 | 469,242 | $7.32 |
| 2026-08-09 | claude-opus-5 | 54 | 1,511 | 2,514,056 | 122,649 | $2.06 |

**Total: $27.22**
