# 0259 — Sidebar pane rows: replace the path label with a column/row position prefix

| | |
|---|---|
| **Status** | resolved |
| **Module** | Sidebar / Pane |
| **Platform** | macOS |
| **First seen** | 2026-07-08 |
| **Closed** | 2026-07-08 |
| **Branch** | issue/0259 |

## Description

The pane rows that appear below the session name in the sidebar currently show the pane's path (after #0258 routed the label through `TabTitleFormatter.chipTitle`). The user doesn't like this: the path gives **no way to identify where the pane is** in the split layout. Two problems, in the user's words:

1. "It is currently the path but has no way to identify where the pane is."
2. "The path … also does not change when the user changes directory anyway" — the label is stale: `cd`-ing in the terminal does not update the sidebar row.

## Resolution notes

> 🟢 Resolved 2026-07-08 — sidebar pane rows now show a `column/row` position (e.g. `1/1`, `2/1`, `1/3`) derived from the split tree, replacing the stale path label; renamed tabs append `— <override>`.

Positions are computed from `SplitTree` shape via span reservation, so a pane's coordinates match where it renders and update automatically on split/close/swap. Visual sign-off is still the user's — see Gotchas.

## Root cause

After #0258, the sidebar pane row routed its label through `TabTitleFormatter.chipTitle`, showing the pane's path. The path neither located the pane in the split layout nor updated on `cd`, so it was both uninformative and stale.

## Fix

Added position derivation to `SplitTree`: each subtree reserves its full `gridSpan` (columns × rows) in the enclosing grid, and a leaf's `column/row` is the top-left cell of its 1×1 rectangle. Horizontal splits advance the column axis by the left child's column span; vertical splits advance the row axis by the upper child's row span. Because sibling rectangles are disjoint along the split axis, every leaf lands on a unique cell for any tree shape (including a narrow leaf beside a wide nested subtree). `SessionSidebarView` reads `session.tree.panePositions[pane.id]?.label` as a pure computed value and appends `— <override>` when the active tab has a `titleOverride`. `ForEach` now iterates `session.tree.allPanes`.

## Review

Reviewed by claude-opus-4-8 over two rounds. Round 1 requested changes: the initial slot-anchoring produced position collisions in nested grids, an invariant test asserted a false global property over a single layout, and mixed-nesting coverage was missing. Round 2 (fix commit `2a2258a`) replaced slot anchoring with span reservation; reviewer independently verified the uniqueness induction and re-ran the unit suite, then approved.

## Verification

`scripts/build.sh unit` — 450 tests / 55 suites passed (up from 448), including the 11 test functions in the new `SplitTreePanePositionTests`. `scripts/build.sh` — test build succeeded. Reviewer independently re-ran `scripts/build.sh unit` and confirmed the same. New tests pin the user-facing examples exactly (single `1/1`; horizontal `1/1`/`2/1`; vertical chain `1/1`/`1/2`/`1/3`; canonical `1/1,1/2,1/3,2/1`), plus hidden-pane slot stability, close/swap recompute, the round-1 colliding repro, a depth-6 alternating nest, and column-span / row-span mixed-nesting repros.

## Files changed

- `BattyKit/Sources/BattyKit/Model/SplitTree.swift` — added `gridSpan`-based position derivation (`panePositions`, `PanePosition` with `label`) via span reservation.
- `BattyKit/Sources/BattyKit/Views/SessionSidebarView.swift` — `PaneRow` label reads the tree position (pure computed) and appends `— <override>` for renamed tabs; `ForEach` switched to `tree.allPanes`; unused `paneIndex` removed.
- `BattyKitTests/SplitTreePanePositionTests.swift` — new suite (11 tests) covering canonical layouts, hidden-pane stability, close/swap, and nested-grid uniqueness.
- `issues/0259.md` — status, resolution sections, work-log rows.

## Gotchas

- **Visual sign-off pending (user).** Rows now read `1/1`, `2/1`, … with no path — needs confirmation in the running app. Marked `resolved`, not `closed`.
- **`— <override>` append format** (position + em-dash + custom name for renamed tabs) is the implementer's reading of Open Question 1; confirm it's the desired presentation.
- **Numbering gaps in mixed nesting are intentional** — e.g. `D=3/1` with no pane at `2/2` reflects rendered geometry (a wide left subtree genuinely occupies two columns), per the issue's Open Questions guidance to derive coordinates from where panes visually sit. Not a bug.
- **Follow-up (non-blocking):** the stale-label root cause for *tab chips* — Combine-backed `workingDirectory` isn't Observation-tracked — is tracked separately as **#0260**; the sidebar fix sidesteps it only for the sidebar rows.

## Desired behavior

Use **column and row numbers** to show where the pane is, as a **prefix that replaces the path**:

- `1/1` — the first pane.
- `2/1` — the pane split horizontally from it (second column, first row).
- `1/3` — first column, third row (two vertical splits in column 1).

So a session with one horizontal split then two vertical splits in the left column would list panes as `1/1`, `1/2`, `1/3`, `2/1`.

## Open questions (for the implementer to confirm or the user to decide)

- **Is the position the whole label, or a prefix to something?** The request says "add these numbers as a prefix and replace the path". Reading: the position replaces the path as the label. If a tab has a user-set rename (`titleOverride`), it likely still deserves display — suggest `1/2 — <custom name>` when a rename exists, position alone otherwise. Confirm with the user if unclear during review.
- **Hidden panes** (#0256) keep their slot in the split tree — they should keep their position number too (the slot doesn't collapse), so a restored pane reappears with the same coordinates.
- **Deep/mixed nesting**: column/row reads naturally for layouts that are "columns of rows" (horizontal splits at the top, vertical within). For pathological nesting (a horizontal split *inside* a vertical row), define a sensible rule — e.g. derive coordinates from the pane's actual frame (x-order → column, y-order → row) rather than tree shape.

## Technical notes

- **Position derivation:** two candidate sources —
  - `SplitTree` structure (`.split(id:direction:ratio:left:right:)` in `BattyKit/Sources/BattyKit/Model/SplitTree.swift`): walk the tree, horizontal splits advance the column, vertical splits advance the row. Simple for canonical layouts; needs the nesting rule above.
  - `SessionRuntime.paneFrames` (`PaneFrameTracker`) already tracks real pane frames — sorting frame origins (x → column, y → row) gives coordinates that match what the user *sees*, regardless of tree shape. Likely the more robust source; verify it's populated for background sessions.
- **Label site:** `PaneRow.paneLabel` in `BattyKit/Sources/BattyKit/Views/SessionSidebarView.swift` (the #0258 `chipTitle` call is what gets replaced).
- **The stale-path observation** is worth a quick root-cause look while in there: `chipTitle` reads `tab.terminal.workingDirectory` / `terminal.title`, which should update on OSC title changes — if the sidebar row wasn't re-rendering on cwd change, that staleness may also affect tab chips. Fixing the label to position-based sidesteps it for the sidebar, but note whatever is found for a follow-up if the chips are affected too.
- Positions must update when panes are split, closed, or swapped (`pane swap` / drag) — they're derived state, so compute in the view from observed model state rather than storing.
- The #0257 CLI ("Pane N" fallbacks, future `batty pane list`) would benefit from the same coordinates — keep the position formatter reusable (e.g. a small helper on `SplitTree` or near `PaneFrameTracker`), not buried in the view.

## Verification expectations

- Unit-test the position derivation: single pane → `1/1`; horizontal split → `1/1`, `2/1`; vertical splits in a column → `1/1`, `1/2`, `1/3`; positions stable when a pane is hidden; positions correct after close/swap.
- `scripts/build.sh unit` + `scripts/build.sh` green.
- Visual sign-off (user): rows read `1/1`, `2/1`, … and no longer show paths.

## Relation

- Refines the pane-row label from **#0258** (which routed it through `TabTitleFormatter.chipTitle`); replaces that label with position coordinates.
- Pane rows/tree introduced in **#0256**; hidden panes keep their slot, so they keep their position.
- Coordinates may be reusable for the **#0257** CLI (`pane focus <id>` discovery, future `pane list`).

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-07-08 | claude-sonnet-5 | 323 | 6,122 | 3,388,352 | 141,962 | $1.09 |
| 2026-07-08 | claude-opus-4-8 | 4,141 | 10,509 | 724,557 | 66,738 | $1.06 |
| 2026-07-08 | claude-sonnet-5 | 820 | 17,450 | 1,601,023 | 142,608 | $0.85 |
| 2026-07-08 | claude-opus-4-8 | 2,957 | 8,154 | 102,855 | 56,763 | $0.62 |

**Total: $3.62**
