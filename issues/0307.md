# 0307 — preflight.sh Sparkle key cross-check drops the trailing base64 pad from SU_PUBLIC_ED_KEY, so every valid key reports a false mismatch

| | |
|---|---|
| **Status** | resolved |
| **Module** | Build |
| **Platform** | macOS |
| **First seen** | 2026-08-04 |
| **Closed** | 2026-08-04 |
| **Branch** | issue/0307 |

## Description

The Sparkle EdDSA cross-check in `scripts/preflight.sh` (added by #0306) compares the private key's public half in the login keychain against `SU_PUBLIC_ED_KEY` in `Configuration/App.xcconfig` — but the `awk -F'='` extraction at lines 309-310 splits on *every* `=`, silently dropping the key's trailing base64 pad. The comparison can therefore never succeed for any valid key: the check reports `cred_fail` ("does NOT match … updates signed here would be unverifiable") even when the keys are byte-identical, blocking or misleading a release preflight with a false negative.

## Resolution notes

> 🟢 Resolved 2026-08-04 — every xcconfig read in `scripts/preflight.sh` now goes through one `xcconfig_value()` helper that splits on the first `=` only, so the Sparkle cross-check passes for a matching key instead of always failing.

The keys on this machine were never wrong; the gate was. `scripts/preflight.sh --credentials-only` now reports `0 failure(s), 0 warning(s)`.

## Root cause

`awk -F'='` splits on *every* `=` in the line. `SU_PUBLIC_ED_KEY`'s value is a base64-encoded 32-byte Ed25519 public key, which is always 44 characters ending in exactly one `=` pad, so `$2` held the key minus its pad and `$3` held the empty string after it. Printing `$2` truncated the value by one character, and `[[ "$SPARKLE_PUB" == "$SU_KEY" ]]` could never be true.

This made the `cred_pass` branch **unreachable for any valid key** — not a machine-specific or key-specific failure. The check had never passed since it was introduced in #0306. The two surrounding branches (`-z "$SPARKLE_PUB"` for a missing keychain key, `-z "$SU_KEY"` for an empty xcconfig value) were unaffected and still behaved correctly, which is why the bug read as a genuine key mismatch rather than a parser fault.

## Fix

Added an `xcconfig_value <key> <file>` helper to `scripts/preflight.sh` and routed all four xcconfig reads through it:

```zsh
xcconfig_value() {
    grep -E "^$1" "$2" 2>/dev/null | head -1 | sed -E 's/^[^=]*=[[:space:]]*//; s/[[:space:]]*$//'
}
```

`s/^[^=]*=[[:space:]]*//` consumes up to and including the *first* `=` only; the second expression trims trailing whitespace, which the old `awk` form did not — a stray trailing space in the xcconfig would have produced the same phantom mismatch.

Converted sites: `SU_PUBLIC_ED_KEY` (the bug), `SU_FEED_URL`, `DEVELOPMENT_TEAM`, and `MARKETING_VERSION`. The latter three could not contain `=` and were not broken; they were converted so a truncating variant can't reappear at the next call site, which is exactly how this bug arose. That consistency call was reviewed explicitly and judged appropriate rather than diff bloat.

Audited and deliberately left alone, with reasoning confirmed by review:

- `scripts/appcast-item.sh:39` — reads `MACOSX_DEPLOYMENT_TARGET` (`15.6`); a dotted version can't contain `=`.
- `scripts/verify-dmg.sh:118-120,160,172` — `awk -F=` over `codesign -dvvv` output. `TeamIdentifier` (10-char team id) and `Identifier` (reverse-DNS bundle id) can't contain `=`. `Authority` carries a certificate CN, which theoretically could, but `APP_AUTHORITY` and `BIN_AUTHORITY` are extracted through the identical idiom and only ever compared to each other, so any truncation is symmetric and the comparison still holds.

## Review

Reviewed by `claude-opus-5`, one round, approved first pass. The review independently reproduced the truncation against the real xcconfig (old idiom yields `…IaMZA`, new yields `…IaMZA=`), re-ran the credentials gate, and exercised the helper against multiple-`=`, trailing-whitespace, empty-value, missing-key, missing-file, and CRLF fixtures. It also corrected a factual error in the audit brief handed to the implementer: the second `awk -F'='` site was `SU_FEED_URL`, not a second `SU_PUBLIC_ED_KEY` read. There is no missed extraction — `preflight.sh:523` reuses the already-fixed `$SU_KEY` variable, and a repo-wide grep finds no other consumer.

Three non-blocking nits were raised and carved out to #0308 rather than fixed here (see `## Relation`).

## Verification

- **`scripts/preflight.sh --credentials-only`** — the Sparkle line flipped from `[✗] … does NOT match …` to `[✓] Sparkle private key present and matches SU_PUBLIC_ED_KEY`. Full run: `0 failure(s), 0 warning(s)`, "This machine can sign, notarize, and publish a Sparkle-verifiable release." Run independently by both the implementer and the reviewer.
- **Byte-level check** — extracted value `17V/QFAmgnEvb+YhA0+c/oA8TghcNVLOY5qYIa6aMZA=` (44 chars, pad intact) equals `generate_keys --account Batty -p`. The old `awk` idiom run side-by-side against the same file still yields the truncated form, confirming the diff is what closed the gap.
- **Edge cases** against scratch fixtures (outside the repo): value containing multiple `=` → preserved whole; trailing whitespace → trimmed; empty value → empty, so the `-z "$SU_KEY"` warn branch still fires; missing key and missing file → empty, script continues (`preflight.sh` runs `set -uo pipefail` without `-e`, so `grep`'s non-zero exit inside `$( )` can't abort it).
- **`zsh -n`** — clean on `preflight.sh`, `appcast-item.sh`, `verify-dmg.sh`.
- **`scripts/build.sh unit`** — 866 tests in 101 suites passed. A sanity gate only; it exercises no part of this diff.
- `scripts/release.sh` was **not** run — it submits to Apple for notarization.

## Files changed

- `scripts/preflight.sh` — added the `xcconfig_value()` helper with a comment explaining why plain `awk -F'='` is wrong here; replaced four `awk -F'='`/`awk -F=` xcconfig extractions with calls to it.

## Gotchas

- The failure message printed both values, and they differed only by the final `=` — easy to read as two different keys rather than one truncated key. When a base64 comparison fails by exactly one trailing character, suspect the parser before the key.
- `grep -E "^$1"` is a prefix match, so a hypothetical `SU_PUBLIC_ED_KEY_OLD` ordered ahead of `SU_PUBLIC_ED_KEY` would win via `head -1`. No such collision exists today and the behavior is unchanged from the pre-fix code. Tracked in #0308.
- `preflight.sh` reads `App.xcconfig` raw and does not follow its `#include? "Active.xcconfig"`. `Beta.xcconfig` sets `SU_PUBLIC_ED_KEY =` (empty), so a Beta-active tree would still read the Prod key here. Unreachable in practice — the product-variant gate already requires Prod — but worth knowing if that gate is ever relaxed.

## Relation

Follow-on: [#0308](0308.md) — hardening nits carved out of this fix (prefix-match key lookup, malformed-line handling).

## Plan

Replace the `awk -F'='` extraction with one that splits on the first `=` only and strips surrounding whitespace, e.g.:

```bash
SU_KEY=$(grep -E '^SU_PUBLIC_ED_KEY' "$APP_XCCONFIG" 2>/dev/null \
    | head -1 | sed -E 's/^[^=]*=[[:space:]]*//; s/[[:space:]]*$//')
```

(guidance, not a contract). Also sweep `scripts/preflight.sh` and the sibling release scripts for any other `awk -F'='` value extraction that would truncate base64 or `=`-containing values — note findings, but keep this issue scoped to the Sparkle key check. `scripts/` is a restricted area per `CLAUDE.md`; this issue is the explicit authorization to fix this check.

## Steps to reproduce

1. Have a valid Sparkle EdDSA private key in the login keychain under account `Batty`, with its matching public key in `Configuration/App.xcconfig` as `SU_PUBLIC_ED_KEY` (the actual state of this machine, verified 2026-08-04).
2. Run the preflight credentials check (`scripts/preflight.sh`, credentials section).
3. Observe the Sparkle cross-check fail.

## Expected behavior

When the keychain key's public half matches `SU_PUBLIC_ED_KEY`, the check takes the `cred_pass` branch at line 320-321 ("Sparkle private key present and matches SU_PUBLIC_ED_KEY").

## Actual behavior

The check always falls through to the `cred_fail` at line 323:

```
[✗] Sparkle private key's public half (17V/QFAmgnEvb+YhA0+c/oA8TghcNVLOY5qYIa6aMZA=) does NOT match SU_PUBLIC_ED_KEY in App.xcconfig (17V/QFAmgnEvb+YhA0+c/oA8TghcNVLOY5qYIa6aMZA) — updates signed here would be unverifiable by installed clients
```

The two values are byte-identical except for the missing trailing `=` on the second — the tell that the xcconfig side was truncated during extraction, not actually different.

## Notes

Mechanism, verified 2026-08-04 against `scripts/preflight.sh` lines 309-310:

```bash
SU_KEY=$(grep -E '^SU_PUBLIC_ED_KEY' "$APP_XCCONFIG" 2>/dev/null \
    | head -1 | awk -F'=' '{sub(/^[ \t]+/, "", $2); print $2}')
```

`awk -F'='` splits the xcconfig line

```
SU_PUBLIC_ED_KEY = 17V/QFAmgnEvb+YhA0+c/oA8TghcNVLOY5qYIa6aMZA=
```

on every `=`, so `$1` is the field name, `$2` is the key value *without* its trailing pad, and `$3` is the empty string after the pad. Printing `$2` drops the final `=`, and the comparison at line 322 (`[[ "$SPARKLE_PUB" == "$SU_KEY" ]]`) can never be true.

- **Not key-specific, not machine-specific.** An Ed25519 public key is 32 bytes, which base64-encodes to exactly 44 characters ending in exactly one `=` pad — always. This check has never passed and cannot pass for any valid key.
- The other branches still behave correctly: `-z "$SU_KEY"` (empty xcconfig value, line 318-319) and `-z "$SPARKLE_PUB"` (missing keychain key, line 316-317). Only the match branch is unreachable.
- The keys on this machine actually do match — this is purely a parsing bug in the preflight gate.
- The suggested `sed` extraction also strips trailing whitespace, which the awk version did not — a stray trailing space in the xcconfig would have caused the same phantom mismatch.
- Related: #0306 (the issue that introduced this cross-check; documents the Sparkle key living in the login keychain under account `Batty`), `scripts/RELEASE-CREDENTIALS.md:114` (references the same cross-check).

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-04 | file | claude-fable-5 | 10 | 2,987 | 168,699 | 50,615 | $0.95 |
| 2026-08-04 | implement | claude-sonnet-5 | 42 | 10,655 | 1,383,593 | 79,458 | $0.58 |
| 2026-08-04 | review | claude-opus-5 | 18 | 8,895 | 370,988 | 60,851 | $0.79 |

**Total: $2.32**
