# 0127 — Pane swap by drag-and-drop with animation

| | |
|---|---|
| **Status** | closed |
| **Module** | Pane / Split |
| **Platform** | macOS |
| **First seen** | 2026-05-14 |
| **Closed** | 2026-05-18 |
| **Commit** | af25405 |

## Description

Swap two panes' positions in the split tree by dragging one over the other. Pick up a pane from its title strip (or a dedicated drag handle), drag it over another pane in the same window, drop — the two panes exchange positions and the layout animates into place. Mirrors tmux's `prefix-{` / `prefix-}` swap, but with direct manipulation instead of a keybinding.

The implementation can borrow from the existing tab-reorder behavior in SlidingTabs, which already handles a similar pattern — chip drag, drop-target highlight, animated reposition. Translating that to panes-in-a-split-tree is the work.

## Acceptance criteria

- A pane's title strip (or another deliberate handle — not the terminal body) initiates a drag when long-pressed or dragged with enough threshold to distinguish from a normal click.
- During the drag, the dragged pane's chrome follows the cursor as a translucent preview.
- Every other pane in the same window highlights as a drop target when the cursor enters its bounds.
- Dropping over a target pane swaps the two panes in the underlying `SplitNode` tree. The split structure (which splits are horizontal / vertical, the ratios) is preserved — only the leaf contents (terminal sessions) trade places.
- The post-drop transition animates smoothly — both panes' frames interpolate to their new positions, not a hard cut. Reuse SwiftUI's `.animation(.spring, value: …)` against the layout model so the renderer's existing layout pass handles the tweening.
- The dragged pane's keyboard focus follows the swap (the originally-dragged pane stays focused, now in its new position).
- Drop on the originally-dragged pane (no-op location) cancels cleanly with a snap-back animation; no state change.
- Drop outside any pane cancels cleanly.

## Out of scope

- Dragging a pane *into a split position* (e.g. drop on the right half of a target pane to convert it into a new split with the dragged pane on the right). That's "rearrange the tree by drag," a strictly larger feature. Pure swap is a clean v1; the split-creation variant can land as a follow-up.
- Dragging a pane out of the window to detach it. Multi-window support is gated on `#0027`; until that lands, drag-to-detach has nowhere to go.
- Dragging a tab between panes. That's a different shape and tracked separately if needed.
- Cross-session pane drag. Pane swap stays within the current session for v1.

## Notes

- The existing tab-chip drag in SlidingTabs is the prior art — read it first. The pane drag will sit one level up in the view hierarchy (`PaneView` rather than tab chip) but the gesture, the translucent preview, and the drop-target highlight should feel consistent.
- The split-tree mutation is a swap on two `SplitNode.leaf(...)` cases (or wherever leaf panes live) — find the two paths to the swapped leaves, exchange them. Make sure focus state, bell counters, and any per-pane runtime state (`PaneRuntime`) travel with the pane, not with the tree position.
- Animation: SwiftUI's implicit layout animation on a `.spring(response: 0.32, dampingFraction: 0.78)` (roughly tmux-fast, not iOS-bouncy) tends to feel right for terminal-app UI. Worth a quick A/B with the existing split-divider drag for consistency.
- Cursor: standard "grabbing hand" during the drag, "no" cursor over invalid drop zones (everywhere except inside the window's other panes).
- File-drop UX (`#0022`) lives on the terminal *body*; pane-drag UX should live on the title strip / handle, so the two don't conflict. Verify by reproducing: drag a file from Finder onto a pane vs. drag the pane handle of one pane onto another.

## Root cause

No pane-swap mechanism existed.

## Fix

- Added `SplitTree.swapPanes(id:with:)` which finds both leaf panes via `findPane`, then rebuilds the tree with them exchanged using the new `SplitTreeNode.swapping(paneA:paneB:in:)` recursive helper. The split structure (direction, ratio) is unchanged; only the leaf `PaneRuntime` references trade positions.
- `PaneView` has `.onDrag` on the `SlidingTabBar` that provides the pane's `UUID` as plain-text via `NSItemProvider`. The `SlidingTabBar` is the drag handle — the terminal body is not a drag source.
- `PaneView` uses a `PaneSwapDropTarget` `ViewModifier` (extracted to keep the main body under the SwiftUI type-checker complexity limit) that adds `.onDrop` on the terminal body. On drop, it loads the UUID string via `loadDataRepresentation`, and if it differs from the current pane, calls `tree.swapPanes(id:with:)` wrapped in `withAnimation(.spring(response: 0.32, dampingFraction: 0.78))`.
- The swap highlight is a 3pt `Color.accentColor` stroke overlay driven by `isPaneSwapTarget` (the `.onDrop` `isTargeted` binding), distinct from the file-drop and focus overlays.

## Files changed

- `BattyKit/Sources/BattyKit/Model/SplitTree.swift` — `swapPanes(id:with:)` + `SplitTreeNode.swapping(paneA:paneB:in:)` helper
- `BattyKit/Sources/BattyKit/Views/PaneView.swift` — `.onDrag` on `SlidingTabBar`; `PaneSwapDropTarget` modifier; `isPaneSwapTarget` state; `import UniformTypeIdentifiers`

## Gotchas

- **Terminal NSViews don't animate during the swap** — the SwiftUI chrome (tab bar, focus border) animates smoothly via the spring, but the underlying `AppTerminalView` positions are updated at the final frame via `TerminalHostStore.updatePlacements`. Smooth NSView animation would require driving the host frame update at every animation tick, which needs `CADisplayLink` or a timer loop. Deferred.
- **Self-drop is a no-op** — dragging a tab bar onto its own pane's body triggers `isTargeted` (shows the highlight) but the UUID check prevents any state change. The macOS DnD system sees a successful drop, so no "no-drop" cursor appears.
- File-drop and pane-swap don't conflict: file-drop listens on the terminal NSView (AppKit level) while pane-swap `.onDrop` sits on the `TerminalPlaceholderView` layer (SwiftUI). The UTTypes differ (file URLs vs plain text), so macOS routes them to the right handler.

## Re-opened

**Status:** the v1 implementation landed but the drag never initiates in practice.

**Diagnosis:** `.onDrag` is attached to the whole `SlidingTabBar` container (`PaneView.swift:83`). `SlidingTabBar` (sibling SPM package) owns its own drag/tap gestures for tab reorder and tab selection. Child gestures win in SwiftUI's priority order, so the outer `.onDrag` never fires — there is no naked tab-bar surface to grab from. The empty gap between the last chip and the `+` button is the only theoretically draggable area and disappears once tabs fill the strip.

**Fix direction** (decided 2026-05-18):

- Add a dedicated **drag handle** on the **far-right** edge of the tab bar (trailing edge, opposite end from the `+` button if the `+` is on the left, or beyond the `+` if it's on the right — whichever doesn't fight the existing chrome layout). The handle is the only drag source; the rest of the tab bar keeps its current click/reorder behavior.
- Use an SF Symbol that reads as "grab to move" — candidates: `line.3.horizontal`, `rectangle.grid.1x2`, `arrow.up.and.down.and.arrow.left.and.right`, or a dot-grid like `square.grid.2x2`. Pick whichever feels closest to the rest of Batty's chrome icons; the handle should be subtle (secondary tint) but discoverable on hover.
- The handle is gated on `hasSiblingPanes` (only shown when there's somewhere to swap to). Single-pane sessions don't display it.
- Cursor on hover over the handle: standard `NSCursor.openHand`; during drag, `closedHand`.
- **Drag preview should visually represent the whole pane**, not just the grip icon, so users see what they're moving. Use `.onDrag(...) { preview }` with a snapshot/styled placeholder of the pane bounds (terminal NSView contents won't snapshot via SwiftUI — a translucent rectangle at pane size with the tab title and a faint border is fine).

**Acceptance criteria for the fix:**

- The handle is visible on every pane in a multi-pane session, on the far-right side of the tab bar.
- Dragging starts reliably from the handle on the first try (no flaky gesture races).
- The drag preview shows a pane-sized representation, not just the grip icon.
- All existing pane-swap behavior (swap mechanics, animation, focus retention, self-drop no-op, file-drop coexistence) continues to work.
- Tab reorder and tab click still work from the chip area — the handle does not regress any existing tab-bar interaction.

The grip-handle approach has prior art on `origin/claude-session-backup` (a parallel implementation in `PaneView.swift` used a leading-edge grip with the same `.onDrag` pattern) — useful reference for one way to wire it, but the final design should use the **far-right** placement per the decision above.

## Re-resolution

Added a dedicated drag handle on the far-right of the tab bar inside `PaneView`. The handle is the only `.onDrag` source; the `.onDrag` previously attached to the `SlidingTabBar` container has been removed so the bar's internal tap/reorder gestures no longer collide with pane-swap. The grip uses `rectangle.grid.1x2` (an SF Symbol that mirrors Batty's pane/split visual metaphor and matches the existing chrome tone — 11pt semibold, secondary tint, 22x22 frame, same dimensions as the `+` button next door). It's gated on `hasSiblingPanes` so single-pane sessions don't show it. Hover sets `NSCursor.openHand`; the system auto-swaps to `closedHand` during the active drag. The drag preview is now a pane-sized translucent rectangle (clamped between 160×96 and 360×240) showing the active tab's title and a faint accent border, rendered via `.onDrag(_:preview:)`. Swap mechanics, the `PaneSwapDropTarget` highlight, and the spring-animated drop are unchanged from v1.

### Files changed

- `BattyKit/Sources/BattyKit/Views/PaneView.swift` — wrapped `SlidingTabBar` in an `HStack`; added `paneDragHandle` and `paneDragPreview` view builders; tracked `paneHeight` alongside `paneWidth` for preview sizing; removed the broken container-level `.onDrag`.

### Gotchas

- **SF Symbol pick:** `rectangle.grid.1x2` reads as "stacked panes" — the same metaphor the rest of Batty uses for split/pane chrome, and visually consistent with the `+`-button weight (11pt semibold). The other candidates (`line.3.horizontal`, `square.grid.2x2`, `arrow.up.and.down.and.arrow.left.and.right`) either looked like a menu/hamburger or were too busy for a 22×22 frame.
- **Cursor:** confirmed by inspection only — macOS swaps `openHand` → `closedHand` automatically once the system DnD starts. If users report the cursor stays open during the drag, fall through to a manual push on the `.onDrag` start.
- **Preview size:** the terminal NSView (libghostty `AppTerminalView`) can't be snapshotted through SwiftUI, so the preview is a styled placeholder rather than a true thumbnail. The clamps keep it readable on tiny panes without dominating the cursor on huge ones.
- **Interactive-only verification:** this is a UI fix. What can't be verified headlessly:
  - The grip appears at the far-right of the tab bar on every multi-pane session.
  - Dragging from the grip starts the drag reliably on the first try (no gesture flake).
  - The drag preview reads as a pane-sized representation of what's being moved.
  - The cursor changes to `openHand` on hover and to `closedHand` during the drag.
  - Tab clicks and tab reorder inside the bar still work cleanly (now that the colliding outer `.onDrag` is gone they should be unobstructed, but UI sign-off is the user's).
  - File-drop on the terminal body (`#0022`) still routes to the file handler — the grip's drag carries plain text (UUID), which UTType routing keeps separate from file URLs, but worth a manual smoke test.
