# 0293 — No way to release a surface's GPU resources without destroying the surface and its PTY

| | |
|---|---|
| **Status** | resolved |
| **Module** | GhosttyKit |
| **Platform** | macOS |
| **First seen** | 2026-07-31 |
| **Closed** | 2026-08-01 |
| **Branch** | issue/0292 |

## Description

libghostty offers no API between "pause rendering" and "destroy everything". The #0285 source investigation dumped every `_ghostty_surface_*` export in the vendored binary: the list contains `set_occlusion` and `free`, and **nothing in between**; `.SwapChain.deinit` appears only alongside renderer teardown. So an embedder with 100+ mostly-invisible surfaces has no way to bound its graphics memory — occlusion (see #0288) stops redraw-driven growth but does not free the swap chain, leaving roughly **48 MB per hidden Terminal Session unreclaimable from Batty's side** without killing the PTY. This is the genuinely upstream defect in the #0285 umbrella, and the ceiling on everything else: even with #0287 and #0288 done, steady-state footprint still scales with total open Terminal Sessions rather than visible ones.

**The upstream asks, cheapest first** (to be filed against ghostty and/or libghostty-spm — github.com/Lakr233/libghostty-spm; this Batty issue tracks them):

1. **Drop `CAMetalLayer.maximumDrawableCount` from 3 to 2 for non-visible surfaces.** The live instrumentation confirmed the default of 3 is in effect by measurement: 93 `'Batty'` IOSurfaces / 31 layers = exactly 3.00, with exactly 1/3 held by WindowServer as front buffers, stable across samples. Dropping to 2 for occluded surfaces would cut drawable memory by a third — ~125 MB of the 377 MB on the measured 26-session instance — and is a far smaller upstream change than freeing the whole swap chain.
2. **Free the swap chain when a surface is occluded** (reallocating on re-display), or add an explicit release/reacquire API for a surface's GPU resources — the full fix, and the larger change.

**Corroborating report from another libghostty-based multiplexer:** github.com/manaflow-ai/cmux issue #1435 — 67 panes, 8.4 GB footprint, ~90% GPU memory, IOSurface 5 GB / IOAccelerator 1.3 GB / owned-graphics 1.3 GB. Nearly identical proportions to Batty's measurements; still open. Caveat carried over from the investigation: cmux almost certainly makes the same app-per-pane integration choice (the path of least resistance through this wrapper's API), so the shared signature is not by itself proof the defect is in the renderer — but the *absence of any GPU-release API* is upstream fact regardless, confirmed by the symbol dump.

## Resolution notes

> 🟡 Resolved 2026-08-01 — a reviewed, submission-ready upstream write-up is in the repo. **Nothing has been filed.**

**Read this before closing.** The deliverable here is a draft, not a filing. No issue, PR, fork, push, or comment was made against any upstream repository — that decision was explicitly reserved for the user. This issue should not move to `closed` until either the ask is filed upstream (link it here) or a decision is recorded not to file it.

## Root cause

There is no way to release a surface's GPU resources without destroying the surface and its PTY. The public surface lifecycle block (`ghostty.h:1117-1187`) contains `ghostty_surface_set_occlusion` (`:1134`) and `ghostty_surface_free` (`:1121`) with nothing between them, and a whole-header search for release/suspend/drawable/swap/reacquire/gpu returns only unrelated enum members. An embedder with 100+ mostly-invisible surfaces has no way to bound graphics memory: roughly 35 MB per layer stays resident until the Tab closes.

## Fix

`issues/0293/upstream-issue.md` — two asks, cheapest first: reduce the drawable count for occluded surfaces, and free/reacquire the swap chain on occlusion. Includes the measured 3:1 drawable-to-layer ratio, the per-instance figures, and the corroborating public report at github.com/manaflow-ai/cmux issue #1435.

## Review

Reviewed by claude-opus-5 over two rounds, then one edit pass applying the reviewer's own recommendations.

Round 1 blocked on the same stale-header problem as #0292, plus three specific to this draft: a printed grep command whose stated hit count (two) did not match what it returns (four, all comments — so the "zero API usages" conclusion held); Ask 1 dropping the "as best we can tell" hedge and asserting facts about Zig internals the draft elsewhere says it cannot see; and the repo-boundary question going unaddressed, when libghostty-spm consumes ghostty as a prebuilt `.binaryTarget` and the maintainer's first reply would reasonably be "file at ghostty-org/ghostty."

Round 2 approved, and found the repo-boundary answer is **stronger than the draft claimed**: `Patches/ghostty/0005-ios-metal-rendering.sh` patches `src/renderer/metal/IOSurfaceLayer.zig` — literally the swap-chain layer both asks target — so renderer-level changes demonstrably land through this repo's build.

Two reviewer recommendations were applied before landing. The draft asserted that nothing an embedder does can reduce a surface's GPU footprint short of `free` — **falsifiable from the draft's own evidence**, since it establishes that a drawable is `width × height × 4` and that resize swaps the pool 1:1; a maintainer could shrink a hidden surface with `set_size`. And it stated per-surface glyph-atlas ownership as fact where #0292 concedes that per-`App` versus per-`surface` cannot be established from outside, because in the measured design app count and surface count move together.

## Verification

- All header and Swift citations re-verified against the pinned checkout at `b146b73`, independently by the reviewer.
- The printed grep re-run: four hits, all comments.
- **cmux #1435 fetched live and confirmed exact**: 67 panes, 8,432 MB, IOSurface 4,994 MB / IOAccelerator 1,341 MB / owned-graphics 1,286 MB, 59+16+15 = 90%, still open, explicitly names libghostty.
- `scripts/build.sh unit` — 783 tests in 92 suites (markdown-only diff).
- Self-containment and tone verified; no Batty references.

## Gotchas

- **The cmux corroboration is consistent with an upstream defect and equally consistent with both hosts making the same integration mistake.** The draft says so explicitly. Do not let it harden into "two independent confirmations" in later retellings — cmux also frames its numbers as accumulation over uptime, whereas our own sampling found the footprint flat and tracking session count instead. Same proportions, different diagnosis.
- **Ask 1 may not actually free anything.** If the pool is a `CAMetalLayer`, lowering the drawable count drains lazily as drawables recycle — which requires drawing, and an occluded surface is not drawing. The draft raises this itself and offers an explicit purge as the equivalent ask.
- The two "minor observations" (orphaned `CAMetalLayer`, unconditional `set_size`) were kept rather than split out, because this issue's own Notes directed that they be mentioned. They are framed as low-confidence context, not asks.

## Relation

- Child of umbrella #0285.
- Complements #0288: occlusion signalling stops growth; this issue is about reclaiming footprint occlusion cannot touch.
- #0291 experiment (b) (`setSurfaceVisible(false)` vs IOSurface region count) determines urgency: if occlusion already frees the swap chain in this Ghostty revision (unverifiable from the stripped static library), this issue is unnecessary. The investigation's confidence that the swap chain survives occlusion is **medium** — verify by measurement before filing upstream.

## Expected behavior

An occluded/hidden surface's GPU resources (swap-chain IOSurfaces at minimum) can be released and later reacquired without destroying the surface or its PTY, so graphics footprint scales with *visible* Terminal Sessions.

## Actual behavior

The only lever that frees GPU memory is `ghostty_surface_free`, which also destroys the terminal state and kills the PTY. Hidden surfaces hold full-size swap chains indefinitely (`updatePlacements` only writes `view.frame` when `placement.isVisible` — `TerminalHostStore.swift:209-212` — so a hidden Tab's chain stays at its last visible size).

## Attachments

[Upstream issue draft, ready for submission](0293/upstream-issue.md)

## Notes

Two minor related upstream nits, recorded here rather than as their own tickets:

- `AppTerminalView` orphans the `CAMetalLayer` it creates in `commonInit` (`AppTerminalView.swift:15, 55-63`): Ghostty replaces `self.layer` with an `IOSurfaceLayer`, but the ivar keeps the original alive with a live `MTLDevice`, and `updateMetalLayerMetrics` keeps writing `drawableSize` to it (`AppTerminalView+Lifecycle.swift:188-192`). Small (a `CAMetalLayer` allocates no drawables until `nextDrawable`), but plainly unintended. Confidence it matters for footprint: low.
- `synchronizeMetrics` calls `ghostty_surface_set_size` unconditionally (`TerminalSurfaceCoordinator.swift:173`) ahead of its own change guard at `:184`, and both `layout()` and `setFrameSize` invoke it on every AppKit layout pass (`AppTerminalView+Lifecycle.swift:144-154`). Harmless if Ghostty short-circuits an unchanged size; a per-layout renderer resize if it doesn't. Confidence: low.

Both are worth a mention in the upstream filing. The orphaned-`CAMetalLayer` nit is also cross-referenced by #0294 (the 2×-content-area drawable anomaly), since `updateMetalLayerMetrics` writing `drawableSize` to the orphaned layer is a plausible mechanism there.

Sources: `issues/0285/investigation-source.md` §2 (the occlusion nuance), §Summary verdict (b), Proposed follow-ups 4, 6, 7; `issues/0285/investigation-live.md` § Attribution (the measured 3:1 drawable ratio).

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-08-01 | claude-sonnet-5 | 9,642 | 4,043 | 5,063,799 | 133,110 | $1.41 |
| 2026-08-01 | claude-opus-5 | 48 | 293 | 1,881,418 | 121,027 | $1.70 |
| 2026-08-01 | claude-sonnet-5 | 110 | 5,655 | 9,198,937 | 199,122 | $2.39 |
| 2026-08-01 | claude-sonnet-5 | 52 | 1,797 | 5,385,132 | 230,398 | $1.67 |

**Total: $7.17** (one session drafted both #0292 and #0293 and one reviewed both; costs are recorded on each issue, not doubled — do not sum across the two)
